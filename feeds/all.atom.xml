<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom"><title>Dao Malick BENIN</title><link href="https://dmb225.github.io/" rel="alternate"/><link href="https://dmb225.github.io/feeds/all.atom.xml" rel="self"/><id>https://dmb225.github.io/</id><updated>2026-02-08T00:00:00+01:00</updated><entry><title>System Design: Lesson 7.1 - Sequencer</title><link href="https://dmb225.github.io/system-design-lesson-71-sequencer.html" rel="alternate"/><published>2026-02-08T00:00:00+01:00</published><updated>2026-02-08T00:00:00+01:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2026-02-08:/system-design-lesson-71-sequencer.html</id><summary type="html">&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;p&gt;There can be millions of events happening per second in a large distributed system. Commenting on a post on Facebook, sharing a Tweet, and posting a picture on Instagram are just a few examples of such events. We need a mechanism to distinguish these events from each other. One …&lt;/p&gt;</summary><content type="html">&lt;h2&gt;Motivation&lt;/h2&gt;
&lt;p&gt;There can be millions of events happening per second in a large distributed system. Commenting on a post on Facebook, sharing a Tweet, and posting a picture on Instagram are just a few examples of such events. We need a mechanism to distinguish these events from each other. One such mechanism is the assignment of globally unique IDs to each of these events.&lt;/p&gt;
&lt;p&gt;Assigning a primary key to an entry in a database is another use case of a unique ID. Usually, the auto-increment feature in databases fulfills this requirement. However, that feature won’t work for a distributed database, where different nodes independently generate the identifiers. For this case, we need a unique ID generator that acts as a primary key in a distributed setting—for example, a horizontally-sharded table.&lt;/p&gt;
&lt;p&gt;A unique ID helps us identify the flow of an event in the logs and is useful for debugging. A real-world example of unique ID usage is Facebook’s end-to-end performance tracing and analysis system, Canopy. Canopy uses TraceID to identify an event uniquely across the execution path that may potentially perform hundreds of microservices to fulfill one user request.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Explain why we need to design a time-sortable unique ID generator.&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Answer:&lt;/strong&gt; A unique ID generator that is time-sortable is important because it helps maintain a chronological order of events or objects within a system. This ordering is crucial for analyzing event sequences, debugging issues, and performing efficient database queries, as it allows quick access to the most recent or relevant data.&lt;/p&gt;
&lt;h2&gt;How do we design a sequencer?&lt;/h2&gt;
&lt;p&gt;We’ve divided the sequencer’s comprehensive design into the following two lessons:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Design of a Unique ID Generator: After enlisting the requirements of the design, we discuss three ways to generate unique IDs: using UUID, using a database, and using a range handler.&lt;/li&gt;
&lt;li&gt;Unique IDs with Causality: In this lesson, we incorporate an additional factor of time in the generation of IDs and explain the process by taking causality into consideration.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Unique IDs are important for identifying events and objects within a distributed system. However, designing a unique ID generator within a distributed system is challenging. In the next lesson, let’s look at the requirements for a distributed unique ID generation system.&lt;/p&gt;</content><category term="Blog"/><category term="system-design"/><category term="sequencer"/></entry><entry><title>System Design: Lesson 7.2 - Sequencer: Design of a Unique ID Generator</title><link href="https://dmb225.github.io/system-design-lesson-72-sequencer-design-of-a-unique-id-generator.html" rel="alternate"/><published>2026-02-08T00:00:00+01:00</published><updated>2026-02-08T00:00:00+01:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2026-02-08:/system-design-lesson-72-sequencer-design-of-a-unique-id-generator.html</id><summary type="html">&lt;p&gt;In the previous lesson, we saw how unique identifiers are used in many systems, for example, to label tweets, uploaded videos, or track execution flow across services. In this lesson, we will build on that by exploring different ways to design a system that generates such unique IDs. We will …&lt;/p&gt;</summary><content type="html">&lt;p&gt;In the previous lesson, we saw how unique identifiers are used in many systems, for example, to label tweets, uploaded videos, or track execution flow across services. In this lesson, we will build on that by exploring different ways to design a system that generates such unique IDs. We will compare approaches like UUIDs, databases, and range handlers, and see how they meet key requirements identified in the following section.&lt;/p&gt;
&lt;h2&gt;Requirements for unique identifiers&lt;/h2&gt;
&lt;p&gt;The requirements for our system are as follows:
- Uniqueness: We need to assign unique identifiers to different events for identification purposes.
- Scalability: The ID generation system should generate at least a billion unique IDs per day.
- Availability: Since multiple events happen even at the level of nanoseconds, our system should generate IDs for all the events that occur.
- 64-bit numeric ID: We restrict the length to 64 bits because this bit size is enough for many years in the future. Let’s calculate the number of years after which our ID range will wrap around.&lt;/p&gt;
&lt;p&gt;64 bits should be enough for a unique ID length.&lt;/p&gt;
&lt;h2&gt;First solution: UUID&lt;/h2&gt;
&lt;p&gt;A straw man solution for our design uses UUIDs (universally unique IDs). This is a 128-bit number and it looks like 123e4567e89b12d3a456426614174000 in hexadecimal. It gives us about 10^38 numbers. UUIDs have different versions. We opt for version 4, which generates a pseudorandom number.&lt;/p&gt;
&lt;p&gt;Each server can generate its own ID and assign the ID to its respective event. No coordination is needed for UUID since it’s independent of the server. Scaling up and down is easy with UUID, and this system is also highly available. Furthermore, it has a low probability of collisions.&lt;/p&gt;
&lt;h3&gt;Cons of using the UUID approach&lt;/h3&gt;
&lt;p&gt;Using 128-bit numbers as primary keys makes the primary-key indexing slower, which results in slow inserts. A workaround might be to interpret an ID as a hex string instead of a number. However, non-numeric identifiers might not be suitable for many use cases. The ID isn’t of 64-bit size. Moreover, there’s a chance of duplication. Although this chance is minimal, we can’t claim UUID to be deterministically unique. Additionally, UUIDs given to clients over time might not be monotonically increasing. The following table summarizes the requirements we have fulfilled using UUID:&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;&lt;/th&gt;
&lt;th&gt;Unique&lt;/th&gt;
&lt;th&gt;Sca﻿lable&lt;/th&gt;
&lt;th&gt;Available&lt;/th&gt;
&lt;th&gt;64-bit numeric ID&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Using UUID&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;h2&gt;Second solution: using a database&lt;/h2&gt;
&lt;p&gt;Let’s try mimicking the auto-increment feature of a database. Consider a central database that provides a current ID and then increments the value by one. We can use the current ID as a unique identifier for our events.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;What can be a potential problem of using a central database?&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Answer:&lt;/strong&gt; This design has a considerable problem: a single point of failure. Reliance on one database can severely affect a system. The entire system will stop working if the central database goes down.&lt;/p&gt;
&lt;p&gt;To cater to the problem of a single point of failure, we modify the conventional auto-increment feature that increments by one. Instead of incrementing by one, let’s rely on a value m, where m equals the number of database servers we have. Each server generates an ID, and the following ID adds m to the previous value. This method is scalable and prevents the duplication of IDs.&lt;/p&gt;
&lt;h3&gt;Pros of using a database&lt;/h3&gt;
&lt;p&gt;This approach is scalable. We can add more servers, and the value of m will be updated accordingly.&lt;/p&gt;
&lt;h3&gt;Cons of using a database&lt;/h3&gt;
&lt;p&gt;Though this method is somewhat scalable, it’s difficult to scale for multiple data centers. The task of adding and removing a server can result in duplicate IDs. For example, suppose m=3, and server A generates the unique IDs 1, 4, and 7. Server B generates the IDs 2, 5, and 8, while server C generates the IDs 3, 6, and 9. Server B faces downtime due to some failure. Now, the value m is updated to 2. Server A generates 9 as its following unique ID, but this ID has already been generated by server C. Therefore, the IDs aren’t unique anymore.&lt;/p&gt;
&lt;p&gt;The table below highlights the limitations of our solution. A unique ID generation system shouldn’t be a single point of failure (SPOF). It should be scalable and available.&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;&lt;/th&gt;
&lt;th&gt;Unique&lt;/th&gt;
&lt;th&gt;Scalable&lt;/th&gt;
&lt;th&gt;Available&lt;/th&gt;
&lt;th&gt;64-bit numeric ID&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Using UUID&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Using database servers&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;h2&gt;Third solution: using a range handler&lt;/h2&gt;
&lt;p&gt;Let’s try to overcome the problems identified in the previous methods. We can use ranges in a central server. Suppose we have multiple ranges for one to two billion, such as 1 to 1,000,000; 1,000,001 to 2,000,000; and so on. In such a case, a central microservice can provide a range to a server upon request.&lt;/p&gt;
&lt;p&gt;Any server can claim a range when it needs it for the first time or if it runs out of the range. Suppose a server has a range, and now it keeps the start of the range in a local variable. Whenever a request for an ID is made, it provides the local variable value to the requestor and increments the value by one.&lt;/p&gt;
&lt;p&gt;Let’s say server 1 claims the number range 300,001 to 400,000. After this range claim, the user ID 300,001 is assigned to the first request. The server then returns 300,002 to the next user, incrementing its current position within the range. This continues until user ID 400,000 is released by the server. The application server then queries the central server for the next available range and repeats this process.&lt;/p&gt;
&lt;p&gt;This resolves the problem of the duplication of user IDs. Each application server can respond to requests concurrently. We can add a load balancer over a set of servers to mitigate the load of requests.&lt;/p&gt;
&lt;p&gt;We use a microservice called range handler that keeps a record of all the taken and available ranges. The status of each range can determine if a range is available or not. The state—that is, which server has what range assigned to it—can be saved on a replicated storage.&lt;/p&gt;
&lt;p&gt;This microservice can become a single point of failure, but a failover server acts as the savior in that case. The failover server hands out ranges when the main server is down. We can recover the state of available and unavailable ranges from the latest checkpoint of the replicated store.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Imagine a scenario where you’re planning to bring a new data center online to accommodate increasing user requests. How would you modify your range allocation strategy to accommodate this change?&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Answer:&lt;/strong&gt; To accommodate a new data center, you should update your range allocation strategy by redistributing data ranges more evenly across all data centers. Techniques like geographic sharding can be used to assign data based on user location, reducing latency. You might also split existing ranges or migrate some to the new data center. Applying consistent hashing helps maintain balance and minimizes disruption during reallocation. Additionally, implementing load balancing ensures requests are efficiently distributed across the infrastructure.&lt;/p&gt;
&lt;h3&gt;Pros of using a range handler&lt;/h3&gt;
&lt;p&gt;This system is scalable, available, and yields user IDs that have no duplicates. Moreover, we can maintain this range in 64 bits, which is numeric.&lt;/p&gt;
&lt;h3&gt;Cons of using a range handler&lt;/h3&gt;
&lt;p&gt;We lose a significant range when a server dies and can only provide a new range once it’s live again. We can overcome this shortcoming by allocating shorter ranges to the servers, although ranges should be large enough to serve identifiers for a while.&lt;/p&gt;
&lt;p&gt;The following table sums up what this approach fulfills for us:&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;&lt;/th&gt;
&lt;th&gt;Unique&lt;/th&gt;
&lt;th&gt;Scalable&lt;/th&gt;
&lt;th&gt;Available&lt;/th&gt;
&lt;th&gt;64-bit numeric ID&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Using UUID&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Using database servers&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Using a range handler&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;We developed a solution that provides us with a unique ID, which we can assign to various events and even use as a primary key. But what if we add a requirement that the ID is time sortable too?&lt;/p&gt;</content><category term="Blog"/><category term="system-design"/><category term="sequencer"/></entry><entry><title>System Design: Lesson 7.3 - Sequencer: Unique IDs with Causality</title><link href="https://dmb225.github.io/system-design-lesson-73-sequencer-unique-ids-with-causality.html" rel="alternate"/><published>2026-02-08T00:00:00+01:00</published><updated>2026-02-08T00:00:00+01:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2026-02-08:/system-design-lesson-73-sequencer-unique-ids-with-causality.html</id><summary type="html">&lt;h2&gt;Causality&lt;/h2&gt;
&lt;p&gt;In the previous lesson, we generated unique IDs to differentiate between various events. Apart from having unique identifiers for events, we’re also interested in finding the sequence of these events. Let’s consider an example where Peter and John are two Twitter users. John posts a comment (event …&lt;/p&gt;</summary><content type="html">&lt;h2&gt;Causality&lt;/h2&gt;
&lt;p&gt;In the previous lesson, we generated unique IDs to differentiate between various events. Apart from having unique identifiers for events, we’re also interested in finding the sequence of these events. Let’s consider an example where Peter and John are two Twitter users. John posts a comment (event A), and Peter replies to John’s comment (event B). Event B is dependent on event A and can’t happen before it. The events are not concurrent here.&lt;/p&gt;
&lt;p&gt;We can also have concurrent events—that is, two events that occur independently of each other. For example, if Peter and John comment on two different Tweets, there’s no happened-before relationship or causality between them. It’s essential to identify the dependence of one event over the other but not in the case of concurrent events.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Note:&lt;/em&gt; The scenario described above can also be handled by assigning a unique ID and encoding the dependence of events using a social graph. We might also use a separate time data structure and a simple unique ID. However, we want a unique ID to do double duty—provide unique identification and also help with the causality of events.&lt;/p&gt;
&lt;p&gt;Some applications need the events to have unique identifiers and carry any relevant causality information. An example of this is giving an identifier to the concurrent writes of a key into a key-value store to implement the last-write-wins strategy.&lt;/p&gt;
&lt;p&gt;We can either use logical or physical clocks to infer causality. Some systems have additional requirements where we want event identifiers’ causality to map wall-clock time. An example of this is a financial application that complies with the European MiFID regulations. MiFID requires clocks to be within 100 microseconds of UTC to detect anomalies during high-volume/high-speed market trades.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Note:&lt;/em&gt; There are many subtleties associated with logical or physical clocks. We can refer to the text below titled “Time in a Distributed System” to refresh our concepts of time.&lt;/p&gt;
&lt;h2&gt;Use UNIX time stamps&lt;/h2&gt;
&lt;p&gt;UNIX time stamps are granular to the millisecond and can be used to distinguish different events. We have an ID-generating server that can generate one ID in a single millisecond. Any request to generate a unique ID is routed to that server, which returns a timestamp and then returns a unique ID. The ability to generate an ID in milliseconds allows us to generate a thousand identifiers per second. This means we can get t = 24 (hour) × 60 (min/hour) × 60 (sec/min) × 1000 (ID/sec) = 86,400,000 IDs in a day. That’s less than a billion per day.&lt;/p&gt;
&lt;p&gt;Our system works well with generating IDs, but it poses a crucial problem. The ID-generating server is a single point of failure (SPOF), and we need to handle it. To cater to SPOF, we can add more servers. Each server generates a unique ID for every millisecond. To make the overall identifier unique across the system, we attach the server ID with the UNIX time stamp. Then, we add a load balancer to distribute the traffic more efficiently.&lt;/p&gt;
&lt;h3&gt;Pros&lt;/h3&gt;
&lt;p&gt;This approach is simple, scalable, and easy to implement. It also enables multiple servers to handle concurrent requests.&lt;/p&gt;
&lt;h3&gt;Cons&lt;/h3&gt;
&lt;p&gt;For two concurrent events, the same time stamp is returned and the same ID can be assigned to them. This way, the IDs are no longer unique.&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;&lt;/th&gt;
&lt;th&gt;Unique&lt;/th&gt;
&lt;th&gt;Sca﻿lable&lt;/th&gt;
&lt;th&gt;Available&lt;/th&gt;
&lt;th&gt;64-bit numeric ID&lt;/th&gt;
&lt;th&gt;Causality maintained&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Using UUID&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Using a database&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Using a range handler&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Using UNIX time stamps&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;td&gt;weak&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;h2&gt;TrueTime API&lt;/h2&gt;
&lt;p&gt;Google’s TrueTime API in Spanner is an interesting option. Instead of a particular time stamp, it reports an interval of time. When asking for the current time, we get back two values: the earliest and latest ones. These are the earliest possible and latest possible time stamps.&lt;/p&gt;
&lt;p&gt;Based on its uncertainty calculations, the clock knows that the actual current time is somewhere within that interval. The width of the interval depends, among other things, on how long it has been since the local quartz clock was last synchronized with a more accurate clock source.&lt;/p&gt;
&lt;p&gt;Google deploys a GPS receiver or atomic clock in each data center, and clocks are synchronized within about 7 ms. This allows Spanner to keep the clock uncertainty to a minimum. The uncertainty of the interval is represented as epsilon.&lt;/p&gt;
&lt;p&gt;Spanner guarantees that if two confidence intervals don’t overlap (that is, A_earliest &amp;lt; A_latest &amp;lt; B_earliest &amp;lt; B_latest), then B definitely happened after A.&lt;/p&gt;
&lt;p&gt;We generate our unique ID using TrueTime intervals. Let’s say:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Earliest interval: &lt;code&gt;T_E&lt;/code&gt;  &lt;/li&gt;
&lt;li&gt;Latest interval: &lt;code&gt;T_L&lt;/code&gt;  &lt;/li&gt;
&lt;li&gt;Uncertainty: &lt;code&gt;ε&lt;/code&gt;  &lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;We use &lt;code&gt;T_E&lt;/code&gt; in &lt;strong&gt;milliseconds&lt;/strong&gt; as a timestamp in our unique ID.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;ID structure:&lt;/strong&gt;
- &lt;strong&gt;Time stamp (41 bits):&lt;/strong&gt;&lt;br&gt;
  We use &lt;code&gt;T_E&lt;/code&gt; as the timestamp.
- &lt;strong&gt;Uncertainty (4 bits):&lt;/strong&gt;&lt;br&gt;
  Since the maximum uncertainty is claimed to be 6–10 ms, we store it using 4 bits.
- &lt;strong&gt;Worker number (10 bits):&lt;/strong&gt;&lt;br&gt;
  This gives us &lt;code&gt;2^10 = 1,024&lt;/code&gt; possible worker IDs.
- &lt;strong&gt;Sequence number (8 bits):&lt;/strong&gt;&lt;br&gt;
  For every ID generated on the server, the sequence number is incremented by one.&lt;br&gt;
  This gives &lt;code&gt;2^8 = 256&lt;/code&gt; combinations, and we reset it to zero when it reaches 256.&lt;/p&gt;
&lt;h3&gt;Pros&lt;/h3&gt;
&lt;p&gt;TrueTime satisfies all the requirements. We’re able to generate a globally unique 64-bit identifier. The causality of events is maintained. The approach is scalable and highly available.&lt;/p&gt;
&lt;h3&gt;Cons&lt;/h3&gt;
&lt;p&gt;If two intervals overlap, then we’re unsure in what order A and B occurred. It’s possible that they’re concurrent events, but a 100% guarantee can’t be given. Additionally, Spanner is expensive because it ensures high database consistency. The dollar cost of a Spanner-like system is also high due to its elaborate infrastructure needs and monitoring.&lt;/p&gt;
&lt;p&gt;The updated table provides the comparison between the different system designs for generating a unique ID.&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;&lt;/th&gt;
&lt;th&gt;Unique&lt;/th&gt;
&lt;th&gt;Scalable&lt;/th&gt;
&lt;th&gt;Available&lt;/th&gt;
&lt;th&gt;64-bit numeric ID&lt;/th&gt;
&lt;th&gt;Causality maintained&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Using UUID&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Using a database&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Using a range handler&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Using UNIX time stamps&lt;/td&gt;
&lt;td&gt;✖️&lt;/td&gt;
&lt;td&gt;weak&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;weak&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Using TrueTime&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;td&gt;✔️&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;&lt;em&gt;Consider a scenario: A data center loses power for 30 minutes. After recovering, the machine clocks are out of sync by 5 minutes. What issues might this cause, and how would you resolve them?&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Answer:&lt;/strong&gt;  The issues include clock drift causing IDs with inaccurate timestamps and collision risks. To resolve this, synchronize clocks with NTP before bringing machines online, prevent ID generation until synchronization, or temporarily rely on sequence numbers without timestamps.&lt;/p&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;We want to avoid duplicate identifiers. Consider what will happen if duplicate payment or purchase orders are generated.&lt;/li&gt;
&lt;li&gt;UUIDs provide probabilistic guarantees about the keys’ non-collision. Deterministically getting non-collision guarantees might need consensus among different distributed entities or stores and read from the replicated store.&lt;/li&gt;
&lt;li&gt;As key length becomes large, it often causes slower tuple updates in a database. Therefore, identifiers should be big enough but not too big.&lt;/li&gt;
&lt;li&gt;Often, it’s desirable that no one is able to guess the next ID. Otherwise, undesirable data leaks can happen, and the organization’s competitors may learn how many orders were processed in a day by simply looking at order IDs. Adding a few random numbers to the bits of the identifier make it hard to guess, although this comes at a performance cost.&lt;/li&gt;
&lt;li&gt;We can use simple counters for generating unique IDs if we don’t want to relate ID to time. Fetching time stamps is slower than simple counters.&lt;/li&gt;
&lt;li&gt;Fetching time stamps is slower than simple counters, though this requires that we store generated IDs persistently. The counter needs to be stored in the database. Storage comes with its own issues. These include multiple concurrent writes becoming overwhelming for the database and the database being the single point of failure.&lt;/li&gt;
&lt;li&gt;For some distributed databases, such as Spanner, it can hurt to generate monotonically increasing or decreasing IDs. Google reports the following: “In fact, using monotonically increasing (or decreasing) values as row keys does not follow best practices in Spanner because it creates hotspots in the database, leading to a reduction in performance.”&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;**Note: Globally ordering events is an expensive procedure. A feature that was fast and simple in a centralized database (auto-increment based ID) becomes slow and complicated in its distributed counterpart due to some fundamental constraints (such as consensus, which is difficult among remote entities).&lt;/p&gt;
&lt;p&gt;For example, Spanner, a geographically distributed database, reports that “if a read-update transaction on a single cell (one column in a single row) has a latency of 10 milliseconds (ms), then the maximum theoretical frequency of issuing of sequence values is 100 per second. This maximum applies to the entire database, regardless of the number of client application instances, or the number of nodes in the database. This is because a single node always manages a single row.” If we could compromise on the requirements for global orderings and gapless identifiers, we would be able to get many identifiers in a shorter time, that is, a better performance.**&lt;/p&gt;</content><category term="Blog"/><category term="system-design"/><category term="sequencer"/></entry><entry><title>System Design: Lesson 6.1 - The Content Delivery Network (CDN)</title><link href="https://dmb225.github.io/system-design-lesson-61-the-content-delivery-network-cdn.html" rel="alternate"/><published>2026-02-07T00:00:00+01:00</published><updated>2026-02-07T00:00:00+01:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2026-02-07:/system-design-lesson-61-the-content-delivery-network-cdn.html</id><summary type="html">&lt;h2&gt;Problem statement&lt;/h2&gt;
&lt;p&gt;Let’s start with a question: If millions of users worldwide use our data-intensive applications, and our service is deployed in a single data center to serve the users’ requests, what possible problems can arise?&lt;/p&gt;
&lt;p&gt;The following problems can arise:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;High latency:&lt;/strong&gt; The user-perceived latency will be high …&lt;/li&gt;&lt;/ul&gt;</summary><content type="html">&lt;h2&gt;Problem statement&lt;/h2&gt;
&lt;p&gt;Let’s start with a question: If millions of users worldwide use our data-intensive applications, and our service is deployed in a single data center to serve the users’ requests, what possible problems can arise?&lt;/p&gt;
&lt;p&gt;The following problems can arise:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;High latency:&lt;/strong&gt; The user-perceived latency will be high due to the physical distance from the serving data center. User-perceived latency has many components, such as transmission delays (a function of available bandwidth), propagation delays (a function of distance), queuing delays (a function of network congestion), and nodal processing delays. Therefore, data transmission over a large distance results in higher latency. Real-time applications require a latency below 200 milliseconds (ms) in general. For the Voice over Internet Protocol (VoIP), latency should not be more than 150 ms, whereas video streaming applications cannot tolerate a latency above a few seconds.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;em&gt;Note:&lt;/em&gt; According to one of the readings taken on December 21, 2021, the average latency from US East (N. Virginia) to US West (N. California) was 62.9 ms. Across continents—for example, from the US East (N. Virginia) to Africa (Cape Town)—was 225.63 ms. This is two-way latency, known as round-trip latency.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Data-intensive applications:&lt;/strong&gt; Data-intensive applications require transferring large traffic. Over a longer distance, this could be a problem due to the network path stretching through different kinds of ISPs. Because of some smaller Path message transmission unit (MTU) links, the throughput of applications on the network might be reduced. Similarly, different portions of the network path might have different congestion characteristics. The problem multiplies as the number of users grows because the origin servers will have to provide the data individually to each user. That is, the primary data center will need to send out a lot of redundant data when multiple clients ask for it. However, applications that use streaming services are both data-intensive and dynamic in nature.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;em&gt;Note:&lt;/em&gt; According to a survey, 78% of the United States consumers use streaming services, which is an increase of 25% in five years.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Scarcity of data center resources:&lt;/strong&gt; Important data center resources like computational capacity and bandwidth become a limitation when the number of users of a service increases significantly. Services engaging millions of users simultaneously need scaling. Even if scaling is achieved in a single data center, it can still suffer from becoming a single point of failure when the data center goes offline due to natural calamity or connectivity issues with the Internet.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;em&gt;Note:&lt;/em&gt; According to one study, YouTube, Netflix, and Amazon Prime collectively generated 80% of Internet traffic in 2020. Circa 2016, the CDN provider Akamai served 15% to 30% of web traffic (about 30 terabits per second). For 90% of Internet users, Akamai was just one hop away. Therefore, we have strong reasons to optimize the delivery and consumption of this data without making the Internet core a bottleneck.&lt;/p&gt;
&lt;p&gt;The solution to these problems is a content delivery network (CDN), a system that brings data closer to users around the world.&lt;/p&gt;
&lt;h2&gt;How will we design a CDN?&lt;/h2&gt;
&lt;p&gt;We’ve divided the design of CDN into six lessons:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Introduction to a CDN: We’ll provide a thorough introduction to CDNs and identify the functional and non-functional requirements.&lt;/li&gt;
&lt;li&gt;Design of a CDN: We’ll explain how to design the CDN. We’ll also briefly describe the API design.&lt;/li&gt;
&lt;li&gt;In-depth Investigation of CDN: Part 1: This lesson explains caching strategies and CDN architecture. Also, we’ll discuss various approaches to finding the nearest proxy server.&lt;/li&gt;
&lt;li&gt;In-depth Investigation of CDN: Part 2: We’ll discuss how to make content consistent in a CDN and the deployment of proxy servers. We’ll also cover the custom and specialized CDN in detail.&lt;/li&gt;
&lt;li&gt;Evaluation of CDN: This lesson will provide an evaluation of our proposed design.&lt;/li&gt;
&lt;/ul&gt;</content><category term="Blog"/><category term="system-design"/><category term="cdn"/></entry><entry><title>System Design: Lesson 6.2 - Introduction to a CDN</title><link href="https://dmb225.github.io/system-design-lesson-62-introduction-to-a-cdn.html" rel="alternate"/><published>2026-02-07T00:00:00+01:00</published><updated>2026-02-07T00:00:00+01:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2026-02-07:/system-design-lesson-62-introduction-to-a-cdn.html</id><summary type="html">&lt;p&gt;A content delivery network (CDN) is a distributed system of servers placed in many geographic locations to improve the speed and reliability of delivering online content. Rather than relying on a single origin server that holds the original version of the data, a CDN uses a network of proxy servers …&lt;/p&gt;</summary><content type="html">&lt;p&gt;A content delivery network (CDN) is a distributed system of servers placed in many geographic locations to improve the speed and reliability of delivering online content. Rather than relying on a single origin server that holds the original version of the data, a CDN uses a network of proxy servers positioned closer to users. When requests arrive, these nearby servers act as intermediaries, providing content on behalf of the origin server. This setup reduces the load on the origin server and helps protect it from traffic spikes and potential attacks.&lt;/p&gt;
&lt;p&gt;The proxy servers are placed at the network edge and are often called edge servers. By storing copies of content near end users, edge servers reduce latency, save bandwidth, and deliver data more quickly. A basic proxy simply forwards requests, but a CDN adds intelligence that optimizes how and where content is delivered.&lt;/p&gt;
&lt;p&gt;The guiding idea behind a CDN is to minimize the distance that data must travel to reach the user. To achieve this, CDNs place small data centers near users and store copies of the required content there. By storing both static and dynamic content close to end users, CDNs can significantly reduce propagation delay, which is the time it takes for data to travel across the network. They also work to reduce transmission and queuing delays by ensuring that sufficient bandwidth is available along the delivery path, often within the user’s ISP.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Note:&lt;/em&gt; Cache is often confused with CDN. Cache is a local storage method for frequently used data, while a CDN spreads content across multiple locations to reach users faster and more reliably.&lt;/p&gt;
&lt;p&gt;Let’s look at the different ways CDN solves the discussed problems:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;High latency:&lt;/strong&gt; A CDN brings content closer to end-users via edge servers, reducing the physical distance data travels and improving response times. This also reduces network congestion and propagation delays.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Data-intensive applications:&lt;/strong&gt; Since the path to the data includes only the ISP and the nearby CDN components, there’s no issue in serving a large number of users through a few CDN components in a specific area. As shown below, the origin data center will need to provide the data to local CDN components only once, whereas local CDN components can then provide the data to different users individually. No user will have to download their own copy of data from the origin servers.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;em&gt;Note:&lt;/em&gt; Various streaming protocols are used to deliver dynamic content by the CDN providers. For example, CDNsun uses the Real-time Messaging Protocol (RTMP), HTTP Live Streaming (HLS), Real-time Streaming Protocol (RTSP), and many more to deliver dynamic content.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Scarcity of data center resources:&lt;/strong&gt; A CDN is used to serve popular content. For this reason, most traffic is handled at the CDN instead of the origin servers. So, different local or distributed CDN components share the load on origin servers.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;em&gt;Note:&lt;/em&gt; A few well-known CDN providers are Akamai, StackPath, Cloudflare, Rackspace, Amazon CloudFront, and Google Cloud CDN.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Does a CDN cache all content from the origin server?&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Answer:&lt;/strong&gt; Not likely. A CDN caches a considerable portion of the content depending on its capabilities, and it mostly caches the static content.&lt;/p&gt;
&lt;p&gt;It also depends on the size of the content. For example, it might be possible for Netflix to store more than 90% of its movies in the CDN, while this might not be feasible for a service like YouTube due to the humongous volume of content.&lt;/p&gt;
&lt;h2&gt;Functional requirements&lt;/h2&gt;
&lt;p&gt;The following functional requirements will be a part of our design:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Retrieve: Depending upon the type of CDN models, a CDN should be able to retrieve content from the origin servers. We’ll cover CDN models in the coming lesson.&lt;/li&gt;
&lt;li&gt;Request: Content delivery from the proxy server is made upon the user’s request. CDN proxy servers should be able to respond to each user’s request in this regard.&lt;/li&gt;
&lt;li&gt;Deliver: In the case of the push model, the origin servers should be able to send the content to the CDN proxy servers.&lt;/li&gt;
&lt;li&gt;Search: The CDN should be able to execute a search against a user query for cached or otherwise stored content within the CDN infrastructure.&lt;/li&gt;
&lt;li&gt;Update: In most cases, content comes from the origin server, but if we run a script in a CDN, the CDN should be able to update the content within peer CDN proxy servers in a PoP.&lt;/li&gt;
&lt;li&gt;Delete: Depending upon the type of content (static or dynamic), it should be possible to delete cached entries from the CDN servers after a certain period.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Non-functional requirements&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Performance: Minimizing latency is one of the core missions of a CDN. The proposed design should have the minimum possible latency.&lt;/li&gt;
&lt;li&gt;Availability: CDNs are expected to be available at all times because of their effectiveness. Availability includes protection against attacks like DDoS and maintaining service continuity.&lt;/li&gt;
&lt;li&gt;Scalability: An increasing number of users will request content from CDNs. Our proposed CDN design should be able to scale horizontally as the requirements increase.&lt;/li&gt;
&lt;li&gt;Reliability and security: Our CDN design should ensure no single point of failure. Apart from failures, the designed CDN must reliably handle massive traffic loads. Furthermore, CDNs should provide protection to hosted content from various attacks.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Building blocks we will use&lt;/h2&gt;
&lt;p&gt;The design of a CDN utilizes the following building blocks:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;DNS is the service that maps human-friendly CDN domain names to machine-readable IP addresses. This IP address will take the users to the specified proxy server.&lt;/li&gt;
&lt;li&gt;Load balancers distribute millions of requests among the operational proxy servers.
In the next lesson, we’ll discuss the design of the CDN.&lt;/li&gt;
&lt;/ul&gt;</content><category term="Blog"/><category term="system-design"/><category term="cdn"/></entry><entry><title>System Design: Lesson 6.3 - CDN Design</title><link href="https://dmb225.github.io/system-design-lesson-63-cdn-design.html" rel="alternate"/><published>2026-02-07T00:00:00+01:00</published><updated>2026-02-07T00:00:00+01:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2026-02-07:/system-design-lesson-63-cdn-design.html</id><summary type="html">&lt;h2&gt;CDN design&lt;/h2&gt;
&lt;p&gt;We’ll explain our CDN design in two phases. In the first phase, we’ll cover the components that comprise a CDN. By the end of this phase, we’ll understand why we need a specific component. In the second phase, we’ll explore the workflow by explaining …&lt;/p&gt;</summary><content type="html">&lt;h2&gt;CDN design&lt;/h2&gt;
&lt;p&gt;We’ll explain our CDN design in two phases. In the first phase, we’ll cover the components that comprise a CDN. By the end of this phase, we’ll understand why we need a specific component. In the second phase, we’ll explore the workflow by explaining how each component interacts with others to develop a fully functional CDN. Let’s dive in.&lt;/p&gt;
&lt;h2&gt;CDN components&lt;/h2&gt;
&lt;p&gt;The following components comprise a CDN:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Clients: End users use various clients, like browsers, smartphones, and other devices, to request content from the CDN.&lt;/li&gt;
&lt;li&gt;Routing system: The routing system directs clients to the nearest CDN facility. To do that effectively, this component receives input from various systems to understand where content is placed, how many requests are made for particular content, the load a particular set of servers is handling, and the URI (Uniform Resource Identifier) namespace of various contents. In the next lesson, we’ll discuss different routing mechanisms to forward users to the nearest CDN facility.&lt;/li&gt;
&lt;li&gt;Scrubber servers: Scrubber servers are used to separate the good traffic from malicious traffic and protect against well-known attacks, like DDoS. Scrubber servers are generally used only when an attack is detected. In that case, the traffic is scrubbed or cleaned and then routed to the target destination.&lt;/li&gt;
&lt;li&gt;Proxy servers: The proxy or edge proxy servers serve the content from RAM to the users. Proxy servers store hot data in RAM, though they can store cold data in SSD or hard drive as well. These servers also provide accounting information and receive content from the distribution system.&lt;/li&gt;
&lt;li&gt;Distribution system: The distribution system is responsible for distributing content to all the edge proxy servers to different CDN facilities. This system uses the Internet and intelligent broadcast-like approaches to distribute content across the active edge proxy servers.&lt;/li&gt;
&lt;li&gt;Origin servers: The CDN infrastructure facilitates users with data received from the origin servers. The origin servers serve any unavailable data at the CDN to clients. Origin servers will use appropriate stores to keep content and other mapping metadata. Though, we won’t discuss the internal architecture of origin infrastructure here.&lt;/li&gt;
&lt;li&gt;Management system: The management systems are important in CDNs from a business and managerial aspect where resource usage and statistics are constantly observed. This component measures important metrics, like latency, downtime, packet loss, server load, and so on. For third-party CDNs, accounting information can also be used for billing purposes.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Workflow&lt;/h2&gt;
&lt;p&gt;The workflow for the abstract design is given below:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;The origin servers provide the URI namespace delegation of all objects cached in the CDN to the request routing system.&lt;/li&gt;
&lt;li&gt;The origin server publishes the content to the distribution system responsible for data distribution across the active edge proxy servers.&lt;/li&gt;
&lt;li&gt;The distribution system distributes the content among the proxy servers and provides feedback to the request routing system. This feedback is helpful in optimizing the selection of the nearest proxy server for a requesting client. This feedback contains information about which content is cached on which proxy server to route traffic to relevant proxy servers.&lt;/li&gt;
&lt;li&gt;The client requests the routing system for a suitable proxy server from the request routing system.&lt;/li&gt;
&lt;li&gt;The request routing system returns the IP address of an appropriate proxy server.&lt;/li&gt;
&lt;li&gt;The client request routes through the scrubber servers for security reasons.&lt;/li&gt;
&lt;li&gt;The scrubber server forwards good traffic to the edge proxy server.&lt;/li&gt;
&lt;li&gt;The edge proxy server serves the client request and periodically forwards accounting information to the management system. The management system updates the origin servers and sends feedback to the routing system about the statistics and detail of the content. However, the request is routed to the origin servers if the content isn’t available in the proxy servers. It’s also possible to have a hierarchy of proxy servers if the content isn’t found in the edge proxy servers. For such cases, the request gets forwarded to the parent proxy servers.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;API Design&lt;/h2&gt;
&lt;p&gt;This section will discuss the API design of the functionalities offered by CDN. This will help us understand how the CDN will receive requests from the clients, receive content from the origin servers, and communicate to other components in the network. Let’s develop APIs for each of the following functionalities:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Retrieve content&lt;/li&gt;
&lt;li&gt;Deliver content&lt;/li&gt;
&lt;li&gt;Request content&lt;/li&gt;
&lt;li&gt;Search content&lt;/li&gt;
&lt;li&gt;Update content&lt;/li&gt;
&lt;li&gt;Delete content&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Content can be anything, like a file, video, audio, or other web object. Here, we’ll use the word “content” to refer to all of the above. For clarity, we won’t discuss the privacy-related parameters—like if the content is public or private, who should be able to access this content, if it should be encrypted, and so on—in the following APIs.&lt;/p&gt;
&lt;h3&gt;Retrieve (proxy server to origin server)&lt;/h3&gt;
&lt;p&gt;If the proxy servers request content, the GET method retrieves the content through the /retrieveContent API below:
&lt;code&gt;retrieveContent(proxyserver_id, content_type, content_version, description)&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Let’s see the details of the parameters:&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Parameter&lt;/th&gt;
&lt;th&gt;Description&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;proxyserver_id&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;This is a unique ID of the requesting proxy server.&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;content_type&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;This data structure contains information about the requested content. Specifically, it includes the category (audio, video, document, script, etc.), the type of clients it’s requested for, and the requested quality (if any).&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;content_version&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;This represents the version number of the content. For the &lt;code&gt;/retrieveContent&lt;/code&gt; API, &lt;code&gt;content_version&lt;/code&gt; contains the current version of the content residing on the proxy server. The value will be &lt;code&gt;NULL&lt;/code&gt; if no previous version is available at the proxy server.&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;description&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;This specifies the content details—for example, the video's extension, resolution details, and so on if the &lt;code&gt;content_type&lt;/code&gt; is video.&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;The above API gives a response in a JSON file, which contains the text, content types, links to the images or videos in the content, and so on.&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;pre&gt;&lt;span&gt;&lt;/span&gt;&lt;code&gt;{
  &amp;quot;Object_links&amp;quot;: [
    {
      &amp;quot;name&amp;quot;: &amp;quot;videos&amp;quot;,
      &amp;quot;link&amp;quot;: &amp;quot;https://app_server.com/api/assets/videos/&amp;quot;
    },
    {
      &amp;quot;name&amp;quot;: &amp;quot;illustrations&amp;quot;,
      &amp;quot;link&amp;quot;: &amp;quot;https://app_server.com/api/assets/illustrations/&amp;quot;
    }
  ]
}
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;h3&gt;Deliver (origin server to proxy servers)&lt;/h3&gt;
&lt;p&gt;The origin servers use this API to deliver the specified content, the updated version, to the proxy servers through the distribution system. We call this the /deliverContent API:
&lt;code&gt;deliverContent(origin_id, server_list, content_type, content_version, description)&lt;/code&gt;&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Parameter&lt;/th&gt;
&lt;th&gt;Description&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;origin_id&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;This uniquely identifies each origin server.&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;server_list&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;This specifies the list of servers to which the content will be pushed by the distribution system.&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;content_version&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;This represents the updated version of the content at the origin server. The proxy server receiving the content will discard the previous version.&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;The rest of the parameters have been explained above already.&lt;/p&gt;
&lt;h3&gt;Request (clients to proxy servers)&lt;/h3&gt;
&lt;p&gt;The users use this API to request the content from the proxy servers. We call this the /requestContent API:
&lt;code&gt;requestContent(user_id, content_type, description)&lt;/code&gt;&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Parameter&lt;/th&gt;
&lt;th&gt;Description&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;user_id&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;This is the unique ID of the user who requested the content.&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;The specified proxy server returns the particular content to the requested users in response to the above API.&lt;/p&gt;
&lt;h3&gt;Search (proxy server to peer proxy servers)&lt;/h3&gt;
&lt;p&gt;Although the content is first searched locally at the proxy server, the proxy servers can also probe requested content in the peer proxy servers in the same PoP through the /searchContent API.This could flood the query to all proxy servers in a PoP. Alternatively, we can use a data store in the PoP to query the content, though proxy servers will need to maintain what content is available on which proxy server.&lt;/p&gt;
&lt;p&gt;The /searchContent API is shown below:
&lt;code&gt;searchContent(proxyserver_id, content_type, description)&lt;/code&gt;&lt;/p&gt;
&lt;h3&gt;Update (proxy server to peer proxy servers)&lt;/h3&gt;
&lt;p&gt;The proxy servers use the /updateContent API to update the specified content in the peer proxy servers in the PoP. It does so when specified isolated scripts run on the CDN to provide image resizing, video resolution conversion, security, and many more services. This type of scripting is known as serverless scripting.&lt;/p&gt;
&lt;p&gt;The /updateContent API is shown below:
&lt;code&gt;updateContent(proxyserver_id, content_type, description)&lt;/code&gt;&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Parameter&lt;/th&gt;
&lt;th&gt;Description&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;proxyserver_id&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;This uniquely identifies the proxy server in the PoP for updating the content.&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;The rest of the parameters have been explained above already.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Note:&lt;/em&gt; The Delete API isn’t discussed here. In our caching chapter, we discussed different eviction mechanisms in detail. Those mechanisms are also applicable for a CDN content eviction. Nevertheless, situations can arise where the Delete APIs may be required. We’ll discuss a few content consistency mechanisms, like how much time content stays in the cache, in the next lesson.&lt;/p&gt;
&lt;p&gt;In the upcoming lessons, we’ll dive deep into the characteristics of CDNs.&lt;/p&gt;</content><category term="Blog"/><category term="system-design"/><category term="cdn"/></entry><entry><title>System Design: Lesson 5.1 - The Key-Value Store</title><link href="https://dmb225.github.io/system-design-lesson-51-the-key-value-store.html" rel="alternate"/><published>2026-02-06T00:00:00+01:00</published><updated>2026-02-06T00:00:00+01:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2026-02-06:/system-design-lesson-51-the-key-value-store.html</id><summary type="html">&lt;h2&gt;Introduction to key-value stores&lt;/h2&gt;
&lt;p&gt;Key-value stores are distributed hash tables (DHTs). A key is generated by the hash function and should be unique. In a key-value store, a key binds to a specific value and doesn’t assume anything about the structure of the value. A value can be a …&lt;/p&gt;</summary><content type="html">&lt;h2&gt;Introduction to key-value stores&lt;/h2&gt;
&lt;p&gt;Key-value stores are distributed hash tables (DHTs). A key is generated by the hash function and should be unique. In a key-value store, a key binds to a specific value and doesn’t assume anything about the structure of the value. A value can be a blob, image, server name, or anything the user wants to store against a unique key.&lt;/p&gt;
&lt;p&gt;Usually, it’s preferred to keep the size of value relatively smaller (KB to MB). We can put large data in the blob store and put links to that data in the value field. Key-value stores are useful in many situations, such as storing user sessions in a web application and building NoSQL databases.&lt;/p&gt;
&lt;p&gt;It’s challenging to scale traditional databases with strong consistency and high availability in a distributed environment. Many real-world services like Amazon, Facebook, Instagram, Netflix, and many more use primary-key access to a data store instead of traditional online transaction processing (OLTP) databases. Examples of key-value store usage include bestseller lists, shopping carts, customer preferences, session management, sales rank, and product catalogs.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Note: Many applications might not require a rich programming model provided by a traditional relational database management system (RDBMS). Using RDBMS for such applications is often expensive in terms of cost and performance.&lt;/em&gt;&lt;/p&gt;
&lt;h2&gt;How will we design a key-value store?&lt;/h2&gt;
&lt;p&gt;We’ve divided the key-value system design into the following four lessons:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Design a Key-value Store: We’ll define the requirements of a key-value store and design the API.&lt;/li&gt;
&lt;li&gt;Ensure Scalability and Replication: We’ll learn to achieve scalability using consistent hashing and replicate the partitioned data.&lt;/li&gt;
&lt;li&gt;Versioning Data and Achieving Configurability: We’ll learn to resolve conflicts that occur due to changes made by more than one entity, and we’ll make our system more configurable for different use cases.&lt;/li&gt;
&lt;li&gt;Enable Fault Tolerance and Failure Detection: We’ll learn to make a key-value store fault-tolerant and how to detect failures in the system.&lt;/li&gt;
&lt;/ul&gt;</content><category term="Blog"/><category term="system-design"/><category term="key-value-store"/></entry><entry><title>System Design: Lesson 5.2 - Design of a Key-Value Store</title><link href="https://dmb225.github.io/system-design-lesson-52-design-of-a-key-value-store.html" rel="alternate"/><published>2026-02-06T00:00:00+01:00</published><updated>2026-02-06T00:00:00+01:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2026-02-06:/system-design-lesson-52-design-of-a-key-value-store.html</id><summary type="html">&lt;h2&gt;Requirements&lt;/h2&gt;
&lt;p&gt;Let’s list the requirements of designing a key-value store to overcome the problems of traditional databases.&lt;/p&gt;
&lt;h3&gt;Functional requirements&lt;/h3&gt;
&lt;p&gt;Typically, key-value stores are expected to offer functions such as get and put. However, what sets this particular key-value store system apart is its distinct characteristics, explained as follows:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Configurable …&lt;/li&gt;&lt;/ul&gt;</summary><content type="html">&lt;h2&gt;Requirements&lt;/h2&gt;
&lt;p&gt;Let’s list the requirements of designing a key-value store to overcome the problems of traditional databases.&lt;/p&gt;
&lt;h3&gt;Functional requirements&lt;/h3&gt;
&lt;p&gt;Typically, key-value stores are expected to offer functions such as get and put. However, what sets this particular key-value store system apart is its distinct characteristics, explained as follows:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Configurable service: Some applications might have a tendency to trade strong consistency for higher availability. We need to provide a configurable service so that different applications could use a range of consistency models. We need tight control over the trade-offs between availability, consistency, cost-effectiveness, and performance.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Note: Such configurations can only be performed when instantiating a new key-value store instance and cannot be changed dynamically when the system is operational.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Ability to always write (when we picked “A” over “C” in the context of CAP):&lt;/strong&gt; The applications should always have the ability to write into the key-value storage. If the user wants strong consistency, this requirement might not always be fulfilled due to the implications of the CAP theorem.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;em&gt;Note: The context of the problem determines what will be classified as a functional requirement and what will be classified as non-functional. For example, the ability to always write (high availability) is a functional requirement for Amazon’s shopping cart application, while in other cases, high availability may be considered a non-functional requirement. Drawing inspiration from Amazon’s Dynamo key-value store, we can categorize the ability to always write as a functional requirement.&lt;/em&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Hardware heterogeneity: We want to add new servers with different and higher capacities, seamlessly, to our cluster without changing or upgrading existing servers. Our system should be able to accommodate and leverage different capacity servers, ensuring correct core functionality (get and put data) while balancing the workload distribution according to each server’s capacity. This calls for a peer-to-peer design with no distinguished nodes.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;Non-functional requirements&lt;/h3&gt;
&lt;p&gt;The non-functional requirements are as follows:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Scalability: Key-value stores should run on tens of thousands of servers distributed across the globe. Incremental scalability is highly desirable. We should add or remove the servers as needed with minimal to no disruption to the service availability. Moreover, our system should be able to handle an enormous number of users of the key-value store.&lt;/li&gt;
&lt;li&gt;Fault tolerance: The key-value store should operate uninterrupted despite failures in servers or their components.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Now that you understand the functional and non-functional requirements of a key-value store&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;What do you think are the key differences between key-value stores and traditional databases?&lt;/li&gt;
&lt;li&gt;In what scenarios are key-value stores particularly advantageous?&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Answer:&lt;/strong&gt; Key-value stores are designed for simple retrievals using a unique key, and they typically lack complex query languages. They often prioritize availability and scalability over strict consistency, and they store unstructured data. Traditional relational databases, on the other hand, organize data into tables with predefined schemas, support complex queries, and usually emphasize consistency.&lt;/p&gt;
&lt;p&gt;Key-value stores are particularly advantageous in scenarios involving massive data volumes, high-speed lookups, or when the data structure is flexible or not fully known in advance.&lt;/p&gt;
&lt;h2&gt;Assumptions&lt;/h2&gt;
&lt;p&gt;We’ll assume the following to keep our design simple:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;The data centers hosting the service are trusted (non-hostile).&lt;/li&gt;
&lt;li&gt;All the required authentication and authorization are already completed.&lt;/li&gt;
&lt;li&gt;User requests and responses are relayed over HTTPS.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;API design&lt;/h2&gt;
&lt;p&gt;Key-value stores, like ordinary hash tables, provide two primary functions, which are get and put.&lt;/p&gt;
&lt;p&gt;Let’s look at the API design.&lt;/p&gt;
&lt;h3&gt;The get function&lt;/h3&gt;
&lt;p&gt;The API call to get a value should look like this: &lt;code&gt;get(key)&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;We return the associated value on the basis of the parameter key. When data is replicated, it locates the object replica associated with a specific key that’s hidden from the end user. It’s done by the system if the store is configured with a weaker data consistency model. For example, in eventual consistency, there might be more than one value returned against a key.&lt;/p&gt;
&lt;h3&gt;The put function&lt;/h3&gt;
&lt;p&gt;The API call to put the value into the system should look like this: &lt;code&gt;put(key, value)&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;It stores the value associated with the key. The system automatically determines where data should be placed. Additionally, the system often keeps metadata about the stored object. Such metadata can include the version of the object.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;When performing data integrity checks, we often store hashes of values (and sometimes values along with their associated key) as metadata. Should these hashes be generated before or after data compression or encryption, and why?&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Answer:&lt;/strong&gt; When performing data integrity checks, hashes should be generated after compression but before encryption. This is because compression reduces data size without altering its meaning, making the hash more reliable and efficient. Hashing before encryption is avoided because encryption often involves salting or IVs, which can change the ciphertext even if the data remains the same, leading to unreliable integrity checks. So, the typical sequence is: compress, then hash, then encrypt. This approach ensures that integrity verification is based on the actual content, not the encrypted form.&lt;/p&gt;
&lt;h2&gt;Data type&lt;/h2&gt;
&lt;p&gt;The key is often a primary key in a key-value store, while the value can be any arbitrary binary data.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Note: Dynamo uses MD5 hashes on the key to generate a 128-bit identifier. These identifiers help the system determine which server node will be responsible for this specific key.&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;In the next lesson, we’ll learn how to design our key-value store. First, we’ll focus on adding scalability, replication, and versioning of our data to our system. Then, we’ll ensure the functional requirements and make our system fault-tolerant. We’ll fulfill a few of our non-functional requirements first because implementing our functional requirements depends on the method chosen for scalability.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Note: This chapter is based on Dynamo, which is an influential work in the domain of key-value stores.&lt;/em&gt;&lt;/p&gt;</content><category term="Blog"/><category term="system-design"/><category term="key-value-store"/></entry><entry><title>System Design: Lesson 5.3 - Key-Value Store Scalability and Replication</title><link href="https://dmb225.github.io/system-design-lesson-53-key-value-store-scalability-and-replication.html" rel="alternate"/><published>2026-02-06T00:00:00+01:00</published><updated>2026-02-06T00:00:00+01:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2026-02-06:/system-design-lesson-53-key-value-store-scalability-and-replication.html</id><summary type="html">&lt;h2&gt;Add scalability&lt;/h2&gt;
&lt;p&gt;Let’s start with one of the core design requirements: scalability. We store key-value data in storage nodes. With a change in demand, we might need to add or remove storage nodes. It means we need to partition data over the nodes in the system to distribute the …&lt;/p&gt;</summary><content type="html">&lt;h2&gt;Add scalability&lt;/h2&gt;
&lt;p&gt;Let’s start with one of the core design requirements: scalability. We store key-value data in storage nodes. With a change in demand, we might need to add or remove storage nodes. It means we need to partition data over the nodes in the system to distribute the load across all nodes.&lt;/p&gt;
&lt;p&gt;For example, let’s consider that we have four nodes, and we want 25% of the requests to go to each node to balance the load equally. The traditional way to solve this is through the modulus operator. Each request that arrives has a key associated with it. When a request comes in, we calculate the hash of the key. Then, we find the remainder by taking the modulus of the hashed value with the number of nodes m. The remainder value x is the node number, and we send the request to that node to process it.&lt;/p&gt;
&lt;p&gt;We want to add and remove nodes with minimal change in our infrastructure. But in this method, when we add or remove a node, we need to move a lot of keys. This is inefficient. For example, node 2 is removed, and suppose for the same key, the new server to process a request will be node 1 because 10 % 3 = 1. Nodes hold information in their local caches, like keys and their values. So, we need to move that request's data to the next node that has to process the request. But this replication can be costly and can cause high latency.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Why didn't we use load balancers to distribute requests to all nodes?&lt;/em&gt;
&lt;strong&gt;Answer:&lt;/strong&gt; In distributed key-value stores, consistent hashing decides which node stores a given key.
This direct mapping lets requests reach the correct node immediately, maintaining data locality and low latency.
A load balancer would interfere by spreading requests randomly, leading to misrouted traffic, extra cross-node communication, and higher latency. Consistent hashing already handles both load distribution and scalability without adding extra routing layers.&lt;/p&gt;
&lt;p&gt;Next, we’ll learn how to copy data efficiently.&lt;/p&gt;
&lt;h2&gt;Consistent hashing&lt;/h2&gt;
&lt;p&gt;Consistent hashing is an effective way to manage the load over the set of nodes. In consistent hashing, we consider that we have a conceptual ring of hashes from 0 to n-1, where n is the number of available hash values. We use each node’s ID, calculate its hash, and map it to the ring. We apply the same process to requests. Each request is completed by the next node that it finds by moving in the clockwise direction in the ring.&lt;/p&gt;
&lt;p&gt;Whenever a new node is added to the ring, the immediate next node is affected. It has to share its data with the newly added node while other nodes are unaffected. It’s easy to scale since we’re able to keep changes to our nodes minimal. This is because only a small portion of overall keys need to move. The hashes are randomly distributed, so we expect the load of requests to be random and distributed evenly on average on the ring.&lt;/p&gt;
&lt;p&gt;The primary benefit of consistent hashing is that as nodes join or leave, it ensures that a minimal number of keys need to move. However, the request load isn’t equally divided in practice. Any server that handles a large chunk of data can become a bottleneck in a distributed system. That node will receive a disproportionately large share of data storage and retrieval requests, reducing the overall system performance. As a result, these are referred to as hotspots.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Note: It’s a good exercise to think of possible solutions to the non-uniform load distribution before reading on.&lt;/em&gt;&lt;/p&gt;
&lt;h2&gt;Use virtual nodes&lt;/h2&gt;
&lt;p&gt;A virtual node is a logical subdivision of a physical node in a distributed system. Rather than assigning a single position to each physical node in the hash ring, each node is represented by multiple virtual nodes, created by applying multiple hash functions to the same key. This approach distributes data and traffic more evenly across the system and helps balance load among nodes.&lt;/p&gt;
&lt;p&gt;Let’s take an example. Suppose we have three hash functions. For each node, we calculate three hashes and place them into the ring. For the request, we use only one hash function. Wherever the request lands onto the ring, it’s processed by the next node found while moving in the clockwise direction. Each server has three positions, so the load of requests is more uniform. Moreover, if a node has more hardware capacity than others, we can add more virtual nodes by using additional hash functions. This way, it’ll have more positions in the ring and serve more requests.&lt;/p&gt;
&lt;h3&gt;Advantages of virtual nodes&lt;/h3&gt;
&lt;p&gt;Following are some advantages of using virtual nodes:
- If a node fails or does routine maintenance, the workload is uniformly distributed over other nodes. For each newly accessible node, the other nodes receive nearly equal load when it comes back online or is added to the system.
- It’s up to each node to decide how many virtual nodes it’s responsible for, considering the heterogeneity of the physical infrastructure. For example, if a node has roughly double the computational capacity as compared to the others, it can take more load.&lt;/p&gt;
&lt;p&gt;We’ve made the proposed design of key-value storage scalable. The next task is to make our system highly available.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Describe how a key-value store can support incremental scalability without disrupting service availability.&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Answer:&lt;/strong&gt; A key-value store can support incremental scalability without disrupting service availability by using techniques such as consistent hashing to only reassign affected keys when nodes are added or removed. Additionally, virtual nodes help reduce data movement during scaling, and background data migration ensures minimal impact on active operations. Implementing read/write forwarding allows the system to handle in-transit data smoothly during scaling transitions.&lt;/p&gt;
&lt;h2&gt;Data replication&lt;/h2&gt;
&lt;p&gt;We have various methods to replicate the storage. It can be either a primary-secondary relationship or a peer-to-peer relationship.&lt;/p&gt;
&lt;h3&gt;Primary-secondary approach&lt;/h3&gt;
&lt;p&gt;In a primary-secondary approach, one of the storage areas is primary, and other storage areas are secondary. The secondary replicates its data from the primary. The primary serves the write requests while the secondary serves read requests. After writing, there’s a lag for replication. Moreover, if the primary goes down, we can’t write into the storage, and it becomes a single point of failure.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Does the primary-secondary approach fulfill the requirements of the key-value store that we defined in the System Design: The Key-value Store lesson?&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Answer:&lt;/strong&gt; The primary-secondary approach in a key-value store generally doesn’t fully meet all the requirements. While it allows for quick read access from secondaries, it can overload the primary with write requests, especially if the secondary is promoted to primary after a failure. During the switch-over, writes might be temporarily unavailable, which affects availability. So, it’s good for read-heavy scenarios but not ideal if continuous write availability is a key requirement.&lt;/p&gt;
&lt;h2&gt;Peer-to-peer approach&lt;/h2&gt;
&lt;p&gt;In the peer-to-peer approach, all involved storage areas are primary, and they replicate the data to stay updated. Both read and write are allowed on all nodes. Usually, it’s inefficient and costly to replicate in all nnn nodes. Instead, three or five is a common choice for the number of storage nodes to be replicated.&lt;/p&gt;
&lt;p&gt;We’ll use a peer-to-peer relationship for replication. We’ll replicate the data on multiple hosts to achieve durability and high availability. Each data item will be replicated at n hosts, where n is a parameter configured per instance of the key-value store. For example, if we choose n to be 5, it means we want our data to be replicated to five nodes.&lt;/p&gt;
&lt;p&gt;Each node will replicate its data to the other nodes. We’ll call a node coordinator that handles read or write operations. It’s directly responsible for the keys. A coordinator node is assigned the key “K.” It’s also responsible for replicating the keys to n-1 successors on the ring (clockwise). These lists of successor virtual nodes are called preference lists. To avoid putting replicas on the same physical nodes, the preference list can skip those virtual nodes whose physical node is already in the list.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;What is the impact of synchronous or asynchronous replication?&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Answer:&lt;/strong&gt; In synchronous replication, each write must be acknowledged by all replicas before it is considered complete. This guarantees strong consistency but can slow down performance and reduce availability if some replicas are unreachable. In asynchronous replication, writes are considered complete without waiting for all replicas, which improves performance and availability but can lead to temporary inconsistencies. These differences reflect the CAP theorem, highlighting the trade-offs between consistency and availability during network partitions.&lt;/p&gt;
&lt;p&gt;In the context of the CAP theorem, key-value stores can either be consistent or be available when there are network partitions. For key-value stores, we prefer availability over consistency. It means if the two storage nodes lost connection for replication, they would keep on handling the requests sent to them, and when the connection is restored, they’ll sync up. In the disconnected phase, it’s highly possible for the nodes to be inconsistent. So, we need to resolve such conflicts. In the next lesson, we’ll learn a concept to handle inconsistencies using the versioning of our data.&lt;/p&gt;</content><category term="Blog"/><category term="system-design"/><category term="key-value-store"/></entry><entry><title>System Design: Lesson 5.4 - Key-Value Store Data Versioning</title><link href="https://dmb225.github.io/system-design-lesson-54-key-value-store-data-versioning.html" rel="alternate"/><published>2026-02-06T00:00:00+01:00</published><updated>2026-02-06T00:00:00+01:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2026-02-06:/system-design-lesson-54-key-value-store-data-versioning.html</id><summary type="html">&lt;h2&gt;Data versioning&lt;/h2&gt;
&lt;p&gt;When network partitions and node failures occur during an update, an object’s version history might become fragmented. As a result, it requires a reconciliation effort on the part of the system. It’s necessary to build a way that explicitly accepts the potential of several copies of …&lt;/p&gt;</summary><content type="html">&lt;h2&gt;Data versioning&lt;/h2&gt;
&lt;p&gt;When network partitions and node failures occur during an update, an object’s version history might become fragmented. As a result, it requires a reconciliation effort on the part of the system. It’s necessary to build a way that explicitly accepts the potential of several copies of the same data so that we can avoid the loss of any updates. It’s critical to realize that some failure scenarios can lead to multiple copies of the same data in the system. So, these copies might be the same or divergent. Resolving the conflicts among these divergent histories is essential and critical for consistency purposes.&lt;/p&gt;
&lt;p&gt;To handle inconsistency, we need to maintain causality between the events. We can do this using the timestamps and update all conflicting values with the value of the latest request. But time isn’t reliable in a distributed system, so we can’t use it as a deciding factor.&lt;/p&gt;
&lt;p&gt;Another approach to maintaining causality effectively is by using vector clocks. A vector clock is a list of (node, counter) pairs. There’s a single vector clock for every version of an object. If two objects have different vector clocks, we’re able to tell whether they’re causally related or not (more on this in a bit). Unless one of the two changes is reconciled, the two are deemed at odds.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Explain how metadata like versioning and checksums, which detect data corruption, help maintain data integrity and consistency in a key-value store.&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Answer:&lt;/strong&gt; Certainly! Metadata like versioning and checksums are vital for maintaining data integrity and consistency in a key-value store.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Versioning&lt;/strong&gt; helps track changes to data, making it easier to resolve conflicts during replication and ensuring clients can identify the most recent data in eventually consistent systems.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Checksums&lt;/strong&gt; verify that data hasn’t been tampered with or corrupted during transmission or storage, ensuring data integrity. They are often computed after compression or encryption to match the stored data format.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Together, these mechanisms enhance the reliability of the system and support recovery in distributed environments.&lt;/p&gt;
&lt;h2&gt;Modify the API design&lt;/h2&gt;
&lt;p&gt;We talked about how we can decide if two events are causally related or not using a vector clock value. For this, we need information about which node performed the operation before and what its vector clock value was. This is the context of an operation. So, we’ll modify our API design as follows.&lt;/p&gt;
&lt;p&gt;The API call to get a value should look like this: &lt;code&gt;get(key)&lt;/code&gt;
We return an object or a collection of conflicting objects along with a context. The context holds encoded metadata about the object, including details such as the object’s version.&lt;/p&gt;
&lt;p&gt;The API call to put the value into the system should look like this: &lt;code&gt;put(key, value)&lt;/code&gt;
The function finds the node where the value should be placed on the basis of the key and stores the value associated with it. The context is returned by the system after the get operation. If we have a list of objects in context that raises a conflict, we’ll ask the client to resolve it.&lt;/p&gt;
&lt;p&gt;To update an object in the key-value store, the client must give the context. We determine version information using a vector clock by supplying the context from a previous read operation. If the key-value store has access to several branches, it provides all objects at the leaf nodes, together with their respective version information in context, when processing a read request. Reconciling disparate versions and merging them into a single new version is considered an update.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Note:&lt;/em&gt; This process of resolving conflicts is comparable to how it’s done in Git. If Git is able to merge multiple versions into one, merging is performed automatically. It’s up to the client (the developer) to resolve conflicts manually if automatic conflict resolution is not possible. Along the same lines, our system can try automatic conflict resolution and, if not possible, ask the application to provide a final resolved value.&lt;/p&gt;
&lt;h2&gt;The get and put operations&lt;/h2&gt;
&lt;p&gt;One of our functional requirements is that the system should be configurable. We want to control the trade-offs between availability, consistency, cost-effectiveness, and performance. So, let’s achieve configurability by implementing the basic get and put functions of the key-value store.&lt;/p&gt;
&lt;p&gt;Every node can handle the get (read) and put (write) operations in our system. A node handling a read or write operation is known as a coordinator. The coordinator is the first among the top n nodes in the preference list.&lt;/p&gt;
&lt;p&gt;There can be two ways for a client to select a node:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;We route the request to a generic load balancer.&lt;/li&gt;
&lt;li&gt;We use a partition-aware client library that routes requests directly to the appropriate coordinator nodes.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Both approaches have their benefits. The client isn’t linked to the code in the first approach, whereas lower latency is achievable in the second. The latency is lower due to the reduced number of hops because the client can directly go to a specific server.&lt;/p&gt;
&lt;p&gt;Let’s make our service configurable by having an ability where we can control the trade-offs between availability, consistency, cost-effectiveness, and performance. We can use a consistency protocol similar to those used in quorum systems.&lt;/p&gt;
&lt;p&gt;Let’s take an example. Say n in the top n of the preference list is equal to 3. This means three copies of the data need to be maintained. We assume that nodes are placed in a ring. Say A, B, C, D, and E is the clockwise order of the nodes in that ring. If the write function is performed on node A, then the copies of that data will be placed on B and C. This is because B and C are the next nodes we find while moving in a clockwise direction of the ring.&lt;/p&gt;
&lt;p&gt;By now, we’ve fulfilled the scalability, availability, conflict-resolution, and configurable service requirements. The last requirement is to have a fault-tolerant system. Let’s discuss how we’ll achieve it in the next lesson.&lt;/p&gt;</content><category term="Blog"/><category term="system-design"/><category term="key-value-store"/></entry><entry><title>System Design: Lesson 5.5 - Key-Value Store Fault Tolerance</title><link href="https://dmb225.github.io/system-design-lesson-55-key-value-store-fault-tolerance.html" rel="alternate"/><published>2026-02-06T00:00:00+01:00</published><updated>2026-02-06T00:00:00+01:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2026-02-06:/system-design-lesson-55-key-value-store-fault-tolerance.html</id><summary type="html">&lt;h2&gt;Handle temporary failures&lt;/h2&gt;
&lt;p&gt;Typically, distributed systems use a quorum-based approach to handle failures. A quorum is the minimum number of votes required for a distributed transaction to proceed with an operation. If a server is part of the consensus and is down, then we can’t perform the required operation …&lt;/p&gt;</summary><content type="html">&lt;h2&gt;Handle temporary failures&lt;/h2&gt;
&lt;p&gt;Typically, distributed systems use a quorum-based approach to handle failures. A quorum is the minimum number of votes required for a distributed transaction to proceed with an operation. If a server is part of the consensus and is down, then we can’t perform the required operation. It affects the availability and durability of our system.&lt;/p&gt;
&lt;p&gt;We’ll use a sloppy quorum instead of strict quorum membership. Usually, a leader manages the communication among the participants of the consensus. The participants send an acknowledgment after committing a successful write. Upon receiving these acknowledgments, the leader responds to the client. However, the drawback is that the participants are easily affected by the network outage. If the leader is temporarily down and the participants can’t reach it, they declare the leader dead. Now, a new leader has to be reelected. Such frequent elections have a negative impact on performance because the system spends more time picking a leader than accomplishing any actual work.&lt;/p&gt;
&lt;p&gt;In the sloppy quorum, the first n healthy nodes from the preference list handle all read and write operations. The n healthy nodes may not always be the first n nodes discovered when moving clockwise in the consistent hash ring.&lt;/p&gt;
&lt;p&gt;Let’s consider the following configuration with n = 3. If node A is briefly unavailable or unreachable during a write operation, the request is sent to the next healthy node from the preference list, which is node D in this case. It ensures the desired availability and durability. After processing the request, the node D includes a hint as to which node was the intended receiver (in this case, A). Once node A is up and running again, node D sends the request information to A so it can update its data. Upon completion of the transfer, D removes this item from its local storage without affecting the total number of replicas in the system.&lt;/p&gt;
&lt;p&gt;This approach is called a hinted handoff. Using it, we can ensure that reads and writes are fulfilled if a node faces temporary failure.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Note:&lt;/em&gt; Note: A highly available storage system must handle data center failure due to power outages, cooling failures, network failures, or natural disasters. For this, we should ensure replication across the data centers. So, if one data center is down, we can recover it from the other.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Hinted handoff helps maintain availability when nodes fail temporarily. But what happens when the system experiences prolonged failures?&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Answer:&lt;/strong&gt; When a node remains down beyond the hinted handoff window, the temporary hints expire or are discarded to avoid unbounded storage growth. At this point, the system moves from quick recovery to full data reconciliation. Once the node is back online, background processes like anti-entropy repair, read repair, or Merkle tree comparison detect and synchronize missing updates. This process may cause temporary inconsistency or data loss if the recovery takes too long.&lt;/p&gt;
&lt;p&gt;It’s worth adding that these mechanisms ensure eventual consistency despite prolonged failures, balancing availability and data integrity.&lt;/p&gt;
&lt;h2&gt;Handle permanent failures&lt;/h2&gt;
&lt;p&gt;In the event of permanent failures of nodes, we should keep our replicas synchronized to make our system more durable. We need to speed up the detection of inconsistencies between replicas and reduce the quantity of transferred data. We’ll use Merkle trees for that.&lt;/p&gt;
&lt;p&gt;In a Merkle tree, the values of individual keys are hashed and used as the leaves of the tree. There are hashes of their children in the parent nodes higher up the tree. Each branch of the Merkle tree can be verified independently without the need to download the complete tree or the entire dataset. While checking for inconsistencies across copies, Merkle trees reduce the amount of data that must be exchanged. There’s no need for synchronization if, for example, the hash values of two trees’ roots are the same and their leaf nodes are also the same. Until the process reaches the tree leaves, the hosts can identify the keys that are out of sync when the nodes exchange the hash values of children. The Merkle tree is a mechanism to implement anti-entropy, which means to keep all the data consistent. It reduces data transmission for synchronization and the number of discs accessed during the anti-entropy process.&lt;/p&gt;
&lt;h2&gt;Anti-entropy with Merkle trees&lt;/h2&gt;
&lt;p&gt;Each node keeps a distinct Merkle tree for the range of keys that it hosts for each virtual node. The nodes can determine if the keys in a given range are correct. The root of the Merkle tree corresponding to the common key ranges is exchanged between two nodes. We’ll make the following comparison:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Compare the hashes of the root node of Merkle trees.&lt;/li&gt;
&lt;li&gt;Do not proceed if they’re the same.&lt;/li&gt;
&lt;li&gt;Traverse left and right children using recursion. The nodes identify whether or not they have any differences and perform the necessary synchronization.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;em&gt;Note: We assume the ranges defined are hypothetical for illustration purposes.&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;The advantage of using Merkle trees is that each branch of the Merkle tree can be examined independently without requiring nodes to download the tree or the complete dataset. It reduces the quantity of data that must be exchanged for synchronization and the number of disc accesses that are required during the anti-entropy procedure.&lt;/p&gt;
&lt;p&gt;The disadvantage is that when a node joins or departs the system, the tree’s hashes are recalculated because multiple key ranges are affected.&lt;/p&gt;
&lt;p&gt;We want our nodes to detect the failure of other nodes in the ring, so let’s see how we can add it to our proposed design.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Discuss the implications of scalability and fault tolerance when using consistent hashing in distributed key-value stores.&lt;/em&gt;&lt;/p&gt;
&lt;h2&gt;Promote membership in the ring to detect failures&lt;/h2&gt;
&lt;p&gt;The nodes can be offline for short periods, but they may also indefinitely go offline. We shouldn’t rebalance partition assignments or fix unreachable replicas when a single node goes down because it’s rarely a permanent departure. Therefore, the addition and removal of nodes from the ring should be done carefully.&lt;/p&gt;
&lt;p&gt;Planned commissioning and decommissioning of nodes results in membership changes. These changes form history. They’re recorded persistently on the storage for each node and reconciled among the ring members using a gossip protocol. A gossip-based protocol also maintains an eventually consistent view of membership. When two nodes randomly choose one another as their peer, both nodes can efficiently synchronize their persisted membership histories.&lt;/p&gt;
&lt;p&gt;Let’s learn how a gossip-based protocol works by considering the following example. Say node A starts up for the first time, and it randomly adds nodes B and E to its token set. The token set has virtual nodes in the consistent hash space and maps nodes to their respective token sets. This information is stored locally on the disk space of the node.&lt;/p&gt;
&lt;p&gt;Now, node A handles a request that results in a change, so it communicates this to B and E. Another node, D, has C nad E in its token set. It makes a change and tells C and E. The other nodes do the same process. This way, every node eventually knows about every other node’s information. It’s an efficient way to share information asynchronously, and it doesn’t take up a lot of bandwidth.&lt;/p&gt;
&lt;p&gt;Decentralized failure detection protocols use a gossip-based protocol that allows each node to learn about the addition or removal of other nodes. The join and leave methods of the arriving or leaving nodes explicitly notify the other nodes about the permanent node additions and removals. The individual nodes detect temporary node failures when they fail to communicate with another node. If a node fails to communicate to any of the nodes present in its token set for the authorized time, then it communicates to the administrators that the node is dead.&lt;/p&gt;
&lt;h2&gt;Conclusion&lt;/h2&gt;
&lt;p&gt;A key-value store provides flexibility and allows us to scale the applications that have unstructured data. Web applications can use key-value stores to store information about a user’s session and preferences. When using a user key, all the data is accessible, and key-value stores are ideal for rapid reads and write operations. Key-value stores can be used to power real-time recommendations and advertising because the stores can swiftly access and present fresh recommendations.&lt;/p&gt;</content><category term="Blog"/><category term="system-design"/><category term="key-value-store"/></entry><entry><title>System Design: Lesson 4.2 - Types of Databases</title><link href="https://dmb225.github.io/system-design-lesson-42-types-of-databases.html" rel="alternate"/><published>2026-02-05T00:00:00+01:00</published><updated>2026-02-05T00:00:00+01:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2026-02-05:/system-design-lesson-42-types-of-databases.html</id><summary type="html">&lt;p&gt;As we discussed earlier, databases are divided into two types: relational and non-relational. Let’s discuss these types in detail.&lt;/p&gt;
&lt;h2&gt;Relational databases&lt;/h2&gt;
&lt;p&gt;Relational databases adhere to particular schemas before storing the data. The data stored in relational databases has prior structure. Mostly, this model organizes data into one or more …&lt;/p&gt;</summary><content type="html">&lt;p&gt;As we discussed earlier, databases are divided into two types: relational and non-relational. Let’s discuss these types in detail.&lt;/p&gt;
&lt;h2&gt;Relational databases&lt;/h2&gt;
&lt;p&gt;Relational databases adhere to particular schemas before storing the data. The data stored in relational databases has prior structure. Mostly, this model organizes data into one or more relations (also called tables), with a unique key for each tuple (instance). Each entity of the data consists of instances and attributes, where instances are stored in rows, and the attributes of each instance are stored in columns. Since each tuple has a unique key, a tuple in one table can be linked to a tuple in other tables by storing the primary keys in other tables, generally known as foreign keys.&lt;/p&gt;
&lt;p&gt;A &lt;em&gt;Structure Query Language (SQL)&lt;/em&gt; is used for manipulating the database. This includes insertion, deletion, and retrieval of data.&lt;/p&gt;
&lt;p&gt;There are various reasons for the popularity and dominance of relational databases, which include simplicity, robustness, flexibility, performance, scalability, and compatibility in managing generic data.&lt;/p&gt;
&lt;p&gt;Relational databases provide the atomicity, consistency, isolation, and durability (ACID) properties to maintain the integrity of the database. ACID is a powerful abstraction that simplifies complex interactions with the data and hides many anomalies (like dirty reads, dirty writes, read skew, lost updates, write skew, and phantom reads) behind a simple transaction abort.&lt;/p&gt;
&lt;p&gt;But ACID is like a big hammer by design so that it’s generic enough for all the problems. If some specific application only needs to deal with a few anomalies, there’s a window of opportunity to use a custom solution for higher performance, though there is added complexity.&lt;/p&gt;
&lt;p&gt;Let’s discuss ACID in detail:&lt;/p&gt;
&lt;p&gt;Let’s discuss ACID in detail:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Atomicity: A transaction is considered an atomic unit. Therefore, either all the statements within a transaction will successfully execute, or none of them will execute. If a statement fails within a transaction, it should be aborted and rolled back.&lt;/li&gt;
&lt;li&gt;Consistency: At any given time, the database should be in a consistent state, and it should remain in a consistent state after every transaction. For example, if multiple users want to view a record from the database, it should return a similar result each time.&lt;/li&gt;
&lt;li&gt;Isolation: In the case of multiple transactions running concurrently, they shouldn’t be affected by each other. The final state of the database should be the same as the transactions that were executed sequentially.&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Durability: The system should guarantee that completed transactions will survive permanently in the database even in system failure events.
Various database management systems (DBMS) are used to define relational database schema along with other operations, such as storing, retrieving, and running SQL queries on data. Some of the popular DBMSs are as follows:&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;MySQL&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;Oracle Database&lt;/li&gt;
&lt;li&gt;Microsoft SQL Server&lt;/li&gt;
&lt;li&gt;IBM DB2&lt;/li&gt;
&lt;li&gt;Postgres&lt;/li&gt;
&lt;li&gt;SQLite&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;Why relational databases?&lt;/h3&gt;
&lt;p&gt;Relational databases are the default choice of software professionals for structured data storage. There are a number of advantages to these databases. One of the greatest powers of the relational database is its abstractions of ACID transactions and related programming semantics. This makes it very convenient for the end programmer to use a relational database. Let’s revisit some important features of relational databases:&lt;/p&gt;
&lt;h4&gt;Flexibility&lt;/h4&gt;
&lt;p&gt;In the context of SQL, data definition language (DDL) provides us the flexibility to modify the database, including tables, columns, renaming the tables, and other changes. DDL even allows us to modify schema while other queries are happening and the database server is running.&lt;/p&gt;
&lt;h4&gt;Reduced redundancy&lt;/h4&gt;
&lt;p&gt;One of the biggest advantages of the relational database is that it eliminates data redundancy. The information related to a specific entity appears in one table while the relevant data to that specific entity appears in the other tables linked through foreign keys. This process is called normalization and has the additional benefit of removing an inconsistent dependency.&lt;/p&gt;
&lt;h4&gt;Concurrency&lt;/h4&gt;
&lt;p&gt;Concurrency is an important factor while designing an enterprise database. In such a case, the data is read and written by many users at the same time. We need to coordinate such interactions to avoid inconsistency in data—for example, the double booking of hotel rooms. Concurrency in a relational database is handled through transactional access to the data. As explained earlier, a transaction is considered an atomic operation, so it also works in error handling to either roll back or commit a transaction on successful execution.&lt;/p&gt;
&lt;h4&gt;Integration&lt;/h4&gt;
&lt;p&gt;The process of aggregating data from multiple sources is a common practice in enterprise applications. A common way to perform this aggregation is to integrate a shared database where multiple applications store their data. This way, all the applications can easily access each other’s data while the concurrency control measures handle the access of multiple applications.&lt;/p&gt;
&lt;h4&gt;Backup and disaster recovery&lt;/h4&gt;
&lt;p&gt;Relational databases guarantee the state of data is consistent at any time. The export and import operations make backup and restoration easier. Most cloud-based relational databases perform continuous mirroring to avoid loss of data and make the restoration process easier and quicker.&lt;/p&gt;
&lt;h3&gt;Drawback&lt;/h3&gt;
&lt;h4&gt;Impedance mismatch&lt;/h4&gt;
&lt;p&gt;Impedance mismatch is the difference between the relational model and the in-memory data structures. The relational model organizes data into a tabular structure with relations and tuples. SQL operation on this structured data yields relations aligned with relational algebra. However, it has some limitations. In particular, the values in a table take simple values that can’t be a structure or a list. The case is different for in-memory, where a complex data structure can be stored. To make the complex structures compatible with the relations, we would need a translation of the data in light of relational algebra. So, the impedance mismatch requires translation between two representations.&lt;/p&gt;
&lt;h2&gt;Why non-relational (NoSQL) databases?&lt;/h2&gt;
&lt;p&gt;A NoSQL database is designed for a variety of data models to access and manage data. There are various types of NoSQL databases, which we’ll explain in the next section. These databases are used in applications that require a large volume of semi-structured and unstructured data, low latency, and flexible data models. This can be achieved by relaxing some of the data consistency restrictions of other databases. The following are some characteristics of the NoSQL database:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Simple design: Unlike relational databases, NoSQL doesn’t require dealing with the impedance mismatch—for example, storing all the employees’ data in one document instead of multiple tables that require join operations. This strategy makes it simple and easier to write less code, debug, and maintain.&lt;/li&gt;
&lt;li&gt;Horizontal scaling: Primarily, NoSQL is preferred due to its ability to run databases on a large cluster. This solves the problem when the number of concurrent users increases. NoSQL makes it easier to scale out since the data related to a specific employee is stored in one document instead of multiple tables over nodes. NoSQL databases often spread data across multiple nodes and balance data and queries across nodes automatically. In case of a node failure, it can be transparently replaced without any application disruption.&lt;/li&gt;
&lt;li&gt;Availability: To enhance the availability of data, node replacement can be performed without application downtime. Most of the non-relational databases’ variants support data replication to ensure high availability and disaster recovery.&lt;/li&gt;
&lt;li&gt;Support for unstructured and semi-structured data: Many NoSQL databases work with data that doesn’t have a schema at the time of database configuration or data writes. For example, document databases are structureless; they allow documents (JSON, XML, BSON, and so on) to have different fields. For example, one JSON document can have fewer fields than the other.&lt;/li&gt;
&lt;li&gt;Cost: Licenses for many RDBMSs are pretty expensive, while many NoSQL databases are open source and freely available. Similarly, some RDBMSs rely on costly proprietary hardware and storage systems, while NoSQL databases usually use clusters of cheap commodity servers.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;NoSQL databases are divided into various categories based on the nature of the operations and features, including document store, columnar database, key-value store, and graph database. We’ll discuss each of them along with their use cases from the system design perspective in the following sections.&lt;/p&gt;
&lt;h3&gt;Types of NoSQL databases&lt;/h3&gt;
&lt;h4&gt;Key-value database&lt;/h4&gt;
&lt;p&gt;Key-value databases use key-value methods like hash tables to store data in key-value pairs. We can see this depicted in the figure a couple of paragraphs below. Here, the key serves as a unique or primary key, and the values can be anything ranging from simple scalar values to complex objects. These databases allow easy partitioning and horizontal scaling of the data. Some popular key-value databases include Amazon DynamoDB, Redis, and Memcached DB.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Use case&lt;/strong&gt;: Key-value databases are efficient for session-oriented applications. Session-oriented applications, such as web applications, store users’ data in the main memory or in a database during a session. This data may include user profile information, recommendations, targeted promotions, discounts, and more. A unique ID (a key) is assigned to each user’s session for easy access and storage. Therefore, a better choice to store such data is the key-value database.&lt;/p&gt;
&lt;p&gt;The schema for storing the item attributes is defined based on the nature of the item and the number of attributes it possesses.&lt;/p&gt;
&lt;h4&gt;Document database&lt;/h4&gt;
&lt;p&gt;A document database is designed to store and retrieve documents in formats like XML, JSON, BSON, and so on. These documents are composed of a hierarchical tree data structure that can include maps, collections, and scalar values. Documents in this type of database may have varying structures and data. MongoDB and Google Cloud Firestore are examples of document databases.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Use case&lt;/strong&gt;: Document databases are suitable for unstructured catalog data, like JSON files or other complex structured hierarchical data. For example, in e-commerce applications, a product has thousands of attributes, which is unfeasible to store in a relational database due to its impact on the reading performance. Here comes the role of a document database, which can efficiently store each attribute in a single file for easy management and faster reading speed. Moreover, it’s also a good option for content management applications, such as blogs and video platforms. An entity required for the application is stored as a single document in such applications.&lt;/p&gt;
&lt;h4&gt;Graph database&lt;/h4&gt;
&lt;p&gt;Graph databases use the graph data structure to store data, where nodes represent entities, and edges show relationships between entities. The organization of nodes based on relationships leads to interesting patterns between the nodes. This database allows us to store the data once and then interpret it differently based on relationships. Popular graph databases include Neo4J, OrientDB, and InfiniteGraph. Graph data is kept in store files for persistent storage. Each of the files contains data for a specific part of the graph, such as nodes, links, properties, and so on.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Use case&lt;/strong&gt;: Graph databases can be used in social applications and provide interesting facts and figures among different kinds of users and their activities. The focus of graph databases is to store data and pave the way to drive analyses and decisions based on relationships between entities. The nature of graph databases makes them suitable for various applications, such as data regulation and privacy, machine learning research, financial services-based applications, and many more.&lt;/p&gt;
&lt;h4&gt;Columnar database&lt;/h4&gt;
&lt;p&gt;Columnar databases organize data by columns instead of rows. This design makes it much faster and more efficient to retrieve specific fields, especially useful when running analytics or summarizing large datasets. As a result, they are well-suited for systems where data is read frequently but updated less often. Popular columnar databases include Amazon Redshift and Google BigQuery, all widely used for high-performance analytics and data warehousing.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Use case&lt;/strong&gt;: These databases shine in analytical tasks that involve processing large amounts of data, such as aggregations or trend analysis. For example, in financial systems that need to calculate total transaction amounts over time, a columnar database can quickly pull just the “amount” field without loading unrelated data like customer names or dates.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Note: Another type of database often mistaken for columnar databases is the wide-column database. While columnar databases store each column’s data together on disk to accelerate analytical queries and aggregations, wide-column databases, such as Apache Cassandra and HBase which organize data in rows grouped into column families, making them more suitable for write-heavy workloads and flexible, semi-structured data use cases.&lt;/em&gt;&lt;/p&gt;
&lt;h3&gt;Drawbacks of NoSQL databases&lt;/h3&gt;
&lt;h4&gt;Lack of standardization&lt;/h4&gt;
&lt;p&gt;NoSQL doesn’t follow any specific standard, like how relational databases follow relational algebra. Porting applications from one type of NoSQL database to another might be a challenge.&lt;/p&gt;
&lt;h4&gt;Consistency&lt;/h4&gt;
&lt;p&gt;NoSQL databases provide different products based on the specific trade-offs between consistency and availability when failures can happen. We won’t have strong data integrity, like primary and referential integrities in a relational database. Data might not be strongly consistent but slowly converging using a weak model like eventual consistency.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Let’s assess our understanding of the lesson’s content with the following question.&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;Imagine we’re designing a recommendation engine for a social networking platform with millions of users. The goal is to provide personalized recommendations for users to connect with others based on their interests, activities, and connections. Which database would be a natural fit for modeling and querying the complex relationship in a social network and why?&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Solution:&lt;/strong&gt; A graph database is the natural fit for modeling and querying complex relationships in a social network. It uses nodes and edges to represent users, interests, activities, and connections, making it efficient for traversing relationships and providing personalized recommendations. Its flexibility and scalability support the dynamic nature of social data, leading to more relevant suggestions.&lt;/p&gt;
&lt;h2&gt;Choose the right database&lt;/h2&gt;
&lt;p&gt;Various factors affect the choice of database to be used in an application. A comparison between the relational and non-relational databases is shown in the following table to help us choose:&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Relational Database&lt;/th&gt;
&lt;th&gt;Non-relational Database&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;If the data to be stored is structured&lt;/td&gt;
&lt;td&gt;If the data to be stored is unstructured&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;If ACID properties are required&lt;/td&gt;
&lt;td&gt;If ACID properties are not strictly required&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;If the size of the data is relatively small and fits on a node&lt;/td&gt;
&lt;td&gt;If the size of the data to be stored is large&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Strong schema and fixed structure&lt;/td&gt;
&lt;td&gt;Flexible or schema-less structure&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Data is stored in tables with rows and columns&lt;/td&gt;
&lt;td&gt;Data is stored as documents, key-value, graph, or wide-column&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Scaling is mainly vertical&lt;/td&gt;
&lt;td&gt;Scaling is mainly horizontal&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;&lt;em&gt;Note: When NoSQL databases first came into being, they were drastically different to program and use as compared to traditional databases. Though, due to extensive research in academia and industry over the last many years, the programmer-facing differences between NoSQL and traditional stores are blurring. We might be using the same SQL constructs to talk to a NoSQL store and get a similar level of performance and consistency as a traditional store. Google’s Cloud Spanner is one such database that’s geo-replicated with automatic horizontal sharding ability and high-speed global snapshots of data.&lt;/em&gt;&lt;/p&gt;</content><category term="Blog"/><category term="system-design"/><category term="databases"/></entry><entry><title>System Design: Lesson 4.3 - Data Replication</title><link href="https://dmb225.github.io/system-design-lesson-43-data-replication.html" rel="alternate"/><published>2026-02-05T00:00:00+01:00</published><updated>2026-02-05T00:00:00+01:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2026-02-05:/system-design-lesson-43-data-replication.html</id><summary type="html">&lt;p&gt;Data is a valuable asset for an organization because it drives the entire business.&lt;/p&gt;
&lt;p&gt;Data provides critical business insights into what’s important and what needs to be changed. Organizations also need to securely save and serve their clients’ data on demand. Timely access to the required data under varying …&lt;/p&gt;</summary><content type="html">&lt;p&gt;Data is a valuable asset for an organization because it drives the entire business.&lt;/p&gt;
&lt;p&gt;Data provides critical business insights into what’s important and what needs to be changed. Organizations also need to securely save and serve their clients’ data on demand. Timely access to the required data under varying conditions (including increasing reads and writes, disk and node failures, network and power outages, and others) is necessary to successfully run an online business.&lt;/p&gt;
&lt;p&gt;We need the following characteristics from our data store:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Availability under faults (failure of one or more disks, nodes, network, or power outages).&lt;/li&gt;
&lt;li&gt;Scalability (with increasing reads, writes, and other operations).&lt;/li&gt;
&lt;li&gt;Performance (low latency and high throughput for the clients).&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;It’s challenging, or even impossible, to achieve the above characteristics on a single node.&lt;/p&gt;
&lt;h2&gt;Replication&lt;/h2&gt;
&lt;p&gt;Replication refers to keeping multiple copies of the data at various nodes (preferably geographically distributed) to achieve availability, scalability, and performance. In this lesson, we assume that a single node is enough to hold our entire data. We won’t use this assumption while discussing the partitioning of data in multiple nodes. Often, the concepts of replication and partitioning go together.&lt;/p&gt;
&lt;p&gt;Replication provides several key benefits for distributed systems:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Keeps data geographically close to your users, thus reducing access latency.&lt;/li&gt;
&lt;li&gt;Allows the system to continue working even if some of its parts have failed, thus increasing availability.&lt;/li&gt;
&lt;li&gt;Scales out the number of machines that can serve read queries, thus increasing read throughput.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;However, with many benefits, like availability, replication comes with its complexities. Replication is relatively simple if the replicated data doesn’t require frequent changes. The main problem in replication arises when we have to maintain changes in the replicated data over time.&lt;/p&gt;
&lt;p&gt;If the data that you are replicating does not change over time, then the replication process is a one-time thing. Frequently changing data is a real challenge; it requires careful thinking about concurrency and all the things that can go wrong so that we can deal with the consequences of those faults.&lt;/p&gt;
&lt;p&gt;Additional complexities that could arise due to replication are as follows:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;How do we keep multiple copies of data consistent with each other?&lt;/li&gt;
&lt;li&gt;How do we deal with failed replica nodes?&lt;/li&gt;
&lt;li&gt;Should we replicate synchronously or asynchronously?&lt;ul&gt;
&lt;li&gt;How do we deal with replication lag in case of asynchronous replication?&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;How do we handle concurrent writes?&lt;/li&gt;
&lt;li&gt;What consistency model needs to be exposed to the end programmers?&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;We’ll explore the answer to these questions in this lesson.&lt;/p&gt;
&lt;h2&gt;Synchronous vs. asynchronous replication&lt;/h2&gt;
&lt;p&gt;There are two ways to disseminate changes to the replica nodes:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Synchronous replication&lt;/li&gt;
&lt;li&gt;Asynchronous replication&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;In synchronous replication, the primary node waits for acknowledgments from secondary nodes to confirm that the data has been updated.&lt;/p&gt;
&lt;p&gt;After receiving acknowledgment from all secondary nodes, the primary node reports success to the client. In asynchronous replication, the primary node doesn’t wait for acknowledgments from the secondary nodes and reports success to the client after updating itself. The advantage of synchronous replication is that all secondary nodes are completely up-to-date with the primary node.&lt;/p&gt;
&lt;p&gt;However, there’s a disadvantage to this approach.&lt;/p&gt;
&lt;p&gt;If one of the secondary nodes fails to acknowledge due to a network failure or fault, the primary node will be unable to acknowledge the client until it receives a successful acknowledgment from the crashed node. This causes high latency in the response from the primary node to the client. On the other hand, the advantage of asynchronous replication is that the primary node can continue its work even if all the secondary nodes are down.&lt;/p&gt;
&lt;p&gt;However, if the primary node fails, the writes that weren’t copied to the secondary nodes will be lost.&lt;/p&gt;
&lt;p&gt;Often, leader-based replication is configured to be completely asynchronous. In this case, if the master fails and is not recoverable, any writes that have not yet been replicated to slaves are lost. This means that a write is not guaranteed to be durable, and weakening durability is a significant trade-off that must be carefully considered. The above paragraph explains a trade-off between data consistency and availability when different components of the system can fail.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Let’s assess our understanding of the content provided in this lesson with this question:&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;Imagine you’re leading the database architecture for a real-time financial trading platform that operates globally. The platform demands extremely low-latency data updates to ensure traders receive up-to-the-moment information for making split-second decisions.&lt;/p&gt;
&lt;p&gt;The existing database infrastructure is struggling to meet the stringent latency requirements.&lt;/p&gt;
&lt;p&gt;In this case, the low latency is paramount, and a certain degree of eventual consistency is acceptable. Which one of the following two choices would you recommend for data updates in the database, and why?&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Synchronous updates&lt;/li&gt;
&lt;li&gt;Asynchronous updates&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Solution:&lt;/strong&gt; In a real-time financial trading platform where low latency is critical and some eventual consistency is acceptable, asynchronous updates are the recommended choice. This approach allows the primary database to continue processing data without waiting for acknowledgments from secondary nodes, significantly reducing latency. It ensures traders receive timely information, which is vital for making split-second decisions.&lt;/p&gt;
&lt;h2&gt;Data replication models&lt;/h2&gt;
&lt;p&gt;If the data that you are replicating does not change over time, then the replication process is a one-time thing. Frequently changing data presents a significant challenge; it requires careful consideration of concurrency and potential issues to mitigate the consequences of those faults.&lt;/p&gt;
&lt;p&gt;At a minimum, we need to address unavailable nodes, network interruptions, and silent data corruption resulting from application bugs.&lt;/p&gt;
&lt;p&gt;Here are the following algorithms for replicating changes across nodes:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Single leader or primary-secondary, or master-slave replication&lt;/li&gt;
&lt;li&gt;Multi-leader replication&lt;/li&gt;
&lt;li&gt;Peer-to-peer or leaderless replication&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;Single leader/primary-secondary replication&lt;/h3&gt;
&lt;p&gt;In primary-secondary replication, data is replicated across multiple nodes.&lt;/p&gt;
&lt;p&gt;One node is designated as the primary. It’s responsible for processing any writes to data stored on the cluster. It also sends all the writes to the secondary nodes and keeps them in sync. Primary-secondary replication is appropriate when our workload is read-heavy.&lt;/p&gt;
&lt;p&gt;To better scale with increasing readers, we can add more followers and distribute the read load across the available followers.&lt;/p&gt;
&lt;p&gt;However, replicating data to many followers can create a primary bottleneck. Additionally, primary-secondary replication is inappropriate if our workload is write-heavy. Another advantage of primary-secondary replication is that it’s read-resilient.&lt;/p&gt;
&lt;p&gt;Secondary nodes can still handle read requests in the event of a primary node failure. Therefore, it’s a helpful approach for read-intensive applications.&lt;/p&gt;
&lt;p&gt;Replication via this approach introduces inconsistency when using asynchronous replication. Clients reading from different replicas may see inconsistent data in the event of a primary node failure, as the primary node cannot propagate updated data to the secondary nodes.&lt;/p&gt;
&lt;p&gt;So, if the primary node fails, any missed updates not passed on to the secondary nodes can be lost.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;What happens when the primary node fails?&lt;/strong&gt;
In case of failure of the primary node, a secondary node can be appointed as a primary node, which speeds up the process of recovering the initial primary node. There are two approaches to select the new primary node: manual and automatic.&lt;/p&gt;
&lt;p&gt;In a manual approach, an operator decides which node should be the primary node and notifies all secondary nodes.&lt;/p&gt;
&lt;p&gt;In an automatic approach, when secondary nodes find out that the primary node has failed, they appoint the new primary node by conducting an election known as a leader election.&lt;/p&gt;
&lt;h4&gt;Primary-secondary replication methods&lt;/h4&gt;
&lt;p&gt;There are many different replication methods in primary-secondary replication:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Statement-based replication&lt;/li&gt;
&lt;li&gt;Write-ahead log (WAL) shipping&lt;/li&gt;
&lt;li&gt;Logical (row-based) replication&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Let’s discuss each of them in detail.&lt;/p&gt;
&lt;h3&gt;Statement-based replication&lt;/h3&gt;
&lt;p&gt;Statement-based replication (SBR) is an approach used in MySQL databases.&lt;/p&gt;
&lt;p&gt;In this approach, the primary node executes the SQL statements such as INSERT, UPDATE, DELETE, etc., and then the statements are written into a log file. In the next step, the log file is sent to the secondary nodes for execution.&lt;/p&gt;
&lt;p&gt;This type of replication was used in MySQL before version 5.1.&lt;/p&gt;
&lt;p&gt;*Best case usage: This type of replication requires all writes to be directed through a single master node, while read-only queries can be served by any replica.&lt;/p&gt;
&lt;p&gt;For workloads that consist mostly of reads and only a small percentage of writes (a common pattern on the web), there is an attractive option to create multiple followers and distribute read requests across them. This removes a load from the leader and allows read requests to be served by nearby replicas.*&lt;/p&gt;
&lt;p&gt;While this type of replication appears beneficial, it also has some drawbacks. For example, any nondeterministic functions such as NOW() might result in distinct writes on the primary and secondary nodes.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Note: The NOW() function returns the current date and time according to the system clock.&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Write-ahead log (WAL) shipping&lt;/strong&gt;
Write-ahead log (WAL) shipping is a data replication technique used in both PostgreSQL and Oracle.&lt;/p&gt;
&lt;p&gt;In this technique, when a transaction occurs, it’s initially recorded in a transactional log file, and the log file is written to disk. Subsequently, the recorded operations are executed on the primary database before being transmitted to secondary nodes for execution.&lt;/p&gt;
&lt;p&gt;Unlike SBR, WAL maintains transactional logs instead of SQL statements in a log file, ensuring consistency when dealing with nondeterministic functions. Writing to disk also aids in recovery in case of crash failures.&lt;/p&gt;
&lt;p&gt;For example, when an operation like an UPDATE is executed in PostgreSQL, it’s first written to the transactional log file and disk before being applied to the database. This entry in the transactional log can include details such as the transaction ID, operation type, affected table, and new values, after which the changes are replicated to the secondary nodes.&lt;/p&gt;
&lt;p&gt;However, the drawback of WAL is its tight coupling with the inner structure of the database engine, making software upgrades on the leader and followers complicated.&lt;/p&gt;
&lt;h3&gt;Logical (row-based) replication&lt;/h3&gt;
&lt;p&gt;Logical (row-based) replication is utilized in various relational databases, including PostgreSQL and MySQL.&lt;/p&gt;
&lt;p&gt;In this approach, changes made to the database are captured at the level of individual rows and then replicated to the secondary nodes. Instead of replicating the actual physical changes made to the database, this approach captures the operations in a logical format and then executes them on secondary nodes.&lt;/p&gt;
&lt;p&gt;For example, when operations like INSERT or UPDATE are performed, the entire affected row is captured on the primary node, containing all the column values of the specified row.&lt;/p&gt;
&lt;p&gt;This captured change is then executed on secondary nodes, ensuring that the data remains consistent with the data on the primary node. It offers advantages in terms of flexibility and compatibility with various schema types.&lt;/p&gt;
&lt;p&gt;Common problems using asynchronous primary-secondary replication
Asynchronous primary-secondary replication introduces certain challenges that can affect data consistency and user experience. Key issues include:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;There is a chance that the new master may not receive all the writes from the old master (assuming it is still down). Hence, those write changes will be discarded, which could impact other listening applications and end clients.&lt;/li&gt;
&lt;li&gt;If the user attempts to read the data immediately after writing, the new data may not have been replicated yet. However, to the user, it will look as though the data they submitted was lost.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Solution:&lt;/strong&gt; When reading something that the user may have modified, read it from the leader; otherwise, read it from a follower.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Cons:&lt;/em&gt; This adds more stress to the master node.&lt;/p&gt;
&lt;h3&gt;Multi-leader replication&lt;/h3&gt;
&lt;p&gt;As discussed above, single leader replication using asynchronous replication has a drawback.&lt;/p&gt;
&lt;p&gt;There’s only one primary node, and all writes must go through it, which limits performance. In the event of the primary node's failure, the secondary nodes may not have the updated database. Multi-leader replication is an alternative to single-leader replication.&lt;/p&gt;
&lt;p&gt;There are multiple primary nodes that process the writes and send them to all other primary and secondary nodes to replicate.&lt;/p&gt;
&lt;p&gt;This type of replication is used in databases, along with external tools such as the Tungsten Replicator for MySQL. This kind of replication is particularly useful in applications where we can continue working even when offline—for example, a calendar application that allows us to schedule meetings even without internet access.&lt;/p&gt;
&lt;p&gt;Once we’re online, it replicates its changes from our local database (our mobile phone or laptop acts as a primary node) to other nodes.&lt;/p&gt;
&lt;h2&gt;Common data distribution/migration pattern&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Bi-direction: Reporting instance&lt;/li&gt;
&lt;li&gt;Unidirectional: Instant fail-over (Multi-leader replication)&lt;/li&gt;
&lt;li&gt;Peer-to-peer: Load balancing (High Availability)&lt;/li&gt;
&lt;li&gt;Broadcast: Wide-level data distribution to multiple instances&lt;/li&gt;
&lt;li&gt;Consolidation: Data warehouse (data storage)&lt;/li&gt;
&lt;/ul&gt;
&lt;h4&gt;Conflict&lt;/h4&gt;
&lt;p&gt;Multi-leader replication gives better performance and scalability than single-leader replication, but it also has a significant disadvantage.&lt;/p&gt;
&lt;p&gt;Since all primary nodes concurrently handle write requests, they may modify the same data, potentially creating a conflict between them. For example, suppose the same data is edited by two clients simultaneously. In that case, their writes will be successful in their associated primary nodes, but when they reach the other primary nodes asynchronously, it creates a conflict.&lt;/p&gt;
&lt;h4&gt;Handle conflicts&lt;/h4&gt;
&lt;p&gt;Conflicts can result in different data at different nodes. These should be handled efficiently without losing any data. Let’s discuss some of the approaches to handling conflicts:&lt;/p&gt;
&lt;h5&gt;Conflict avoidance&lt;/h5&gt;
&lt;p&gt;A simple strategy to deal with conflicts is to prevent them from happening in the first place.&lt;/p&gt;
&lt;p&gt;Conflicts can be avoided if the application can verify that all writes for a given record are routed through the same leader. However, the conflict may still occur if a user moves to a different location and is now near a different data center.&lt;/p&gt;
&lt;p&gt;If that happens, we need to reroute the traffic. In such scenarios, the conflict avoidance approach fails, resulting in concurrent writes.&lt;/p&gt;
&lt;h5&gt;Last-write-wins&lt;/h5&gt;
&lt;p&gt;Using their local clock, all nodes assign a timestamp to each update.&lt;/p&gt;
&lt;p&gt;When a conflict occurs, the update with the latest timestamp is selected. This approach can also create difficulty because the clock synchronization across nodes is challenging in distributed systems. There’s a clock skew that can result in data loss.&lt;/p&gt;
&lt;h5&gt;Custom logic&lt;/h5&gt;
&lt;p&gt;In this approach, we can write our own logic to handle conflicts according to the needs of our application. This custom logic can be executed on both read and write operations. When the system detects a conflict, it calls our custom conflict handler.&lt;/p&gt;
&lt;h4&gt;Multi-leader replication topologies&lt;/h4&gt;
&lt;p&gt;There are several topologies through which multi-leader replication is implemented, including the circular topology, star topology, and all-to-all topology.&lt;/p&gt;
&lt;p&gt;The most common is the all-to-all topology. In a star and circular topology, there’s again a similar drawback that if one of the nodes fails, it can affect the whole system. That’s why all-to-all is the most used topology.&lt;/p&gt;
&lt;h3&gt;Peer-to-peer/leaderless replication&lt;/h3&gt;
&lt;p&gt;In primary-secondary replication, the primary node is a bottleneck and a single point of failure.&lt;/p&gt;
&lt;p&gt;Moreover, it helps to achieve read scalability but fails to provide write scalability. The peer-to-peer replication model resolves these problems by eliminating the need for a single primary node. All nodes have equal weight and can accept both read and write requests.&lt;/p&gt;
&lt;p&gt;This replication scheme is stored in the Cassandra database.&lt;/p&gt;
&lt;p&gt;Like primary-secondary replication, this replication can also yield inconsistency. This is because when several nodes accept write requests, it may lead to concurrent writes. A helpful approach used for solving write-write inconsistency is called quorums.&lt;/p&gt;
&lt;h3&gt;Quorums&lt;/h3&gt;
&lt;p&gt;Let’s suppose we have three nodes.&lt;/p&gt;
&lt;p&gt;If at least two out of three nodes are guaranteed to return successful updates, it means only one node has failed. This means that if we read from two nodes, at least one of them will have the updated version, and our system can continue working.&lt;/p&gt;
&lt;p&gt;If we have n nodes, then every write must be updated in at least w nodes to be considered a success, and we must read from r nodes.&lt;/p&gt;
&lt;p&gt;We’ll get an updated value from reading as long as w + r &amp;gt; n because at least one of the nodes must have an updated write from which we can read. Quorum reads and writes adhere to these r and w values. These n, w and r are configurable in Dynamo-style databases.&lt;/p&gt;
&lt;p&gt;For more details on the topic of Quorum, refer to these links:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href="https://www.educative.io/answers/what-is-a-quorum"&gt;What is Quorum?&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://www.educative.io/answers/what-is-quorum-in-distributed-systems"&gt;What is Quorum in distributed systems?&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;</content><category term="Blog"/><category term="system-design"/><category term="databases"/></entry><entry><title>System Design: Lesson 4.4 - Data Partitioning</title><link href="https://dmb225.github.io/system-design-lesson-44-data-partitioning.html" rel="alternate"/><published>2026-02-05T00:00:00+01:00</published><updated>2026-02-05T00:00:00+01:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2026-02-05:/system-design-lesson-44-data-partitioning.html</id><summary type="html">&lt;h2&gt;Why do we partition data?&lt;/h2&gt;
&lt;p&gt;Data is an asset for any organization. Increasing data and concurrent read/write traffic to the data puts scalability pressure on traditional databases. As a result, the latency and throughput are affected. Traditional databases are attractive due to their properties such as range queries, secondary …&lt;/p&gt;</summary><content type="html">&lt;h2&gt;Why do we partition data?&lt;/h2&gt;
&lt;p&gt;Data is an asset for any organization. Increasing data and concurrent read/write traffic to the data puts scalability pressure on traditional databases. As a result, the latency and throughput are affected. Traditional databases are attractive due to their properties such as range queries, secondary indices, and transactions with the ACID properties.&lt;/p&gt;
&lt;p&gt;At some point, a single node-based database isn’t enough to tackle the load. We might need to distribute the data over many nodes, but still export all the nice properties of relational databases. In practice, it has proved challenging to provide single-node database-like properties over a distributed database.&lt;/p&gt;
&lt;p&gt;One solution is to move data to a NoSQL-like system. However, the historical codebase and its close cohesion with traditional databases make it an expensive problem to tackle.&lt;/p&gt;
&lt;p&gt;Organizations might scale traditional databases by using a third-party solution. But often, integrating a third-party solution has its complexities. More importantly, there are abundant opportunities to optimize for the specific problem at hand and get much better performance than a general-purpose solution.&lt;/p&gt;
&lt;p&gt;Data partitioning (or sharding) enables us to use multiple nodes where each node manages some part of the whole data. To handle increasing query rates and data amounts, we strive for balanced partitions and balanced read/write load.&lt;/p&gt;
&lt;p&gt;We’ll discuss different ways to partition data, related challenges, and their solutions in this lesson.&lt;/p&gt;
&lt;h2&gt;Sharding&lt;/h2&gt;
&lt;p&gt;To divide load among multiple nodes, we need to partition the data by a phenomenon known as partitioning or sharding. In this approach, we split a large dataset into smaller chunks of data stored at different nodes on our network.&lt;/p&gt;
&lt;p&gt;The partitioning must be balanced so that each partition receives about the same amount of data. If partitioning is unbalanced, the majority of queries will fall into a few partitions. Partitions that are heavily loaded will create a system bottleneck. The efficacy of partitioning will be harmed because a significant portion of data retrieval queries will be sent to the nodes that carry the highly congested partitions. Such partitions are known as hotspots. Generally, we use the following two ways to shard the data:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Vertical sharding&lt;/li&gt;
&lt;li&gt;Horizontal sharding&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;Vertical sharding&lt;/h3&gt;
&lt;p&gt;We can put different tables in various database instances, which might be running on a different physical server. We might break a table into multiple tables so that some columns are in one table while the rest are in the other. We should be careful if there are joins between multiple tables. We may like to keep such tables together on one shard.&lt;/p&gt;
&lt;p&gt;Often, vertical sharding is used to increase the speed of data retrieval from a table consisting of columns with very wide text or a binary large object (blob). In this case, the column with large text or a blob is split into a different table.&lt;/p&gt;
&lt;p&gt;As shown in the figure a couple paragraphs below, the Employee table is divided into two tables: a reduced Employee table and an EmployeePicture table. The EmployeePicture table has just two columns, EmployeeID and Picture, separated from the original table. Moreover, the primary key EmployeeID of the Employee table is added in both partitioned tables. This makes the data read and write easier, and the reconstruction of the table is performed efficiently.&lt;/p&gt;
&lt;p&gt;Vertical sharding has its intricacies and is more amenable to manual partitioning, where stakeholders carefully decide how to partition data. In comparison, horizontal sharding is suitable to automate even under dynamic conditions.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Note: Creating shards by moving specific tables of a database around is also a form of vertical sharding. Usually, those tables are put in the same shard because they often appear together in queries, for example, for joins. We will see an example of such a use-case ahead in the course.&lt;/em&gt;&lt;/p&gt;
&lt;h3&gt;Horizontal sharding&lt;/h3&gt;
&lt;p&gt;At times, some tables in the databases become too big and affect read/write latency. Horizontal sharding or partitioning is used to divide a table into multiple tables by splitting data row-wise, as shown in the figure in the next section. Each partition of the original table distributed over database servers is called a shard. Usually, there are two strategies available:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Key-range based sharding&lt;/li&gt;
&lt;li&gt;Hash-based sharding&lt;/li&gt;
&lt;/ul&gt;
&lt;h4&gt;Key-range based sharding&lt;/h4&gt;
&lt;p&gt;In the key-range based sharding, each partition is assigned a continuous range of keys.&lt;/p&gt;
&lt;p&gt;Sometimes, a database consists of multiple tables bound by foreign key relationships. In such a case, the horizontal partition is performed using the same partition key on all tables in a relation. Tables (or subtables) that belong to the same partition key are distributed to one database shard. &lt;/p&gt;
&lt;p&gt;The basic design techniques used in multi-table sharding are as follows:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;There’s a partition key in the Customer mapping table. This table resides on each shard and stores the partition keys used in the shard. Applications create a mapping logic between the partition keys and database shards by reading this table from all shards to make the mapping efficient. Sometimes, applications use advanced algorithms to determine the location of a partition key belonging to a specific shard.&lt;/li&gt;
&lt;li&gt;The partition key column, Customer_Id, is replicated in all other tables as a data isolation point. It has a trade-off between an impact on increased storage and locating the desired shards efficiently. Apart from this, it’s helpful for data and workload distribution to different database shards. The data routing logic uses the partition key at the application tier to map queries specified for a database shard.
Primary keys are unique across all database shards to avoid key collision during data migration among shards and the merging of data in the online analytical processing (OLAP)environment.
The column Creation_date serves as the data consistency point, with an assumption that the clocks of all nodes are synchronized. This column is used as a criterion for merging data from all database shards into the global view when essential.&lt;/li&gt;
&lt;/ul&gt;
&lt;h5&gt;Advantages&lt;/h5&gt;
&lt;p&gt;Using key-range-based sharding method, the range-query-based scheme is easy to implement. We precisely know where (which node, which shard) to look for a specific range of keys.
Range queries can be performed using the partitioning keys, and those can be kept in partitions in sorted order. How exactly such a sorting happens over time as new data comes in is implementation specific.&lt;/p&gt;
&lt;h5&gt;Disadvantages&lt;/h5&gt;
&lt;p&gt;Range queries can’t be performed using keys other than the partitioning key.&lt;/p&gt;
&lt;p&gt;If keys aren’t selected properly, some nodes may have to store more data due to an uneven distribution of the traffic.&lt;/p&gt;
&lt;h4&gt;Hash-based sharding&lt;/h4&gt;
&lt;p&gt;Hash-based sharding uses a hash function on an attribute. This hash function produces a hash value that is used to perform partitioning. The main concept is to use a hash function on the key to get a hash value and then mod by the number of partitions. Once we’ve found an appropriate hash function for keys, we may give each partition a range of hashes (rather than a range of keys). Any key whose hash occurs inside that range will be kept in that partition.&lt;/p&gt;
&lt;h5&gt;Advantages&lt;/h5&gt;
&lt;p&gt;Keys are uniformly distributed across the nodes.&lt;/p&gt;
&lt;h5&gt;Disadvantages&lt;/h5&gt;
&lt;p&gt;We can’t perform range queries with this technique. Keys will be spread over all partitions.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Why do you need databases? Why can’t you just use files?&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Answer:&lt;/strong&gt; Databases are crucial for several reasons: they efficiently manage large volumes of data, ensure data consistency and integrity, allow for easy updates, provide robust security measures, guarantee data availability, offer scalability, enable efficient data retrieval, and support data recovery and backup. These capabilities make databases superior to using files for data management, especially in applications requiring quick access to accurate and secure data&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Note: How many shards per database?&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;Empirically, we can determine how much each node can serve with acceptable performance. It can help us find out the maximum amount of data that we would like to keep on any one node. For example, if we find out that we can put a maximum of 50 GB of data on one node, we have the following:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Database size = 10 TB&lt;/li&gt;
&lt;li&gt;Size of a single shard = 50 GB&lt;/li&gt;
&lt;li&gt;Number of shards the database should be distributed in 10 TB/50 GB = 200 shards&lt;/li&gt;
&lt;/ul&gt;
&lt;h4&gt;Consistent hashing&lt;/h4&gt;
&lt;p&gt;Consistent hashing assigns each server or item in a distributed hash table a place on an abstract circle, called a ring, irrespective of the number of servers in the table. This permits servers and objects to scale without compromising the system’s overall performance.&lt;/p&gt;
&lt;h5&gt;Advantages of consistent hashing&lt;/h5&gt;
&lt;ul&gt;
&lt;li&gt;It’s easy to scale horizontally.&lt;/li&gt;
&lt;li&gt;It increases the throughput and improves the latency of the application.&lt;/li&gt;
&lt;/ul&gt;
&lt;h5&gt;Disadvantages of consistent hashing&lt;/h5&gt;
&lt;ul&gt;
&lt;li&gt;Randomly assigning nodes in the ring may cause non-uniform distribution.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;Rebalance the partitions&lt;/h3&gt;
&lt;p&gt;Query load can be imbalanced across the nodes due to many reasons, including the following:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;The distribution of the data isn’t equal.&lt;/li&gt;
&lt;li&gt;There’s too much load on a single partition.&lt;/li&gt;
&lt;li&gt;There’s an increase in the query traffic, and we need to add more nodes to keep up.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;We can apply the following strategies to rebalance partitions.&lt;/p&gt;
&lt;h3&gt;Avoid hash mod n&lt;/h3&gt;
&lt;p&gt;Usually, we avoid the hash of a key for partitioning (we used such a scheme to explain the concept of hashing in simple terms earlier). The problem with the addition or removal of nodes in the case of hash mod n is that every node’s partition number changes, and a lot of data moves. For example, assume we have hash(key)=1235. If we have five nodes at the start, the key will start on the first node (1235 mod 5=0). Now, if a new node is added, the key would have to be moved to the sixth node (1235 mod 6 = 5), and so on. This moving of keys from one node to another makes rebalancing costly.&lt;/p&gt;
&lt;h3&gt;Fixed number of partitions&lt;/h3&gt;
&lt;p&gt;In this approach, the number of partitions to be created is fixed at the time when we set our database up. We create a higher number of partitions than the nodes and assign these partitions to nodes. So, when a new node is added to the system, it can take a few partitions from the existing nodes until the partitions are equally divided.&lt;/p&gt;
&lt;p&gt;There’s a downside to this approach. The size of each partition grows with the total amount of data in the cluster since all the partitions contain a small part of the total data. If a partition is very small, it will result in too much overhead because we may have to make a large number of small-sized partitions, each costing us some overhead. If the partition is very large, rebalancing the nodes and recovering from node failures will be expensive. It’s very important to choose the right number of partitions. A fixed number of partitions is used in Elasticsearch, Riak, and many more.&lt;/p&gt;
&lt;h3&gt;Dynamic partitioning&lt;/h3&gt;
&lt;p&gt;In this approach, when the size of a partition reaches the threshold, it’s split equally into two partitions. One of the two split partitions is assigned to one node and the other one to another node. In this way, the load is divided equally. The number of partitions adapts to the overall data amount, which is an advantage of dynamic partitioning.&lt;/p&gt;
&lt;p&gt;However, there’s a downside to this approach. It’s difficult to apply dynamic rebalancing while serving the reads and writes. Dynamic rebalancing during reads and writes is challenging because it involves moving data between nodes, causing latency and potential conflicts. Ensuring data consistency (as data is simultaneously moved and accessed) and availability (potentially requiring pauses in reads/writes during rebalancing) introduces complexities that can impact system performance and reliability. This approach is used in HBase and MongoDB.&lt;/p&gt;
&lt;h3&gt;Partition proportionally to nodes&lt;/h3&gt;
&lt;p&gt;In this approach, the number of partitions is proportionate to the number of nodes, which means every node has fixed partitions. In earlier approaches, the number of partitions was dependent on the size of the dataset. That isn’t the case here. While the number of nodes remains constant, the size of each partition rises according to the dataset size. However, as the number of nodes increases, the partitions shrink. When a new node enters the network, it splits a certain number of current partitions at random, then takes one half of the split and leaves the other half alone. This can result in an unfair split. This approach is used by Cassandra and Ketama.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;How is rebalancing triggered when partitioning proportionally to nodes: automatically or manually?&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Answer:&lt;/strong&gt; When partitioning proportionally to nodes, rebalancing is typically performed automatically. The system calculates partition distribution based on current node capacities and automatically moves data when nodes are added or removed.&lt;/p&gt;
&lt;p&gt;Manual rebalancing, where an administrator manually redistributes partitions, is uncommon in this context but may be used in specialized setups.&lt;/p&gt;
&lt;h3&gt;Partitioning and secondary indexes&lt;/h3&gt;
&lt;p&gt;We’ve discussed key-value data model partitioning schemes in which the records are retrieved with primary keys. But what if we have to access the records through secondary indexes? Secondary indexes are the records that aren’t identified by primary keys but are just a way of searching for some value.&lt;/p&gt;
&lt;p&gt;We can partition with secondary indexes in the following ways.&lt;/p&gt;
&lt;h4&gt;Partition secondary indexes by document&lt;/h4&gt;
&lt;p&gt;Each partition is fully independent in this indexing approach. Each partition has its secondary indexes covering just the documents in that partition. It’s unconcerned with the data held in other partitions. If we want to write anything to our database, we need to handle that partition only containing the document ID we’re writing. It’s also known as the local index.&lt;/p&gt;
&lt;p&gt;However, this type of querying on secondary indexes can be expensive. As a result of being restricted by the latency of a poor-performing partition, read query latencies may increase.&lt;/p&gt;
&lt;h4&gt;Partition secondary indexes by the term&lt;/h4&gt;
&lt;p&gt;Instead of creating a secondary index for each partition (a local index), we can make a global index for secondary terms that encompasses data from all partitions.&lt;/p&gt;
&lt;p&gt;Partitioning secondary indexes by the term is more read-efficient than partitioning secondary indexes by the document. This is because it only accesses the partition that contains the term. However, a single write in this approach affects multiple partitions, making the method write-intensive and complex.&lt;/p&gt;
&lt;h2&gt;Request routing&lt;/h2&gt;
&lt;p&gt;We’ve learned how to partition our data. However, one question arises here: How does a client know which node to connect to while making a request? The allocation of partitions to nodes varies after rebalancing. If we want to read a specific key, how do we know which IP address we need to connect to read?&lt;/p&gt;
&lt;p&gt;This problem is also known as service discovery. The following are a few approaches to this problem:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Allow the clients to request any node in the network. If that node doesn’t contain the requested data, it forwards that request to the node that does contain the related data.&lt;/li&gt;
&lt;li&gt;The second approach contains a routing tier. All the requests are first forwarded to the routing tier, and it determines which node to connect to fulfill the request.&lt;/li&gt;
&lt;li&gt;The clients already have the information related to partitioning and which partition is connected to which node. So, they can directly contact the node that contains the data they need.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;In all of these approaches, the main challenge is to determine how these components know about updates in the partitioning of the nodes.&lt;/p&gt;
&lt;h2&gt;ZooKeeper&lt;/h2&gt;
&lt;p&gt;To track changes in the cluster, many distributed data systems need a separate management server like ZooKeeper. Zookeeper keeps track of all the mappings in the network, and each node connects to ZooKeeper for the information. Whenever there’s a change in the partitioning, or a node is added or removed, ZooKeeper gets updated and notifies the routing tier about the change. HBase, Kafka and SolrCloud use ZooKeeper.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Question:&lt;/strong&gt; Let’s assess our understanding of what’s described in this lesson with the following question:&lt;/p&gt;
&lt;p&gt;Imagine you’re a database architect for a rapidly expanding e-commerce platform with a global user base. The platform experiences varying user activity levels across different regions. The existing monolithic database struggles to handle the increasing load. Users in different regions have distinct sets of preferences and tend to interact more within their regions. Now, we’re looking for an efficient and scalable solution to optimize performance, enhance scalability, and cater to regional variations in user behavior. Regarding this, which one of the following strategies would you adopt, and why?&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Database sharding&lt;/li&gt;
&lt;li&gt;Database replication&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Solution:&lt;/strong&gt; The best strategy here is database sharding. Sharding involves dividing the database into smaller, manageable pieces called shards, each handling a specific region or subset of data. This approach improves scalability and performance by reducing the load on any single database instance and aligns well with regional user preferences and activity levels. It allows the platform to efficiently handle increasing traffic and regional variations.&lt;/p&gt;
&lt;h2&gt;Conclusion&lt;/h2&gt;
&lt;p&gt;For all current distributed systems, partitioning has become the standard protocol. Because systems contain increasing amounts of data, partitioning the data makes sense since it speeds up both writes and reads. It increases the system’s availability, scalability, and performance.&lt;/p&gt;</content><category term="Blog"/><category term="system-design"/><category term="databases"/></entry><entry><title>System Design: Lesson 4.5 - Trade-offs in Databases</title><link href="https://dmb225.github.io/system-design-lesson-45-trade-offs-in-databases.html" rel="alternate"/><published>2026-02-05T00:00:00+01:00</published><updated>2026-02-05T00:00:00+01:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2026-02-05:/system-design-lesson-45-trade-offs-in-databases.html</id><summary type="html">&lt;h2&gt;Which is the best database sharding approach?&lt;/h2&gt;
&lt;p&gt;Both horizontal and vertical sharding involve adding resources to our computing infrastructure. Our business stakeholders must decide which is suitable for our organization. We must scale our resources accordingly for our organization and business to grow, to prevent downtime, and to reduce latency …&lt;/p&gt;</summary><content type="html">&lt;h2&gt;Which is the best database sharding approach?&lt;/h2&gt;
&lt;p&gt;Both horizontal and vertical sharding involve adding resources to our computing infrastructure. Our business stakeholders must decide which is suitable for our organization. We must scale our resources accordingly for our organization and business to grow, to prevent downtime, and to reduce latency. We can scale these resources through a combination of adjustments to CPU, physical memory requirements, hard disk adjustments, and network bandwidth.&lt;/p&gt;
&lt;p&gt;The following sections explain the pros and cons of centralized and distributed databases:&lt;/p&gt;
&lt;h2&gt;Advantages and disadvantages of a centralized database&lt;/h2&gt;
&lt;h3&gt;Advantages&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;Data maintenance, such as updating and taking backups of a centralized database, is easy.&lt;/li&gt;
&lt;li&gt;Centralized databases provide stronger consistency and ACID transactions than distributed databases.&lt;/li&gt;
&lt;li&gt;Centralized databases provide a much simpler programming model for the end programmers as compared to distributed databases.&lt;/li&gt;
&lt;li&gt;It’s more efficient for businesses that have a small amount of data to store that can reside on a single node.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;Disadvantages&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;A centralized database can slow down, causing high latency for end users, when the number of queries per second accessing the centralized database is approaching single-node limits.&lt;/li&gt;
&lt;li&gt;A centralized database has a single point of failure. Because of this, its probability of not being accessible is much higher.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Advantages and disadvantages of a distributed database&lt;/h2&gt;
&lt;h3&gt;Advantages&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;It’s fast and easy to access data in a distributed database because data is retrieved from the nearest database shard or the one frequently used.&lt;/li&gt;
&lt;li&gt;Data with different levels of distribution transparency can be stored in separate places.&lt;/li&gt;
&lt;li&gt;Intensive transactions consisting of queries can be divided into multiple optimized subqueries, which can be processed in a parallel fashion.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;Disadvantages&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;Sometimes, data is required from multiple sites, which takes more time than expected.&lt;/li&gt;
&lt;li&gt;Relations are partitioned vertically or horizontally among different nodes. Therefore, operations such as joins need to reconstruct complete relations by carefully fetching data. These operations can become much more expensive and complex.&lt;/li&gt;
&lt;li&gt;It’s difficult to maintain consistency of data across sites in the distributed database, and it requires extra measures.&lt;/li&gt;
&lt;li&gt;Updations and backups in distributed databases take time to synchronize data.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Conclusion&lt;/h2&gt;
&lt;p&gt;Data distribution (vertical and horizontal sharding) across multiple nodes aims to improve the following features, considering that the queries are optimized:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Reliability (fault-tolerance)&lt;/li&gt;
&lt;li&gt;Performance&lt;/li&gt;
&lt;li&gt;Balanced storage capacity and dollar costs&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Both centralized and distributed databases have their pros and cons. We should choose them according to the needs of our application.&lt;/p&gt;</content><category term="Blog"/><category term="system-design"/><category term="databases"/></entry><entry><title>System Design: Lesson 3.2 - Global and local load balancing</title><link href="https://dmb225.github.io/system-design-lesson-32-global-and-local-load-balancing.html" rel="alternate"/><published>2026-02-04T00:00:00+01:00</published><updated>2026-02-04T00:00:00+01:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2026-02-04:/system-design-lesson-32-global-and-local-load-balancing.html</id><summary type="html">&lt;h2&gt;Introduction&lt;/h2&gt;
&lt;p&gt;From the previous lesson, it may seem like load balancing is performed only within the data center. However, load balancing is required at a global and a local scale. Let’s understand the function of each of the two:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Global server load balancing (GSLB): GSLB involves the distribution of …&lt;/li&gt;&lt;/ul&gt;</summary><content type="html">&lt;h2&gt;Introduction&lt;/h2&gt;
&lt;p&gt;From the previous lesson, it may seem like load balancing is performed only within the data center. However, load balancing is required at a global and a local scale. Let’s understand the function of each of the two:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Global server load balancing (GSLB): GSLB involves the distribution of traffic load across multiple geographical regions.&lt;/li&gt;
&lt;li&gt;Local load balancing: This refers to load balancing achieved within a data center. This type of load balancing focuses on improving efficiency and better resource utilization of the hosting servers in a data center.
Let’s understand each of the two techniques below.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Global server load balancing&lt;/h2&gt;
&lt;p&gt;GSLB ensures that globally arriving traffic load is intelligently forwarded to a data center. For example, power or network failure in a data center requires that all the traffic be rerouted to another data center. GSLB takes forwarding decisions based on the users’ geographic locations, the number of hosting servers in different locations, the health of data centers, and so on.&lt;/p&gt;
&lt;p&gt;In the next lesson, we’ll also learn how GSLB offers automatic zonal failover. GSLB service can be installed on-premises or obtained through Load Balancing as a Service (LBaaS).&lt;/p&gt;
&lt;p&gt;The GSLB can forward requests to three different data centers. Each local load balancing layer within a data center will maintain a control plane connection with the GSLB providing information about the health of the LBs and the server farm. GSLB uses this information to drive traffic decisions and forward traffic load based on each region’s configuration and monitoring information.&lt;/p&gt;
&lt;p&gt;Now, we’ll discuss how the domain name system (DNS) can perform GSLB.&lt;/p&gt;
&lt;h2&gt;Load balancing in DNS&lt;/h2&gt;
&lt;p&gt;We understand that DNS can respond with multiple IP addresses for a DNS query. In the lesson on DNS, we discussed that it’s possible to do load balancing through DNS while looking at the output of nslookup. DNS uses a simple technique of reordering the list of IP addresses in response to each DNS query. Therefore, different users get a reordered IP address list. It results in users visiting a different server to entertain their requests. In this way, DNS distributes the load of requests on different data centers. This is performing GSLB. In particular, DNS uses round-robin to perform load balancing.&lt;/p&gt;
&lt;p&gt;However, round-robin has the following limitations:&lt;/p&gt;
&lt;p&gt;Different ISPs have a different number of users. An ISP serving many customers will provide the same cached IP to its clients, resulting in uneven load distribution on end-servers.
Because the round-robin load-balancing algorithm doesn’t consider any end-server crashes, it keeps on distributing the IP address of the crashed servers until the TTL of the cached entries expires. Availability of the service, in that case, can take a hit due to DNS-level load balancing.&lt;/p&gt;
&lt;p&gt;Despite its limitations, round-robin is still widely used by many DNS service providers. Furthermore, DNS uses short TTL for cached entries to do effective load balancing among different data centers.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Note: DNS isn’t the only form of GSLB. Application delivery controllers (ADCs) and cloud-based load balancing (discussed later) are better ways to do GSLB.&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Application delivery controllers (ADCs) are part of the application delivery network (ADN). They can be considered the superset of LBs offering various services, including load balancing. The primary task of ADCs is to perform web acceleration to reduce the load from the server farm. Some well-known services between layers 3 and 7 include caching, SSL offloading, proxy/reverse proxy services, IP traffic optimization, and many more. ADCs also implement GSLB.&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Anycast is a networking technique where the same IP address is assigned to multiple servers in different locations. Traffic is then routed to the nearest server based on the network's routing table, improving response times. With this in mind, explain the role of anycast in global load balancing and its advantages compared to traditional Global Server Load Balancing (GSLB) mechanisms.&lt;/em&gt;&lt;/p&gt;
&lt;h3&gt;Anycast in load balancing&lt;/h3&gt;
&lt;p&gt;Anycast plays a key role in global load balancing by assigning the same IP address to multiple servers located in different regions. When a user makes a request, the network’s routing infrastructure directs the traffic to the nearest or best-performing server based on network topology, which helps reduce latency and improve response times.&lt;/p&gt;
&lt;p&gt;The advantages of anycast compared to traditional GSLB mechanisms include faster failover, as traffic is automatically rerouted if a server or location becomes unreachable, and simplified DNS management since there’s no need for complex round-robin or geo-aware DNS configurations.&lt;/p&gt;
&lt;p&gt;It’s especially effective for latency-sensitive applications like video streaming, gaming, or real-time communication.&lt;/p&gt;
&lt;h2&gt;The need for local load balancers&lt;/h2&gt;
&lt;p&gt;DNS plays a vital role in balancing the load, but it suffers from the following limitations:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;The small size of the DNS packet (512 Bytes) isn’t enough to include all possible IP addresses of the servers.&lt;/li&gt;
&lt;li&gt;There’s limited control over the client’s behavior. Clients may select arbitrarily from the received set of IP addresses. Some of the received IP addresses may belong to busy data centers.&lt;/li&gt;
&lt;li&gt;Clients can’t determine the closest address to establish a connection with. Although DNS geolocation and anycasting-like solutions can be implemented, they aren’t trivial solutions.&lt;/li&gt;
&lt;li&gt;In case of failures, recovery can be slow through DNS because of the caching mechanism, especially when TTL values are longer.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;To solve some of the above problems, we need another layer of load balancing in the form of local LB. In the next lesson, we’ll discuss different details about local load balancers.&lt;/p&gt;
&lt;h2&gt;What is local load balancing?&lt;/h2&gt;
&lt;p&gt;Local load balancers reside within a data center. They behave like a reverse proxy and make their best effort to divide incoming requests among the pool of available servers. Incoming clients’ requests seamlessly connect to the LB that uses a virtual IP address (VIP).&lt;/p&gt;
&lt;h3&gt;Can DNS be considered a global server load balancer (GSLB)?&lt;/h3&gt;
&lt;p&gt;Yes, DNS can be used as part of global server load balancing (GSLB), but it is typically implemented through custom DNS infrastructure. There are two common approaches to global traffic management (GTM):&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;GTM through ADCs: Some Application Delivery Controllers (ADCs) support GSLB by forwarding requests based on real-time data such as server health and data center capacity.&lt;/li&gt;
&lt;li&gt;GTM through DNS: Organizations or DNS providers configure authoritative DNS servers that respond to client requests based on the IP address of the DNS resolver (which is usually close to the user). This allows them to direct traffic to the data center that is geographically or topologically nearest, or based on routing policies such as latency or availability. This is typically achieved via third-party managed DNS providers (e.g., AWS Route 53, Cloudflare) or custom DNS infrastructure that incorporates geolocation and health data.&lt;/li&gt;
&lt;/ul&gt;</content><category term="Blog"/><category term="system-design"/><category term="load-balancers"/></entry><entry><title>System Design: Lesson 3.3 - Advanced Details of Load Balancers</title><link href="https://dmb225.github.io/system-design-lesson-33-advanced-details-of-load-balancers.html" rel="alternate"/><published>2026-02-04T00:00:00+01:00</published><updated>2026-02-04T00:00:00+01:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2026-02-04:/system-design-lesson-33-advanced-details-of-load-balancers.html</id><summary type="html">&lt;p&gt;This lesson will focus on some of the well-known algorithms used in the local load balancers. We’ll also understand how load balancers are connected to form a hierarchy, sharing work across different tiers of LBs.&lt;/p&gt;
&lt;h2&gt;Algorithms of load balancers&lt;/h2&gt;
&lt;p&gt;Load balancers distribute client requests according to an algorithm. Some …&lt;/p&gt;</summary><content type="html">&lt;p&gt;This lesson will focus on some of the well-known algorithms used in the local load balancers. We’ll also understand how load balancers are connected to form a hierarchy, sharing work across different tiers of LBs.&lt;/p&gt;
&lt;h2&gt;Algorithms of load balancers&lt;/h2&gt;
&lt;p&gt;Load balancers distribute client requests according to an algorithm. Some well-known algorithms are given below:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Round-robin scheduling: In this algorithm, each request is forwarded to a server in the pool in a repeating sequential manner.&lt;/li&gt;
&lt;li&gt;Weighted round-robin: If some servers have a higher capability of serving clients’ requests, then it’s preferred to use a weighted round-robin algorithm. In a weighted round-robin algorithm, each node is assigned a weight. LBs forward clients’ requests according to the weight of the node. The higher the weight, the higher the number of assignments.&lt;/li&gt;
&lt;li&gt;Least connections: In certain cases, even if all the servers have the same capacity to serve clients, uneven load on certain servers is still a possibility. For example, some clients may have a request that requires longer to serve. Or some clients may have subsequent requests on the same connection. In that case, we can use algorithms like least connections, where newer arriving requests are assigned to servers with fewer existing connections. LBs keep a state of the number and mapping of existing connections in such a scenario. We’ll discuss more about state maintenance later in the lesson.&lt;/li&gt;
&lt;li&gt;Least response time: In performance-sensitive services, algorithms such as least response time are required. This algorithm ensures that the server with the least response time is requested to serve the clients.&lt;/li&gt;
&lt;li&gt;IP hash: Some applications provide a different level of service to users based on their IP addresses. In that case, hashing the IP address is performed to assign users’ requests to servers.&lt;/li&gt;
&lt;li&gt;URL hash: It may be possible that some services within the application are provided by specific servers only. In that case, a client requesting service from a URL is assigned to a certain cluster or set of servers. The URL hashing algorithm is used in those scenarios.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;There are other algorithms, such as randomized or weighted least connections algorithms.&lt;/p&gt;
&lt;h2&gt;Static versus dynamic algorithms&lt;/h2&gt;
&lt;p&gt;Algorithms can be static or dynamic depending on the machine’s state. Let’s look at each of the categories individually:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Static algorithms don’t consider the changing state of the servers. Therefore, task assignment is carried out based on existing knowledge about the server’s configuration. Naturally, these algorithms aren’t complex, and they get implemented in a single router or commodity machine where all the requests arrive.&lt;/li&gt;
&lt;li&gt;Dynamic algorithms are algorithms that consider the current or recent state of the servers. Dynamic algorithms maintain state by communicating with the server, which adds communication overhead. State maintenance makes the design of the algorithm much more complicated.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Dynamic algorithms require different load balancing servers to communicate with each other to exchange information. Therefore, dynamic algorithms can be modular because no single entity will do the decision-making. Although this adds complexity to dynamic algorithms, it results in improved forwarding decisions. Finally, dynamic algorithms monitor the health of the servers and forward requests to active servers only.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Note: In practice, dynamic algorithms provide far better results because they maintain a state of serving hosts and are, therefore, worth the effort and complexity.&lt;/em&gt;&lt;/p&gt;
&lt;h2&gt;Stateful versus stateless LBs&lt;/h2&gt;
&lt;p&gt;While static and dynamic algorithms are required to consider the health of the hosting servers, a state is maintained to hold session information of different clients with hosting servers.&lt;/p&gt;
&lt;p&gt;If the session information isn’t kept at a lower layer (distributed cache or database), load balancers are used to keep the session information. Below, we describe two ways of handling session maintenance through LBs:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Stateful&lt;/li&gt;
&lt;li&gt;Stateless&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;Stateful load balancing&lt;/h3&gt;
&lt;p&gt;As the name indicates, stateful load balancing involves maintaining a state of the sessions established between clients and hosting servers. The stateful LB incorporates state information in its algorithm to perform load balancing.&lt;/p&gt;
&lt;p&gt;Essentially, the stateful LBs retain a data structure that maps incoming clients to hosting servers. Stateful LBs increase complexity and limit scalability because session information of all the clients is maintained across all the load balancers. That is, load balancers share their state information with each other to make forwarding decisions.&lt;/p&gt;
&lt;h3&gt;Stateless load balancing&lt;/h3&gt;
&lt;p&gt;Stateless load balancing maintains no state and is, therefore, faster and lightweight. Stateless LBs use consistent hashing to make forwarding decisions. However, if infrastructure changes (for example, a new application server joining), stateless LBs may not be as resilient as stateful LBs because consistent hashing alone isn’t enough to route a request to the correct application server. Therefore, a local state may still be required along with consistent hashing.&lt;/p&gt;
&lt;p&gt;Therefore, a state maintained across different load balancers is considered as stateful load balancing. Whereas, a state maintained within a load balancer for internal use is assumed as stateless load balancing.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Having familiarized yourself with load balancers, consider a scenario where you deploy a website that serves static content, such as text and images. Would you choose a stateful or stateless load balancer, and why?&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Solution:&lt;/strong&gt; Well, for serving static content like text and images, a stateless load balancer is the best choice. It doesn’t need to keep track of user sessions, which makes it simpler, faster, and easier to scale. This approach ensures efficient distribution of requests and better performance overall.&lt;/p&gt;
&lt;h2&gt;Types of load balancers&lt;/h2&gt;
&lt;p&gt;Depending on the requirements, load balancing can be performed at the network/transport and application layer of the open systems interconnection (OSI) layers.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Layer 4 load balancers: Layer 4 refers to load balancing performed using transport protocols like TCP and UDP. These types of LBs maintain connections/sessions with clients and ensure that the same (TCP/UDP) communication is forwarded to the same back-end server. Even though TLS termination is performed at layer 7 LBs, some layer 4 LBs also support it.&lt;/li&gt;
&lt;li&gt;Layer 7 load balancers: Layer 7 load balancers are based on the data of application layer protocols. It’s possible to make application-aware forwarding decisions based on HTTP headers, URLs, cookies, and other application-specific data—for example, user ID. Apart from performing TLS termination, these LBs can take responsibilities like rate limiting users, HTTP routing, and header rewriting.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;em&gt;Note: Layer 7 load balancers are smart in terms of inspection. However, layer 4 load balancers are faster in terms of processing.&lt;/em&gt;&lt;/p&gt;
&lt;h2&gt;Load balancer deployment&lt;/h2&gt;
&lt;p&gt;We discussed the trade-offs of load balancing performed at different OSI layers. In practice, however, a single layer LB isn’t enough for a large data center. In fact, multiple layers of load balancers coordinate to take informed forwarding decisions. A traditional data center may have a three-tier LB.&lt;/p&gt;
&lt;h3&gt;Tier-0 and tier-1 LBs&lt;/h3&gt;
&lt;p&gt;If DNS can be considered as the tier-0 load balancer, equal cost multipath (ECMP) routers are the tier-1 LBs. From the name of ECMP, it’s evident that this layer divides incoming traffic on the basis of IP or some other algorithm like round-robin or weighted round-robin. Tier-1 LBs will balance the load across different paths to higher tiers of load balancers.&lt;/p&gt;
&lt;p&gt;ECMP routers play a vital role in the horizontal scalability of the higher-tier LBs.&lt;/p&gt;
&lt;h3&gt;Tier-2 LBs&lt;/h3&gt;
&lt;p&gt;The second tier of LBs include layer 4 load balancers. Tier-2 LBs make sure that for any connection, all incoming packets are forwarded to the same tier-3 LBs. To achieve this goal, a technique like consistent hashing can be utilized. But in case of any changes to the infrastructure, consistent hashing may not be enough. Therefore, we have to maintain a local or global state as we’ll see in the coming sections of the lesson.&lt;/p&gt;
&lt;p&gt;Tier-2 load balancers can be considered the glue between tier-1 and tier-3 LBs. Excluding tier-2 LBs could result in erroneous forwarding decisions in case of failures or dynamic scaling of LBs.&lt;/p&gt;
&lt;h3&gt;Tier-3 LBs&lt;/h3&gt;
&lt;p&gt;Layer 7 LBs provide services at tier 3. Since these LBs are in direct contact with the back-end servers, they perform health monitoring of servers at HTTP level. This tier enables scalability by evenly distributing requests among the set of healthy back-end servers and provides high availability by monitoring the health of servers directly. This tier also reduces the burden on end-servers by handling low-level details like TCP-congestion control protocols, the discovery of Path MTU (maximum transmission unit), the difference in application protocol between client and back-end servers, and so on. The idea is to leave the computation and data serving to the application servers and effectively utilize load balancing commodity machines for trivial tasks. In some cases, layer 7 LBs are at the same level as the service hosts.&lt;/p&gt;
&lt;p&gt;To summarize, tier 1 balances the load among the load balancers themselves. Tier 2 enables a smooth transition from tier 1 to tier 3 in case of failures, whereas tier 3 does the actual load balancing between back-end servers. Each tier performs other tasks to reduce the burden on end-servers.&lt;/p&gt;
&lt;h2&gt;Implementation of load balancers&lt;/h2&gt;
&lt;p&gt;Different kinds of load balancers can be implemented depending on the number of incoming requests, organization, and application-specific requirements:&lt;/p&gt;
&lt;h3&gt;Hardware load balancers&lt;/h3&gt;
&lt;p&gt;Load balancers were introduced in the 1990s as hardware devices. Hardware load balancers work as stand-alone devices and are quite expensive. Nonetheless, they have their performance benefits and are able to handle a lot of concurrent users. Configuration of hardware-based solutions is problematic because it requires additional human resources. Therefore, they aren’t the go-to solutions even for large enterprises that can afford them. Availability can be an issue with hardware load balancers because additional hardware will be required to failover to in case of failures. Finally, hardware LBs can have higher maintenance/operational costs and compatibility issues making them less flexible. Not to mention that hardware LBs have vendor locks as well.&lt;/p&gt;
&lt;h3&gt;Software load balancers&lt;/h3&gt;
&lt;p&gt;Software load balancers are becoming increasingly popular because of their flexibility, programmability, and cost-effectiveness. That’s all possible because they’re implemented on commodity hardware. Software LBs scale well as requirements grow. Availability won’t be an issue with software LBs because small additional costs are required to implement shadow load balancers on commodity hardware. Additionally, software LBs can provide predictive analysis that can help prepare for future traffic patterns.&lt;/p&gt;
&lt;h3&gt;Cloud load balancers&lt;/h3&gt;
&lt;p&gt;With the advent of the field of cloud computing, Load Balancers as a Service (LBaaS) has been introduced. This is where cloud owners provide load balancing services. Users pay according to their usage or the service-level agreement (SLA) with the cloud provider. Cloud-based LBs may not necessarily replace a local on-premise load balancing facility, but they can perform global traffic management between different zones. Primary advantages of such load balancers include ease of use, management, metered cost, flexibility in terms of usage, auditing, and monitoring services to improve business decisions.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Note: Another interesting implementation of load balancers comes in the form of client-side load balancing. Client-side load balancing is suited where there are numerous services, each with many instances (such as load balancing in Twitter). Our focus, however, is on traditional load balancers because most three-tier applications employ these in their design.&lt;/em&gt;&lt;/p&gt;
&lt;h2&gt;Conclusion&lt;/h2&gt;
&lt;p&gt;LBs have come a long way to become a service offered in the cloud, starting since their inception in the form of hardware. They’re a key component of any enterprise-level service. Horizontal scalability of hosting servers will always require a good load balancing layer capable of providing load balancing, session maintenance, TLS offloading, service discovery, and more.&lt;/p&gt;
&lt;p&gt;In this chapter, we have explored various functions of load balancing. In addition to different roles, load balancers are often responsible for mitigating distributed denial-of-service (DDoS) attacks.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;How can they distinguish between legitimate traffic and malicious traffic during such incidents?&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Solution:&lt;/strong&gt; During DDoS attacks, load balancers distinguish between legitimate and malicious traffic by analyzing traffic patterns for anomalies, such as sudden spikes from a single IP. They also use rate-limiting to restrict request rates, challenge-response protocols like CAPTCHAs to verify users, and leverage threat intelligence to block known malicious IPs or regions. Additionally, behavioral analytics and machine learning models help identify attack patterns.&lt;/p&gt;
&lt;p&gt;This requires that you have some way of knowing if something has been modified without actually querying it. For example, user profile information on a social network is typically only editable by the profile owner and not by anyone else.&lt;/p&gt;</content><category term="Blog"/><category term="system-design"/><category term="load-balancers"/></entry><entry><title>System Design: Lesson 4.1 - Introduction to Databases</title><link href="https://dmb225.github.io/system-design-lesson-41-introduction-to-databases.html" rel="alternate"/><published>2026-02-04T00:00:00+01:00</published><updated>2026-02-04T00:00:00+01:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2026-02-04:/system-design-lesson-41-introduction-to-databases.html</id><summary type="html">&lt;h2&gt;Problem statement&lt;/h2&gt;
&lt;p&gt;Let’s start with a simple question. Can we make a software application without using databases? Let’s suppose we have an application like WhatsApp. People use our application to communicate with their friends. Now, where and how can we store information (a list of people’s names …&lt;/p&gt;</summary><content type="html">&lt;h2&gt;Problem statement&lt;/h2&gt;
&lt;p&gt;Let’s start with a simple question. Can we make a software application without using databases? Let’s suppose we have an application like WhatsApp. People use our application to communicate with their friends. Now, where and how can we store information (a list of people’s names and their respective messages) permanently and retrieve it?&lt;/p&gt;
&lt;p&gt;We can use a simple file to store all the records on separate lines and retrieve them from the same file. But using a file for storage has some limitations.&lt;/p&gt;
&lt;h2&gt;Limitations of file storage&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;We can’t offer concurrent management to separate users accessing the storage files from different locations.&lt;/li&gt;
&lt;li&gt;We can’t grant different access rights to different users.&lt;/li&gt;
&lt;li&gt;How will the system scale and be available when adding thousands of entries?&lt;/li&gt;
&lt;li&gt;How will we search content for different users in a short time?&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Solution&lt;/h2&gt;
&lt;p&gt;The above limitations can be addressed using databases.&lt;/p&gt;
&lt;p&gt;A database is an organized collection of data that can be managed and accessed easily. Databases are created to make it easier to store, retrieve, modify, and delete data in connection with different data-processing procedures.&lt;/p&gt;
&lt;p&gt;Some of the applications where we use database management are the banking systems, online shopping stores, and so on. Different organizations have different sizes of databases according to their needs.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Note: It seems that the World Data Center for Climate (WDCC) is the largest database in the world. It contains around 220 terabytes of web data and 6 petabytes of additional data.&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;There are two basic types of databases:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;SQL (relational databases)&lt;/li&gt;
&lt;li&gt;NoSQL (non-relational databases)&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;They differ in terms of their intended use case, the type of information they hold, and the storage method they employ.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Relational database has a well defined structure such as attributes (columns of the table). NoSQL databases such as document databases often have application-defined structure of data.&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;Relational databases, like phone books that record contact numbers and addresses, are organized and have predetermined schemas. Non-relational databases, like file directories that store anything from a person’s contact information to shopping preferences, are unstructured, scattered, and feature a dynamic schema. We’ll discuss their differences and their types in detail in the next lesson.&lt;/p&gt;
&lt;h2&gt;Advantages&lt;/h2&gt;
&lt;p&gt;A proper database is essential for every business or organization. This is because the database stores all essential information about the organization, such as personnel records, transactions, salary information, and so on. The following are some of the reasons why the database is important:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Managing large data: A large amount of data can be easily handled with a database, which wouldn’t be possible using other tools.&lt;/li&gt;
&lt;li&gt;Retrieving accurate data (data consistency): Due to different constraints in databases, we can retrieve accurate data whenever we want.&lt;/li&gt;
&lt;li&gt;Easy updation: It is quite easy to update data in databases using data manipulation language (DML).&lt;/li&gt;
&lt;li&gt;Security: Databases ensure the security of the data. A database only allows authorized users to access data.&lt;/li&gt;
&lt;li&gt;Data integrity: Databases ensure data integrity by using different constraints for data.&lt;/li&gt;
&lt;li&gt;Availability: Databases can be replicated (using data replication) on different servers, which can be concurrently updated. These replicas ensure availability.&lt;/li&gt;
&lt;li&gt;Scalability: Databases are divided (using data partitioning) to manage the load on a single node. This increases scalability.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;How will we explain databases?&lt;/h2&gt;
&lt;p&gt;We have divided the database chapter into four lessons:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Types of Databases: We’ll discuss the different types of databases, their advantages, and their disadvantages.&lt;/li&gt;
&lt;li&gt;Data Replication: We’ll discuss what data replication is and its different models with their pros and cons.&lt;/li&gt;
&lt;li&gt;Data Partitioning: We’ll discuss what data partitioning is and its different models with their pros and cons.&lt;/li&gt;
&lt;li&gt;Cost-benefit analysis: We’ll discuss which database sharding approach is best for different kinds of databases.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Let’s get started by understanding different types of databases and their preferred use cases.&lt;/p&gt;</content><category term="Blog"/><category term="system-design"/><category term="databases"/></entry><entry><title>System Design: Lesson 1 - Building Blocks</title><link href="https://dmb225.github.io/system-design-lesson-1-building-blocks.html" rel="alternate"/><published>2026-02-03T00:00:00+01:00</published><updated>2026-02-03T00:00:00+01:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2026-02-03:/system-design-lesson-1-building-blocks.html</id><summary type="html">&lt;p&gt;System design problems usually have some similarities, though specific details are often unique. We have extracted these similarities across design problems as the basic building blocks we’ll be covering. One example of a building block is a load-balancing component, which we’ll probably use in every design problem in …&lt;/p&gt;</summary><content type="html">&lt;p&gt;System design problems usually have some similarities, though specific details are often unique. We have extracted these similarities across design problems as the basic building blocks we’ll be covering. One example of a building block is a load-balancing component, which we’ll probably use in every design problem in one way or the other.&lt;/p&gt;
&lt;p&gt;The purpose of separating the building blocks is to thoroughly discuss their design just once. This means that later we can use them anywhere without having to go over them in detail again. We can think about building blocks as bricks to construct more effective, capable systems.&lt;/p&gt;
&lt;p&gt;Many of the building blocks we discuss are also available for actual use in the public clouds, such as Amazon Web Services (AWS), Azure, and Google Cloud Platform (GCP). We can use such constructs to build a system to further cement our understanding. (We won’t construct the system in this course, but we’ve left it as an exercise for interested learners.)&lt;/p&gt;
&lt;p&gt;We’ll discuss the following building blocks in detail:&lt;/p&gt;
&lt;h3&gt;Domain Name System&lt;/h3&gt;
&lt;p&gt;This building block focuses on how to design hierarchical and distributed naming systems for computers connected to the Internet via different Internet protocols.&lt;/p&gt;
&lt;h3&gt;Load Balancers&lt;/h3&gt;
&lt;p&gt;Here, we’ll understand the design of a load balancer, which is used to fairly distribute incoming clients’ requests among a pool of available servers. It also reduces load and can bypass failed servers.&lt;/p&gt;
&lt;h3&gt;Databases&lt;/h3&gt;
&lt;p&gt;This building block enables us to store, retrieve, modify, and delete data in connection with different data-processing procedures. Here, we’ll discuss database types, replication, partitioning, and analysis of distributed databases.&lt;/p&gt;
&lt;h3&gt;Key-Value Store&lt;/h3&gt;
&lt;p&gt;It is a non-relational database that stores data in the form of a key-value pair. Here, we’ll explain the design of a key-value store along with important concepts such as achieving scalability, durability, and configurability.&lt;/p&gt;
&lt;h3&gt;Content Delivery Network&lt;/h3&gt;
&lt;p&gt;In this chapter, we’ll design a content delivery network (CDN) that’s used to keep viral content such as videos, images, audio, and webpages. It efficiently delivers content to end users while reducing latency and burden on the data centers.&lt;/p&gt;
&lt;h3&gt;Sequencer&lt;/h3&gt;
&lt;p&gt;In this building block, we’ll focus on the design of a unique IDs generator with a major focus on maintaining causality. It also explains three different methods for generating unique IDs.&lt;/p&gt;
&lt;h3&gt;Service Monitoring&lt;/h3&gt;
&lt;p&gt;Monitoring systems are critical in distributed systems because they help analyze the system and alert the stakeholders if a problem occurs. Monitoring is often useful to get early warning systems so that system administrators can act ahead of an impending problem becoming a huge issue. Here, we’ll build two monitoring systems, one for the server-side and the other for client-side errors.&lt;/p&gt;
&lt;h3&gt;Distributed Caching&lt;/h3&gt;
&lt;p&gt;In this building block, we’ll design a distributed caching system where multiple cache servers coordinate to store frequently accessed data.&lt;/p&gt;
&lt;h3&gt;Distributed Messaging Queue&lt;/h3&gt;
&lt;p&gt;In this building block, we’ll focus on the design of a queue consisting of multiple servers, which is used between interacting entities called producers and consumers. It helps decouple producers and consumers, results in independent scalability, and enhances reliability.&lt;/p&gt;
&lt;h3&gt;Publish-Subscribe System&lt;/h3&gt;
&lt;p&gt;In this building block, we’ll focus on the design of an asynchronous service-to-service communication method called a pub-sub system. It is popular in serverless, microservices architectures and data processing systems.&lt;/p&gt;
&lt;h3&gt;Rate Limiter&lt;/h3&gt;
&lt;p&gt;Here, we’ll design a system that throttles incoming requests for a service based on the predefined limit. It is generally used as a defensive layer for services to avoid their excessive usage—whether intended or unintended.&lt;/p&gt;
&lt;h3&gt;Blob Store&lt;/h3&gt;
&lt;p&gt;This building block focuses on a storage solution for unstructured data—for example, multimedia files and binary executables.&lt;/p&gt;
&lt;h3&gt;Distributed Search&lt;/h3&gt;
&lt;p&gt;A search system takes a query from a user and returns relevant content in a few seconds or less. This building block focuses on the three integral components: crawl, index, and search.&lt;/p&gt;
&lt;h3&gt;Distributed Logging&lt;/h3&gt;
&lt;p&gt;Logging is an I/O intensive operation that is time-consuming and slow. Here, we’ll design a system that allows services in a distributed system to log their events efficiently. The system will be made scalable and reliable.&lt;/p&gt;
&lt;h3&gt;Distributed Task Scheduling&lt;/h3&gt;
&lt;p&gt;We’ll design a distributed task scheduler system that mediates between tasks and resources. It intelligently allocates resources to tasks to meet task-level and system-level goals. It’s often used to offload background processing to be completed asynchronously.&lt;/p&gt;
&lt;h3&gt;Sharded Counters&lt;/h3&gt;
&lt;p&gt;This building block demonstrates an efficient distributed counting system to deal with millions of concurrent read/write requests, such as likes on a celebrity’s tweet.&lt;/p&gt;</content><category term="Blog"/><category term="system-design"/></entry><entry><title>System Design: Lesson 2.1 - Introduction to Domain Name System (DNS)</title><link href="https://dmb225.github.io/system-design-lesson-21-introduction-to-domain-name-system-dns.html" rel="alternate"/><published>2026-02-03T00:00:00+01:00</published><updated>2026-02-03T00:00:00+01:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2026-02-03:/system-design-lesson-21-introduction-to-domain-name-system-dns.html</id><summary type="html">&lt;h2&gt;The origins of DNS&lt;/h2&gt;
&lt;p&gt;Let’s consider the example of a mobile phone where a unique number is associated with each user. To make calls to friends, we can initially try to memorize some of the phone numbers. However, as the number of contacts grows, we’ll have to use …&lt;/p&gt;</summary><content type="html">&lt;h2&gt;The origins of DNS&lt;/h2&gt;
&lt;p&gt;Let’s consider the example of a mobile phone where a unique number is associated with each user. To make calls to friends, we can initially try to memorize some of the phone numbers. However, as the number of contacts grows, we’ll have to use a phone book to keep track of all our contacts. This way, whenever we need to make a call, we’ll refer to the phone book and dial the number we need.&lt;/p&gt;
&lt;p&gt;Similarly, computers are uniquely identified by IP addresses—for example, 104.18.2.119 is an IP address. We use IP addresses to visit a website hosted on a machine. Since humans cannot easily remember IP addresses to visit domain names (an example domain name being google.com), we need a phone book-like repository that can maintain all mappings of domain names to IP addresses. In this chapter, we’ll see how DNS serves as the Internet’s phone book.&lt;/p&gt;
&lt;h2&gt;What is DNS?&lt;/h2&gt;
&lt;p&gt;The domain name system (DNS) is the Internet’s naming service that maps human-friendly domain names to machine-readable IP addresses. The service of DNS is transparent to users. When a user enters a domain name in the browser, the browser has to translate the domain name to IP address by asking the DNS infrastructure. Once the desired IP address is obtained, the user’s request is forwarded to the destination web server.&lt;/p&gt;
&lt;p&gt;The entire operation is performed very quickly. Therefore, the end user experiences minimum delay. We’ll also see how browsers save some of the frequently used mappings for later use in the next lesson.&lt;/p&gt;
&lt;h4&gt;? Question ?&lt;/h4&gt;
&lt;p&gt;Imagine two users enter the same domain name at the exact same time. How does DNS ensure both reach the correct server?&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Solution:&lt;/strong&gt; DNS is a distributed system with multiple servers that resolve domain names independently. It ensures consistency by directing both users to the current IP address of the server, using caching, propagation, and authoritative DNS records. This way, even if two users enter the same domain name at the exact same time, they will both reach the correct server without any conflict.&lt;/p&gt;
&lt;h2&gt;Important details&lt;/h2&gt;
&lt;p&gt;Let’s highlight some of the important details about DNS, some of which we’ll cover in the next lesson:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Name servers: It’s important to understand that the DNS isn’t a single server. It’s a complete infrastructure with numerous servers. DNS servers that respond to users’ queries are called name servers.&lt;/li&gt;
&lt;li&gt;Resource records: The DNS database stores domain name to IP address mappings in the form of resource records (RR). The RR is the smallest unit of information that users request from the name servers. There are different types of RRs. The table below describes common RRs. The three important pieces of information are type, name, and value. The name and value change depending upon the type of the RR.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;Common Types of Resource Records&lt;/h3&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Type&lt;/th&gt;
&lt;th&gt;Description&lt;/th&gt;
&lt;th&gt;Name&lt;/th&gt;
&lt;th&gt;Value&lt;/th&gt;
&lt;th&gt;Example (Type, Name, Value)&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;A&lt;/td&gt;
&lt;td&gt;Provides the hostname to IP address mapping&lt;/td&gt;
&lt;td&gt;Hostname&lt;/td&gt;
&lt;td&gt;IP address&lt;/td&gt;
&lt;td&gt;(A, relay1.main.google.com, 104.18.2.119)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;NS&lt;/td&gt;
&lt;td&gt;Provides the hostname that is the authoritative DNS for a domain&lt;/td&gt;
&lt;td&gt;Domain name&lt;/td&gt;
&lt;td&gt;Hostname&lt;/td&gt;
&lt;td&gt;(NS, google.com, dns.google.com)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;CNAME&lt;/td&gt;
&lt;td&gt;Provides the mapping from alias to canonical hostname&lt;/td&gt;
&lt;td&gt;Hostname&lt;/td&gt;
&lt;td&gt;Canonical name&lt;/td&gt;
&lt;td&gt;(CNAME, google.com, server1.primary.google.com)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;MX&lt;/td&gt;
&lt;td&gt;Provides the mapping of mail server from alias to canonical host&lt;/td&gt;
&lt;td&gt;Hostname&lt;/td&gt;
&lt;td&gt;Canonical name&lt;/td&gt;
&lt;td&gt;(MX, mail.google.com, mailserver1.backup.google.com)&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;ul&gt;
&lt;li&gt;Caching: DNS uses caching at different layers to reduce request latency for the user. Caching plays an important role in reducing the burden on DNS infrastructure because it has to cater to the queries of the entire Internet.&lt;/li&gt;
&lt;li&gt;Hierarchy: DNS name servers are in a hierarchical form. The hierarchical structure allows DNS to be highly scalable because of its increasing size and query load. In the next lesson, we’ll look at how a tree-like structure is used to manage the entire DNS database.
Let's explore more details of the above points in the next lesson (&lt;em&gt;How the Domain Name System works&lt;/em&gt;) to get more clarity.&lt;/li&gt;
&lt;/ul&gt;</content><category term="Blog"/><category term="system-design"/><category term="dns"/></entry><entry><title>System Design: Lesson 2.2 - How the Domain Name System works</title><link href="https://dmb225.github.io/system-design-lesson-22-how-the-domain-name-system-works.html" rel="alternate"/><published>2026-02-03T00:00:00+01:00</published><updated>2026-02-03T00:00:00+01:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2026-02-03:/system-design-lesson-22-how-the-domain-name-system-works.html</id><summary type="html">&lt;p&gt;Through this lesson, we’ll answer the following questions:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;How is the DNS hierarchy formed using various types of DNS name servers?&lt;/li&gt;
&lt;li&gt;How is caching performed at different levels of the Internet to reduce the querying burden over the DNS infrastructure?&lt;/li&gt;
&lt;li&gt;How does the distributed nature of the DNS infrastructure …&lt;/li&gt;&lt;/ul&gt;</summary><content type="html">&lt;p&gt;Through this lesson, we’ll answer the following questions:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;How is the DNS hierarchy formed using various types of DNS name servers?&lt;/li&gt;
&lt;li&gt;How is caching performed at different levels of the Internet to reduce the querying burden over the DNS infrastructure?&lt;/li&gt;
&lt;li&gt;How does the distributed nature of the DNS infrastructure help its robustness?&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Let’s get started.&lt;/p&gt;
&lt;h2&gt;DNS hierarchy&lt;/h2&gt;
&lt;p&gt;As stated before, the DNS isn’t a single server that accepts requests and responds to user queries. It’s a complete infrastructure with name servers at different hierarchies.&lt;/p&gt;
&lt;p&gt;There are mainly four types of servers in the DNS hierarchy:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;DNS resolver: Resolvers initiate the querying sequence and forward requests to the other DNS name servers. Typically, DNS resolvers lie within the premise of the user’s network. However, DNS resolvers can also cater to users’ DNS queries through caching techniques, as we will see shortly. These servers can also be called local or default servers.&lt;/li&gt;
&lt;li&gt;Root-level name servers: These servers receive requests from local servers. Root name servers maintain name servers based on top-level domain names, such as .com, .edu, .us, and so on. For instance, when a user requests the IP address of google.com, root-level name servers will return a list of top-level domain (TLD) servers that hold the IP addresses of the .com domain.&lt;/li&gt;
&lt;li&gt;Top-level domain (TLD) name servers: These servers hold the IP addresses of authoritative name servers. The querying party will get a list of IP addresses that belong to the authoritative servers of the organization.&lt;/li&gt;
&lt;li&gt;Authoritative name servers: These are the organization’s DNS name servers that provide the IP addresses of the web or application servers.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;DNS names are processed from right to left, unlike UNIX file paths that go from left to right. For example, when resolving google.com, the resolver first looks up .com, then google. Visually, the DNS hierarchy forms a tree structure, starting from the root domain at the top and branching out to subdomains.&lt;/strong&gt;&lt;/p&gt;
&lt;h2&gt;Iterative versus recursive query resolution&lt;/h2&gt;
&lt;p&gt;There are two ways to perform a DNS query:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Iterative: The local server requests the root, TLD, and the authoritative servers for the IP address.&lt;/li&gt;
&lt;li&gt;Recursive: The end user requests the local server. The local server further requests the root DNS name servers. The root name servers forward the requests to other name servers.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;em&gt;Note: Typically, an iterative query is preferred to reduce query load on DNS infrastructure.&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;Fun fact: These days, we’ll find many third-party public DNS resolvers offered by Google, Cloudflare, OpenDNS, and many more. The interesting fact is that these public DNS servers may provide quicker responses than the local ISP DNS facilities.&lt;/p&gt;
&lt;h2&gt;Caching&lt;/h2&gt;
&lt;p&gt;Caching refers to the temporary storage of frequently requested resource records. A record is a data unit within the DNS database that shows a name-to-value binding. Caching reduces response time to the user and decreases network traffic. When we use caching at different hierarchies, it can reduce a lot of querying burden on the DNS infrastructure. Caching can be implemented in the browser, operating systems, local name server within the user’s network, or the ISP’s DNS resolvers.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Note: Even if there is no cache available to resolve a user’s query and it’s imperative to visit the DNS infrastructure, caching can still be beneficial. The local server or ISP DNS resolver can cache the IP addresses of TLD servers or authoritative servers and avoid requesting the root-level server.&lt;/em&gt;&lt;/p&gt;
&lt;h4&gt;Why does DNS sacrifice strong consistency to achieve high performance and scalability?&lt;/h4&gt;
&lt;p&gt;&lt;strong&gt;Solution:&lt;/strong&gt; DNS sacrifices strong consistency to achieve high performance and scalability because it handles a vast number of read operations compared to write operations. By propagating updates lazily, DNS ensures quick responses to queries without overwhelming its infrastructure.&lt;/p&gt;
&lt;h2&gt;DNS as a distributed system&lt;/h2&gt;
&lt;p&gt;Although the DNS hierarchy facilitates the distributed Internet that we know today, it’s a distributed system itself. The distributed nature of DNS has the following advantages:&lt;/p&gt;
&lt;p&gt;It avoids becoming a single point of failure (SPOF).
It achieves low query latency so users can get responses from nearby servers.
It gets a higher degree of flexibility during maintenance and updates or upgrades. For example, if one DNS server is down or overburdened, another DNS server can respond to user queries.
There are 13 logical root name servers (named letter A through M) with many instances spread throughout the globe. These servers are managed by 12 different organizations.&lt;/p&gt;
&lt;p&gt;Let’s now go over how DNS is scalable, reliable, and consistent.&lt;/p&gt;
&lt;h3&gt;Highly scalable&lt;/h3&gt;
&lt;p&gt;Due to its hierarchical nature, DNS is a highly scalable system. Roughly 1,000 replicated instances of 13 root-level servers are spread throughout the world strategically to handle user queries. The working labor is divided among TLD and root servers to handle a query and, finally, the authoritative servers that are managed by the organizations themselves to make the entire system work. As shown in the DNS hierarchy tree above, different services handle different portions of the tree enabling scalability and manageability of the system.&lt;/p&gt;
&lt;h3&gt;Reliable&lt;/h3&gt;
&lt;p&gt;Three main reasons make the DNS a reliable system:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Caching: The caching is done in the browser, the operating system, and the local name server, and the ISP DNS resolvers also maintain a rich cache of frequently visited services. Even if some DNS servers are temporarily down, cached records can be served to make DNS a reliable system.&lt;/li&gt;
&lt;li&gt;Server replication: DNS has replicated copies of each logical server spread systematically across the globe to entertain user requests at low latency. The redundant servers improve the reliability of the overall system.&lt;/li&gt;
&lt;li&gt;Protocol: Although many clients rely on the unreliable User Datagram Protocol (UDP) to request and receive DNS responses, it’s important to acknowledge that UDP also offers distinct advantages. It is much faster, and therefore, improves DNS performance. Furthermore, internet service reliability has improved since its inception, so UDP is usually favored over TCP. DNS queries are usually retransmitted at the transport layer if there’s no response for the previous one. Therefore, request-response might need additional round trips, which provides a shorter delay as compared to TCP, which needs a three-way handshake every time before data exchange.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;em&gt;Note: DNS usually relies on UDP for quick, lightweight queries. However, when networks are congested or responses exceed 512 bytes, DNS switches to TCP for reliable delivery. TCP is also used for zone transfers, where consistent and complete data exchange between DNS servers is required. Modern clients may use DNS over HTTPS (DoH) or DNS over TLS (DoT) for added security and privacy.&lt;/em&gt;&lt;/p&gt;
&lt;h3&gt;Consistent&lt;/h3&gt;
&lt;p&gt;DNS uses various protocols to update and transfer information among replicated servers in a hierarchy. DNS compromises on strong consistency to achieve high performance because data is read frequently from DNS databases as compared to writing. However, DNS provides eventual consistency and updates records on replicated servers lazily. Typically, it can take from a few seconds up to three days to update records on the DNS servers across the Internet. The time it takes to propagate information among different DNS clusters depends on the DNS infrastructure, the size of the update, and which part of the DNS tree is being updated.&lt;/p&gt;
&lt;p&gt;Consistency can suffer because of caching too. Since authoritative servers are located within the organization, it may be possible that certain resource records are updated on the authoritative servers in case of server failures at the organization. Therefore, cached records at the default/local and ISP servers may be outdated. To mitigate this issue, each cached record comes with an expiration time called time-to-live (TTL).&lt;/p&gt;
&lt;p&gt;&lt;em&gt;To maintain high availability, the TTL value should be small. This is because if any server or cluster fails, the organization can update the resource records right away. Users will experience non-availability only for the time the TTL has not expired. However, if the TTL is large, the organization will update its resource records, whereas users will keep pinging the outdated server that would have crashed long ago. Companies that long for high availability maintain a TTL value as low as 120 seconds. Therefore, even in the case of a failure, the maximum downtime is a few minutes.&lt;/em&gt;&lt;/p&gt;
&lt;h3&gt;Aout some commands&lt;/h3&gt;
&lt;h4&gt;nslookup&lt;/h4&gt;
&lt;p&gt;The Non-authoritative answer, as the name suggests, is the answer provided by a server that is not the authoritative server of Google. It isn’t in the list of authoritative nameservers that Google maintains. So, where does the answer come from? The answer is provided by second, third, and fourth-hand name servers configured to reply to our DNS query—for example, our university or office DNS resolver, our ISP nameserver, our ISP’s ISP nameserver, and so on. In short, it can be considered as a cached version of Google’s authoritative nameservers response. If we try multiple domain names, we’ll realize that we receive a cached response most of the time.
If we run the same command multiple times, we’ll receive the same IP addresses list but in a different order each time. The reason for that is DNS is indirectly performing load balancing. It’s an important term that we’ll gain familiarity with in the coming lessons.&lt;/p&gt;
&lt;h4&gt;dig&lt;/h4&gt;
&lt;p&gt;The Query time: 4 msec represents the time it takes to get a response from the DNS server. For various reasons, these numbers may be different in our case.
The 300 value in the ANSWER SECTION represents the number of seconds the cache is maintained in the DNS resolver. This means that Google’s ADNS keeps a TTL value of five minutes.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Question&lt;/strong&gt;
DNS caching improves performance but introduces the risk of stale data. Suppose an organization updates its website’s IP address, but many users still access the old IP due to caching. Propose a strategy to minimize disruption in such scenarios.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Solution:&lt;/strong&gt; To minimize disruption when updating DNS records, organizations can temporarily lower the TTL (Time To Live) value before making changes, ensuring clients refresh their cached data more frequently. Additionally, implementing a phased rollout across authoritative DNS servers helps control the update process smoothly. Using DNS-based traffic management can redirect users still accessing the old IP to a temporary server with a message or alternative service. Lastly, notifying users in advance about upcoming changes helps set expectations and reduces confusion.&lt;/p&gt;</content><category term="Blog"/><category term="system-design"/><category term="dns"/></entry><entry><title>System Design: Lesson 3.1 - Introduction to Load Balancers</title><link href="https://dmb225.github.io/system-design-lesson-31-introduction-to-load-balancers.html" rel="alternate"/><published>2026-02-03T00:00:00+01:00</published><updated>2026-02-03T00:00:00+01:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2026-02-03:/system-design-lesson-31-introduction-to-load-balancers.html</id><summary type="html">&lt;h2&gt;What is load balancing?&lt;/h2&gt;
&lt;p&gt;Millions of requests could arrive per second in a typical data center. To serve these requests, thousands (or a hundred thousand) servers work together to share the load of incoming requests.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Note: Here, it’s important that we consider how the incoming requests will be divided …&lt;/em&gt;&lt;/p&gt;</summary><content type="html">&lt;h2&gt;What is load balancing?&lt;/h2&gt;
&lt;p&gt;Millions of requests could arrive per second in a typical data center. To serve these requests, thousands (or a hundred thousand) servers work together to share the load of incoming requests.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Note: Here, it’s important that we consider how the incoming requests will be divided among all the available servers.
A load balancer (LB) is the answer to the question. The job of the load balancer is to fairly divide all clients’ requests among the pool of available servers. Load balancers perform this job to avoid overloading or crashing servers.&lt;/em&gt;&lt;/p&gt;
&lt;h2&gt;Why do we need load balancers?&lt;/h2&gt;
&lt;p&gt;The load balancing layer is the first point of contact within a data center after the firewall. A load balancer may not be required if a service entertains a few hundred or even a few thousand requests per second. However, for increasing client requests, load balancers provide the following capabilities:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Scalability: By adding servers, the capacity of the application/service can be increased seamlessly. Load balancers make such upscaling or downscaling transparent to the end users.&lt;/li&gt;
&lt;li&gt;Availability: Even if some servers go down or suffer a fault, the system still remains available. One of the jobs of the load balancers is to hide faults and failures of servers.
Performance: Load balancers can forward requests to servers with a lesser load so the user can get a quicker response time. This not only improves performance but also improves resource utilization.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The requests received by a load balancer are distributed among multiple servers using a configured algorithm that we’ll discuss in the upcoming lessons:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Round-robin&lt;/li&gt;
&lt;li&gt;Weighted round-robin&lt;/li&gt;
&lt;li&gt;Least response time&lt;/li&gt;
&lt;li&gt;Least connections&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Load balancers ensure reliability and availability by continuously monitoring server health and directing traffic only to servers capable of responding efficiently. We’ll explore these algorithms and their impact on system performance in more detail in the &lt;em&gt;Advanced Details of Load Balancers&lt;/em&gt; lesson.&lt;/p&gt;
&lt;h2&gt;Placing load balancers&lt;/h2&gt;
&lt;p&gt;Generally, LBs sit between clients and servers. Requests go through to servers and back to clients via the load-balancing layer. However, that isn’t the only point where load balancers are used.&lt;/p&gt;
&lt;p&gt;Let’s consider the three well-known groups of servers. That is the web, the application, and the database servers. To divide the traffic load among the available servers, load balancers can be used between the server instances of these three services in the following way:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Place LBs between end users of the application and web servers/application gateway.&lt;/li&gt;
&lt;li&gt;Place LBs between the web servers and application servers that run the business/application logic.&lt;/li&gt;
&lt;li&gt;Place LBs between the application servers and database servers.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;In reality, load balancers can potentially be used between any two services with multiple instances within a system’s design.&lt;/p&gt;
&lt;p&gt;Services offered by load balancers
LBs not only enable services to be scalable, available, and highly performant, but they also offer some key services like the following:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Health checking: LBs use the heartbeat protocol to monitor the health and, therefore, reliability of end-servers. Another advantage of health checking is the improved user experience.&lt;/li&gt;
&lt;li&gt;TLS termination: LBs reduce the burden on end-servers by handling TLS termination with the client.&lt;/li&gt;
&lt;li&gt;Predictive analytics: LBs can predict traffic patterns through analytics performed over traffic passing through them or using statistics of traffic obtained over time.&lt;/li&gt;
&lt;li&gt;Reduced human intervention: Because of LB automation, reduced system administration efforts are required in handling failures.&lt;/li&gt;
&lt;li&gt;Service discovery: An advantage of LBs is that the clients’ requests are forwarded to appropriate hosting servers by inquiring about the service registry.&lt;/li&gt;
&lt;li&gt;Security: LBs may also improve security by mitigating attacks like denial-of-service (DoS) at different layers of the OSI model (layers 3, 4, and 7).&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;As a whole, load balancers provide flexibility, reliability, redundancy, and efficiency to the overall design of the system.&lt;/p&gt;
&lt;h4&gt;Avoiding single point of failure (SPOF)&lt;/h4&gt;
&lt;p&gt;To prevent load balancers from becoming a single point of failure, organizations deploy them in pairs or clusters. These clusters use heartbeat communication to monitor health. When the primary load balancer fails, a backup automatically takes over to keep the service running. In rare cases where the entire cluster fails, engineers can manually reroute traffic to restore availability.&lt;/p&gt;
&lt;p&gt;In the coming lessons, we’ll see how load balancers can be used in complex applications and which type of load balancer is appropriate for which use case.&lt;/p&gt;</content><category term="Blog"/><category term="system-design"/><category term="load-balancers"/></entry><entry><title>Algorithm patterns: Fast and Slow Pointers</title><link href="https://dmb225.github.io/algorithm-patterns-fast-and-slow-pointers.html" rel="alternate"/><published>2025-08-29T00:00:00+02:00</published><updated>2025-08-29T00:00:00+02:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2025-08-29:/algorithm-patterns-fast-and-slow-pointers.html</id><summary type="html">&lt;p&gt;The &lt;strong&gt;fast and slow pointers&lt;/strong&gt; pattern is a versatile and efficient technique for traversing linear data structures, such as arrays, linked lists, or even graphs, using two pointers that move at different speeds. This approach is often used to identify cycles or find a specific target. The speeds of the …&lt;/p&gt;</summary><content type="html">&lt;p&gt;The &lt;strong&gt;fast and slow pointers&lt;/strong&gt; pattern is a versatile and efficient technique for traversing linear data structures, such as arrays, linked lists, or even graphs, using two pointers that move at different speeds. This approach is often used to identify cycles or find a specific target. The speeds of the pointers can be adjusted according to the problem statement. The two pointers pattern focuses on comparing data values, whereas the fast and slow pointers method is typically used to analyze the structure or properties of the data.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;About the pattern&lt;/h2&gt;
&lt;p&gt;Similar to the two pointers pattern, the fast and slow pointers pattern uses two pointers to traverse an iterable data structure, but at different speeds, often to identify cycles or find a specific target. The speeds of the pointers can be adjusted according to the problem statement. The two pointers pattern focuses on comparing data values, whereas the fast and slow pointers method is typically used to analyze the structure or properties of the data.&lt;/p&gt;
&lt;p&gt;The key idea is that the pointers start at the same location and then start moving at different speeds. The slow pointer moves one step at a time, while the fast pointer moves by two steps. Due to the different speeds of the pointers, this pattern is also commonly known as the Hare and Tortoise algorithm, where the Hare is the fast pointer while Tortoise is the slow pointer. If a cycle exists, the two pointers will eventually meet during traversal. This approach enables the algorithm to detect specific properties within the data structure, such as cycles, midpoints, or intersections.&lt;/p&gt;
&lt;p&gt;To visualize this, imagine two runners on a circular track starting at the same point. With different running speeds, the faster runner will eventually overtake the slower one, illustrating how this method works to detect cycles.&lt;/p&gt;
&lt;p&gt;Here’s a basic pseudocode template that you can adapt to your specific needs:&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;pre&gt;&lt;span&gt;&lt;/span&gt;&lt;code&gt;&lt;span class="k"&gt;FUNCTION&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;fastAndSlow&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;dataStructure&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;&lt;span class="err"&gt;:&lt;/span&gt;
&lt;span class="w"&gt;  &lt;/span&gt;&lt;span class="err"&gt;#&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;initialize&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;pointers&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="ow"&gt;or&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;indices&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;
&lt;span class="w"&gt;  &lt;/span&gt;&lt;span class="n"&gt;fastPointer&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;dataStructure&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="k"&gt;start&lt;/span&gt;&lt;span class="w"&gt;   &lt;/span&gt;&lt;span class="err"&gt;#&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="ow"&gt;or&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="mi"&gt;0&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;if&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;the&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;data&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;structure&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;is&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;an&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;array&lt;/span&gt;
&lt;span class="w"&gt;  &lt;/span&gt;&lt;span class="n"&gt;slowPointer&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;dataStructure&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="k"&gt;start&lt;/span&gt;&lt;span class="w"&gt;   &lt;/span&gt;&lt;span class="err"&gt;#&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="ow"&gt;or&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="mi"&gt;0&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;if&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;the&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;data&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;structure&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;is&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;an&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;array&lt;/span&gt;

&lt;span class="w"&gt;  &lt;/span&gt;&lt;span class="k"&gt;WHILE&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;fastPointer&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="o"&gt;!=&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;null&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="ow"&gt;AND&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;fastPointer&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="k"&gt;next&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="o"&gt;!=&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;null&lt;/span&gt;&lt;span class="err"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;
&lt;span class="w"&gt;    &lt;/span&gt;&lt;span class="err"&gt;#&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;For&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nl"&gt;arrays&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;WHILE&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;fastPointer&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="o"&gt;&amp;lt;&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;dataStructure&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="n"&gt;length&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="ow"&gt;AND&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;fastPointer&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="o"&gt;+&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="mi"&gt;1&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="o"&gt;&amp;lt;&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;dataStructure&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="nl"&gt;length&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;

&lt;span class="w"&gt;    &lt;/span&gt;&lt;span class="n"&gt;slowPointer&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;slowPointer&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="k"&gt;next&lt;/span&gt;&lt;span class="w"&gt;            &lt;/span&gt;
&lt;span class="w"&gt;    &lt;/span&gt;&lt;span class="err"&gt;#&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;For&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nl"&gt;arrays&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;slowPointer&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;slowPointer&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="o"&gt;+&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="mi"&gt;1&lt;/span&gt;

&lt;span class="w"&gt;    &lt;/span&gt;&lt;span class="n"&gt;fastPointer&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;fastPointer&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="k"&gt;next&lt;/span&gt;&lt;span class="p"&gt;.&lt;/span&gt;&lt;span class="k"&gt;next&lt;/span&gt;&lt;span class="w"&gt;       &lt;/span&gt;
&lt;span class="w"&gt;    &lt;/span&gt;&lt;span class="err"&gt;#&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;For&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nl"&gt;arrays&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;fastPointer&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;fastPointer&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="o"&gt;+&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="mi"&gt;2&lt;/span&gt;

&lt;span class="w"&gt;    &lt;/span&gt;&lt;span class="k"&gt;IF&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;fastPointer&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="o"&gt;!=&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;null&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="ow"&gt;AND&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;someCondition&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;fastPointer&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;slowPointer&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;&lt;span class="err"&gt;:&lt;/span&gt;
&lt;span class="w"&gt;      &lt;/span&gt;&lt;span class="err"&gt;#&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;For&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nl"&gt;arrays&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;use&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;someCondition&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;dataStructure&lt;/span&gt;&lt;span class="o"&gt;[&lt;/span&gt;&lt;span class="n"&gt;fastPointer&lt;/span&gt;&lt;span class="o"&gt;]&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;dataStructure&lt;/span&gt;&lt;span class="o"&gt;[&lt;/span&gt;&lt;span class="n"&gt;slowPointer&lt;/span&gt;&lt;span class="o"&gt;]&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;if&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;needed&lt;/span&gt;
&lt;span class="w"&gt;      &lt;/span&gt;&lt;span class="n"&gt;handleCondition&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;fastPointer&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;slowPointer&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;
&lt;span class="w"&gt;      &lt;/span&gt;&lt;span class="k"&gt;BREAK&lt;/span&gt;

&lt;span class="w"&gt;  &lt;/span&gt;&lt;span class="err"&gt;#&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;process&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;the&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;result&lt;/span&gt;
&lt;span class="w"&gt;  &lt;/span&gt;&lt;span class="n"&gt;processResult&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;slowPointer&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;
&lt;span class="w"&gt;  &lt;/span&gt;&lt;span class="err"&gt;#&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;For&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nl"&gt;arrays&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;processResult&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;slowPointer&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;might&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;process&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;dataStructure&lt;/span&gt;&lt;span class="o"&gt;[&lt;/span&gt;&lt;span class="n"&gt;slowPointer&lt;/span&gt;&lt;span class="o"&gt;]&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;In the template above, the fastPointer moves at twice the speed of the slowPointer, allowing efficient traversal and detection of specific conditions. &lt;/p&gt;
&lt;p&gt;The fastPointer advances two steps at a time (fastPointer.next.next for linked lists or fastPointer + 2 for arrays), while the slowPointer moves one step at a time (slowPointer.next or slowPointer + 1).&lt;/p&gt;
&lt;p&gt;The loop continues until the fastPointer reaches the end of the structure or a condition (someCondition) is met, triggering handleCondition.&lt;/p&gt;
&lt;p&gt;Finally, the result is processed using the slowPointer, which often points to a meaningful position, such as the middle of the structure or the start of a cycle.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;Examples&lt;/h2&gt;
&lt;p&gt;The following examples illustrate some problems that can be solved with this approach:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Middle of the linked list:&lt;/strong&gt; Given the head of a singly linked list, return the middle node of the linked list.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The idea is to iterate the linked list using fast and slow pointers with slow pointer moving one step whereas the fast pointer moving two steps. When the fast pointer reaches the last node or becomes NULL, the slow pointer will be pointing to the middle of the linked list.&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Detect cycle in an array:&lt;/strong&gt; Given an array of non-negative integers where elements are indexes pointing to the next element, determine if there is a cycle in the array.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;To detect a cycle in the given array, we'll use the fast and slow pointers. The slow pointer moves forward one step at a time, while the fast pointer moves two steps forward. If there is a cycle, the fast pointer will eventually meet the slow pointer.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;Real-world problems&lt;/h2&gt;
&lt;p&gt;Many problems in the real world use the fast and slow pointers pattern. Let’s look at some examples.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Symlink verification:&lt;/strong&gt; Imagine you’re an IT administrator responsible for maintaining a large server. In one of the directories, multiple files and symbolic links (symlinks) are scattered around, each pointing to various files or even to other symlinks. Occasionally, a well-intentioned script or misconfiguration might create a loop—say, a symlink A points to B, and somehow B eventually links back to A. This creates a cycle where following those symlinks never ends. In a symlink verification utility, the fast and slow pointer approach helps detect loops where symlinks endlessly reference each other. One pointer (the “tortoise”) follows links one step at a time, while the other (the “hare”) jumps two steps. If both ever land on the same file or link again, it confirms a cycle.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Compiling an object-oriented program:&lt;/strong&gt; During compilation, each object-oriented module may depend on others, sometimes creating a loop (A depends on B, and B depends on A). With fast and slow pointers, you track dependencies step by step: the “tortoise” moves one module at a time, and the “hare” jumps two modules at a time. If they meet, there’s a cyclic dependency. This quick, space-efficient check helps catch and break these loops, ensuring a clean compilation sequence.&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h2&gt;Does your problem match this pattern?&lt;/h2&gt;
&lt;p&gt;Yes, if the following condition is fulfilled:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Linear data structure:&lt;/strong&gt; The input data can be traversed in a linear fashion, such as an array, linked list, or string.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;In addition, if either of these conditions is fulfilled:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Cycle or intersection detection:&lt;/strong&gt; The problem involves detecting a loop within a linked list or an array or involves finding an intersection between two linked lists or arrays.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Find the starting element of the second quantile:&lt;/strong&gt; This means identifying the element where the second part of a divided dataset begins—like the second half, second third (tertile), or second quarter (quartile). For example, the task might ask you to find the middle element of an array or a linked list, which marks the start.&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;</content><category term="Blog"/><category term="algorithms"/></entry><entry><title>Algorithm Patterns: Sliding Window</title><link href="https://dmb225.github.io/algorithm-patterns-sliding-window.html" rel="alternate"/><published>2025-08-29T00:00:00+02:00</published><updated>2025-08-29T00:00:00+02:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2025-08-29:/algorithm-patterns-sliding-window.html</id><summary type="html">&lt;p&gt;The &lt;strong&gt;sliding window&lt;/strong&gt; pattern is a technique for efficiently processing sequential data, such as arrays and strings. It involves maintaining a dynamic window that slides across the data, allowing you to examine a fixed portion at a time. Instead of repeatedly scanning the entire dataset, this approach adjusts the window …&lt;/p&gt;</summary><content type="html">&lt;p&gt;The &lt;strong&gt;sliding window&lt;/strong&gt; pattern is a technique for efficiently processing sequential data, such as arrays and strings. It involves maintaining a dynamic window that slides across the data, allowing you to examine a fixed portion at a time. Instead of repeatedly scanning the entire dataset, this approach adjusts the window’s boundaries as needed to track relevant elements. It is especially useful for solving problems involving contiguous subarrays or substrings, such as finding the maximum sum of a subarray or detecting repeated patterns in a string. This pattern can be viewed as a variation of the two-pointer approach, where the pointers define the window’s start and end.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;About the pattern&lt;/h2&gt;
&lt;p&gt;Imagine you have a tray of 10 cookies and want to find the most chocolate chips in any 3 cookies next to each other. Using a naive approach, you’d stand at each cookie and count its chocolate chips along with those of its immediate left and right neighbors to form every possible group of 3. This means repeating the counting process for each cookie, which quickly becomes inefficient as the number of cookies grows.&lt;/p&gt;
&lt;p&gt;We can avoid this hassle by using a smarter approach. Instead of recounting the chips for each group from scratch, you start by counting the chips in the first 3 cookies. Then, as you move to the next group, you simply subtract the chips from the cookie you leave behind and add the chips from the new cookie you include. &lt;/p&gt;
&lt;p&gt;Consider the following steps:&lt;/p&gt;
&lt;p&gt;Step 1: Count the chips in the first three cookies. This is your starting total—and your initial “best so far.”&lt;/p&gt;
&lt;p&gt;Step 2: Slide the window one cookie to the right:
- Subtract the chips from the cookie that just slipped out of the window.
- Add the chips from the fresh cookie that slid into view.&lt;/p&gt;
&lt;p&gt;Step 3: If this new sum tops your current record, replace it with the initial “best so far”.&lt;/p&gt;
&lt;p&gt;Step 4: Repeat the above steps for each group of three cookies, all the way to the last one.&lt;/p&gt;
&lt;p&gt;By updating the total in constant time with each slide, you find the group of neighboring cookies with maximum chocolate chips without ever recounting the entire window.&lt;/p&gt;
&lt;p&gt;The sliding window can be of a fixed size or dynamic.
- The fixed-size window is used when the window size is given or constant. For example, find the largest sum of any three consecutive numbers.
- The dynamic window is used when the window size changes based on conditions. For example, find the smallest subarray whose sum exceeds a target.&lt;/p&gt;
&lt;p&gt;How is the sliding window implemented?&lt;br&gt;
The sliding window technique uses two pointers representing the window’s boundaries. These pointers move through an array or a string to examine a portion of the data at a time. The window is then updated by adding new elements and removing old ones as the pointers move.&lt;/p&gt;
&lt;p&gt;In a fixed-size window, both pointers move together, keeping the window size constant. For example, if you need to find the largest sum of three consecutive numbers, the window size stays at three while sliding across the array.&lt;/p&gt;
&lt;p&gt;In a dynamic window, the window size can grow or shrink based on a condition. One pointer (usually the right one) expands the window, while the other (left) contracts it when the condition is met, like finding the smallest subarray with a sum greater than a target. This two pointer strategy allows the sliding window to process data in linear time, making it a powerful tool for problems involving continuous data sections.&lt;/p&gt;
&lt;p&gt;Here is the template for the sliding window, where we maintain a dynamic window that adjusts as we traverse the array.&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;pre&gt;&lt;span&gt;&lt;/span&gt;&lt;code&gt;&lt;span class="k"&gt;FUNCTION&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;slidingWindow&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;arr&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;k&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;processWindow&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;&lt;span class="err"&gt;:&lt;/span&gt;
&lt;span class="w"&gt;  &lt;/span&gt;&lt;span class="err"&gt;#&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;Initialize&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;the&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;window&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;result&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="nf"&gt;sum&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;product&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="nf"&gt;count&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;etc&lt;/span&gt;&lt;span class="p"&gt;.)&lt;/span&gt;
&lt;span class="w"&gt;  &lt;/span&gt;&lt;span class="n"&gt;windowResult&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="o"&gt;=&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;INITIAL_VALUE&lt;/span&gt;

&lt;span class="w"&gt;  &lt;/span&gt;&lt;span class="err"&gt;#&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;Compute&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;the&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;initial&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;window&lt;/span&gt;&lt;span class="err"&gt;&amp;#39;&lt;/span&gt;&lt;span class="n"&gt;s&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;result&lt;/span&gt;
&lt;span class="w"&gt;  &lt;/span&gt;&lt;span class="k"&gt;FOR&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;i&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;FROM&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="mi"&gt;0&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;TO&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;k&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="o"&gt;-&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="mi"&gt;1&lt;/span&gt;&lt;span class="err"&gt;:&lt;/span&gt;
&lt;span class="w"&gt;    &lt;/span&gt;&lt;span class="k"&gt;UPDATE&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;windowResult&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;WITH&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;arr&lt;/span&gt;&lt;span class="o"&gt;[&lt;/span&gt;&lt;span class="n"&gt;i&lt;/span&gt;&lt;span class="o"&gt;]&lt;/span&gt;

&lt;span class="w"&gt;  &lt;/span&gt;&lt;span class="err"&gt;#&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;Process&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;the&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;first&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;window&lt;/span&gt;
&lt;span class="w"&gt;  &lt;/span&gt;&lt;span class="n"&gt;processWindow&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;windowResult&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;

&lt;span class="w"&gt;  &lt;/span&gt;&lt;span class="err"&gt;#&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;Slide&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;the&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;window&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;across&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;the&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;array&lt;/span&gt;
&lt;span class="w"&gt;  &lt;/span&gt;&lt;span class="k"&gt;FOR&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;i&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;FROM&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;k&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;TO&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;length&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;of&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;arr&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="o"&gt;-&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="mi"&gt;1&lt;/span&gt;&lt;span class="err"&gt;:&lt;/span&gt;
&lt;span class="w"&gt;    &lt;/span&gt;&lt;span class="k"&gt;UPDATE&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;windowResult&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;BY&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;ADDING&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;arr&lt;/span&gt;&lt;span class="o"&gt;[&lt;/span&gt;&lt;span class="n"&gt;i&lt;/span&gt;&lt;span class="o"&gt;]&lt;/span&gt;&lt;span class="w"&gt;  &lt;/span&gt;&lt;span class="err"&gt;#&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;Add&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;a&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;new&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;element&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;to&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;the&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;window&lt;/span&gt;
&lt;span class="w"&gt;    &lt;/span&gt;&lt;span class="k"&gt;UPDATE&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;windowResult&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;BY&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;REMOVING&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;arr&lt;/span&gt;&lt;span class="o"&gt;[&lt;/span&gt;&lt;span class="n"&gt;i - k&lt;/span&gt;&lt;span class="o"&gt;]&lt;/span&gt;&lt;span class="w"&gt;  &lt;/span&gt;&lt;span class="err"&gt;#&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;Remove&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;outgoing&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;element&lt;/span&gt;
&lt;span class="w"&gt;    &lt;/span&gt;&lt;span class="n"&gt;processWindow&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;windowResult&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;&lt;span class="w"&gt;  &lt;/span&gt;&lt;span class="err"&gt;#&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;Operation&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;on&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;the&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="n"&gt;updated&lt;/span&gt;&lt;span class="w"&gt; &lt;/span&gt;&lt;span class="k"&gt;window&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;We start by computing the result for the first k elements and storing it in windowResult. This represents the initial window in the array. Next, we slide the window across arr by moving one step at a time. For each step:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Process the current window’s result using the function processWindow(windowResult). This step can involve updating a maximum, computing an average, counting occurrences, or any other required calculation.&lt;/li&gt;
&lt;li&gt;Add the new incoming element (arr[i]) to the window, updating windowResult accordingly.&lt;/li&gt;
&lt;li&gt;Remove the outgoing element (arr[i—k]) from the window to maintain the correct size.&lt;/li&gt;
&lt;li&gt;Repeat the process until the entire array is covered, ensuring we efficiently compute the desired result for every possible k-sized subarray.&lt;/li&gt;
&lt;/ol&gt;
&lt;hr&gt;
&lt;h2&gt;How does the sliding window technique reduce time complexity?&lt;/h2&gt;
&lt;p&gt;We have learned that the sliding window helps process sequential data like arrays or strings efficiently. Let’s quickly see why that is the case.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Avoids nested loops: Without a sliding window, many problems require checking all subarrays using two or more loops, leading to O(n2) time complexity or more. The sliding window allows us to update the window by adjusting pointers, reducing the complexity of traversing and processing the entire array to linear, O(n).&lt;/li&gt;
&lt;li&gt;Reuses computation: Instead of recalculating values for each window from scratch, the sliding window approach reuses previous calculations by adding new elements and removing old ones. In the example we discussed above, where we find the most chocolate chips in any 3 cookies, we only update the sum by adding the next cookie and removing the leftmost one.&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h2&gt;Examples&lt;/h2&gt;
&lt;p&gt;The following examples illustrate some problems that can be solved with this approach:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Maximum sum subarray of size K: Given an array of integers and a positive integer k, find the maximum sum of any contiguous subarray of size k.&lt;/li&gt;
&lt;li&gt;Longest substring without repeating characters: Given a string, find the length of the longest substring without repeating characters.&lt;/li&gt;
&lt;/ol&gt;
&lt;hr&gt;
&lt;h2&gt;Real-world problems&lt;/h2&gt;
&lt;p&gt;Many problems in the real world use the sliding window pattern. Let’s look at some examples.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Telecommunications: Find the maximum number of users connected to a cellular network’s base station in every k-millisecond sliding window.&lt;/li&gt;
&lt;li&gt;Video streaming: Given a stream of numbers representing the number of buffering events in a given user session, calculate the median number of buffering events in each one-minute interval.&lt;/li&gt;
&lt;li&gt;Social media content mining: Given the lists of topics that two users have posted about, find the shortest sequence of posts by one user that includes all the topics that the other user has posted about.&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h2&gt;Does your problem match this pattern?&lt;/h2&gt;
&lt;p&gt;Yes, if all of these conditions are fulfilled:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Contiguous data: The input data is stored in a contiguous manner, such as an array or string.&lt;/li&gt;
&lt;li&gt;Processing subsets of elements: The problem requires repeated computations on a contiguous subset of data elements (a subarray or a substring), such that the window moves across the input array from one end to the other. The size of the window may be fixed or variable, depending on the requirements of the problem. &lt;/li&gt;
&lt;li&gt;Efficient computation time complexity: The computations performed every time the window moves take constant or very small time.&lt;/li&gt;
&lt;/ul&gt;</content><category term="Blog"/><category term="algorithms"/></entry><entry><title>Algorithm Patterns: Two Pointers</title><link href="https://dmb225.github.io/algorithm-patterns-two-pointers.html" rel="alternate"/><published>2025-08-28T00:00:00+02:00</published><updated>2025-08-28T00:00:00+02:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2025-08-28:/algorithm-patterns-two-pointers.html</id><summary type="html">&lt;p&gt;The &lt;strong&gt;two pointers&lt;/strong&gt; pattern is a versatile and efficient technique widely used in algorithmic problem-solving, especially when working with sequential data structures like arrays, strings, or linked lists. As the name suggests, this approach involves maintaining two pointers (or indices) that traverse the data structure in a coordinated manner—often …&lt;/p&gt;</summary><content type="html">&lt;p&gt;The &lt;strong&gt;two pointers&lt;/strong&gt; pattern is a versatile and efficient technique widely used in algorithmic problem-solving, especially when working with sequential data structures like arrays, strings, or linked lists. As the name suggests, this approach involves maintaining two pointers (or indices) that traverse the data structure in a coordinated manner—often starting from different positions or moving in opposite directions. By dynamically adjusting these pointers based on specific conditions, we can efficiently explore the data and solve problems with optimal time and space complexity.&lt;/p&gt;
&lt;p&gt;Whenever you need to find two elements in a sequence that satisfy a certain condition, the two pointers pattern should be one of the first strategies you consider.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;About the pattern&lt;/h2&gt;
&lt;p&gt;The two pointers technique is particularly useful when:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;You need to process or compare elements at different positions in a linear data structure.&lt;/li&gt;
&lt;li&gt;The problem involves searching for pairs or subarrays that meet certain criteria.&lt;/li&gt;
&lt;li&gt;You want to optimize for time and space, often reducing the need for nested loops.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The pointers can move in the same direction, in opposite directions, or even traverse two different data structures, depending on the problem.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;Example: Checking for palindromes&lt;/h2&gt;
&lt;p&gt;A classic example is checking if a string is a palindrome (reads the same forwards and backwards). Here, one pointer starts at the beginning and the other at the end, moving towards each other while comparing characters.&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;pre&gt;&lt;span&gt;&lt;/span&gt;&lt;code&gt;&lt;span class="k"&gt;def&lt;/span&gt; &lt;span class="nf"&gt;is_palindrome&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;s&lt;/span&gt;&lt;span class="p"&gt;):&lt;/span&gt;
    &lt;span class="n"&gt;left&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt; &lt;span class="n"&gt;right&lt;/span&gt; &lt;span class="o"&gt;=&lt;/span&gt; &lt;span class="mi"&gt;0&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt; &lt;span class="nb"&gt;len&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;s&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt; &lt;span class="o"&gt;-&lt;/span&gt; &lt;span class="mi"&gt;1&lt;/span&gt;

    &lt;span class="k"&gt;while&lt;/span&gt; &lt;span class="n"&gt;left&lt;/span&gt; &lt;span class="o"&gt;&amp;lt;&lt;/span&gt; &lt;span class="n"&gt;right&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;
        &lt;span class="k"&gt;while&lt;/span&gt; &lt;span class="n"&gt;left&lt;/span&gt; &lt;span class="o"&gt;&amp;lt;&lt;/span&gt; &lt;span class="n"&gt;right&lt;/span&gt; &lt;span class="ow"&gt;and&lt;/span&gt; &lt;span class="ow"&gt;not&lt;/span&gt; &lt;span class="n"&gt;s&lt;/span&gt;&lt;span class="p"&gt;[&lt;/span&gt;&lt;span class="n"&gt;left&lt;/span&gt;&lt;span class="p"&gt;]&lt;/span&gt;&lt;span class="o"&gt;.&lt;/span&gt;&lt;span class="n"&gt;isalnum&lt;/span&gt;&lt;span class="p"&gt;():&lt;/span&gt;
            &lt;span class="n"&gt;left&lt;/span&gt; &lt;span class="o"&gt;+=&lt;/span&gt; &lt;span class="mi"&gt;1&lt;/span&gt;
        &lt;span class="k"&gt;while&lt;/span&gt; &lt;span class="n"&gt;left&lt;/span&gt; &lt;span class="o"&gt;&amp;lt;&lt;/span&gt; &lt;span class="n"&gt;right&lt;/span&gt; &lt;span class="ow"&gt;and&lt;/span&gt; &lt;span class="ow"&gt;not&lt;/span&gt; &lt;span class="n"&gt;s&lt;/span&gt;&lt;span class="p"&gt;[&lt;/span&gt;&lt;span class="n"&gt;right&lt;/span&gt;&lt;span class="p"&gt;]&lt;/span&gt;&lt;span class="o"&gt;.&lt;/span&gt;&lt;span class="n"&gt;isalnum&lt;/span&gt;&lt;span class="p"&gt;():&lt;/span&gt;
            &lt;span class="n"&gt;right&lt;/span&gt; &lt;span class="o"&gt;-=&lt;/span&gt; &lt;span class="mi"&gt;1&lt;/span&gt;
        &lt;span class="k"&gt;if&lt;/span&gt; &lt;span class="n"&gt;s&lt;/span&gt;&lt;span class="p"&gt;[&lt;/span&gt;&lt;span class="n"&gt;left&lt;/span&gt;&lt;span class="p"&gt;]&lt;/span&gt;&lt;span class="o"&gt;.&lt;/span&gt;&lt;span class="n"&gt;lower&lt;/span&gt;&lt;span class="p"&gt;()&lt;/span&gt; &lt;span class="o"&gt;!=&lt;/span&gt; &lt;span class="n"&gt;s&lt;/span&gt;&lt;span class="p"&gt;[&lt;/span&gt;&lt;span class="n"&gt;right&lt;/span&gt;&lt;span class="p"&gt;]&lt;/span&gt;&lt;span class="o"&gt;.&lt;/span&gt;&lt;span class="n"&gt;lower&lt;/span&gt;&lt;span class="p"&gt;():&lt;/span&gt;
            &lt;span class="k"&gt;return&lt;/span&gt; &lt;span class="kc"&gt;False&lt;/span&gt;
        &lt;span class="n"&gt;left&lt;/span&gt; &lt;span class="o"&gt;+=&lt;/span&gt; &lt;span class="mi"&gt;1&lt;/span&gt;
        &lt;span class="n"&gt;right&lt;/span&gt; &lt;span class="o"&gt;-=&lt;/span&gt; &lt;span class="mi"&gt;1&lt;/span&gt;
    &lt;span class="k"&gt;return&lt;/span&gt; &lt;span class="kc"&gt;True&lt;/span&gt;

&lt;span class="k"&gt;def&lt;/span&gt; &lt;span class="nf"&gt;main&lt;/span&gt;&lt;span class="p"&gt;():&lt;/span&gt;
    &lt;span class="n"&gt;test_cases&lt;/span&gt; &lt;span class="o"&gt;=&lt;/span&gt; &lt;span class="p"&gt;[&lt;/span&gt;
        &lt;span class="s2"&gt;&amp;quot;A man, a plan, a canal: Panama&amp;quot;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;
        &lt;span class="s2"&gt;&amp;quot;race a car&amp;quot;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;
        &lt;span class="s2"&gt;&amp;quot;1A@2!3 23!2@a1&amp;quot;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;
        &lt;span class="s2"&gt;&amp;quot;No &amp;#39;x&amp;#39; in Nixon&amp;quot;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;
        &lt;span class="s2"&gt;&amp;quot;12321&amp;quot;&lt;/span&gt;&lt;span class="p"&gt;,&lt;/span&gt;
    &lt;span class="p"&gt;]&lt;/span&gt;
    &lt;span class="k"&gt;for&lt;/span&gt; &lt;span class="n"&gt;s&lt;/span&gt; &lt;span class="ow"&gt;in&lt;/span&gt; &lt;span class="n"&gt;test_cases&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;
        &lt;span class="nb"&gt;print&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="sa"&gt;f&lt;/span&gt;&lt;span class="s2"&gt;&amp;quot;&lt;/span&gt;&lt;span class="se"&gt;\t&lt;/span&gt;&lt;span class="s2"&gt;string: &lt;/span&gt;&lt;span class="si"&gt;{&lt;/span&gt;&lt;span class="n"&gt;s&lt;/span&gt;&lt;span class="si"&gt;}&lt;/span&gt;&lt;span class="s2"&gt;&amp;quot;&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;
        &lt;span class="n"&gt;result&lt;/span&gt; &lt;span class="o"&gt;=&lt;/span&gt; &lt;span class="n"&gt;is_palindrome&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="n"&gt;s&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;
        &lt;span class="nb"&gt;print&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="sa"&gt;f&lt;/span&gt;&lt;span class="s2"&gt;&amp;quot;&lt;/span&gt;&lt;span class="se"&gt;\n\t&lt;/span&gt;&lt;span class="s2"&gt;Result: &lt;/span&gt;&lt;span class="si"&gt;{&lt;/span&gt;&lt;span class="n"&gt;result&lt;/span&gt;&lt;span class="si"&gt;}&lt;/span&gt;&lt;span class="s2"&gt;&amp;quot;&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;
        &lt;span class="nb"&gt;print&lt;/span&gt;&lt;span class="p"&gt;(&lt;/span&gt;&lt;span class="s2"&gt;&amp;quot;-&amp;quot;&lt;/span&gt; &lt;span class="o"&gt;*&lt;/span&gt; &lt;span class="mi"&gt;100&lt;/span&gt;&lt;span class="p"&gt;)&lt;/span&gt;

&lt;span class="k"&gt;if&lt;/span&gt; &lt;span class="vm"&gt;__name__&lt;/span&gt; &lt;span class="o"&gt;==&lt;/span&gt; &lt;span class="s2"&gt;&amp;quot;__main__&amp;quot;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt;
    &lt;span class="n"&gt;main&lt;/span&gt;&lt;span class="p"&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;Why is this efficient?&lt;/strong&gt;&lt;br&gt;
A naive approach might use nested loops, resulting in O(n²) time complexity. The two pointers method, however, only requires a single pass through the string—O(n) time.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;More examples&lt;/h2&gt;
&lt;p&gt;Here are some classic problems that benefit from the two pointers pattern:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Reversing an array&lt;/strong&gt;&lt;br&gt;
   Reverse an array in place by swapping elements from both ends, moving towards the center.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Pair with given sum in a sorted array&lt;/strong&gt;&lt;br&gt;
   Given a sorted array and a target sum, use two pointers (one at the start, one at the end) to find a pair that adds up to the target.&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;hr&gt;
&lt;h2&gt;Real-world problems&lt;/h2&gt;
&lt;p&gt;The two pointers pattern isn't just for coding interviews. It appears in real-world systems too. For example:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Memory management:&lt;/strong&gt;&lt;br&gt;
  In memory allocation, two pointers can represent the start and end of a memory block. Allocation moves the start pointer forward; deallocation can move it backward, efficiently managing available memory.&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h2&gt;Does your problem match this pattern?&lt;/h2&gt;
&lt;p&gt;Consider using the two pointers technique if:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Linear data structure:&lt;/strong&gt; Your input is a sequence (array, string, linked list).&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Process pairs:&lt;/strong&gt; You need to process or compare elements at two different positions.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Dynamic pointer movement:&lt;/strong&gt; Both pointers can move independently based on certain conditions.&lt;/li&gt;
&lt;/ul&gt;</content><category term="Blog"/><category term="algorithms"/></entry><entry><title>Hello world</title><link href="https://dmb225.github.io/hello-world.html" rel="alternate"/><published>2025-08-20T00:00:00+02:00</published><updated>2025-08-20T00:00:00+02:00</updated><author><name>Dao Malick BENIN</name></author><id>tag:dmb225.github.io,2025-08-20:/hello-world.html</id><content type="html">&lt;p&gt;Hello world! My name is Dao Malick BENIN.&lt;/p&gt;
&lt;p&gt;The goal of this blog is to share what I learn and explore in the world of software engineering.&lt;/p&gt;</content><category term="Blog"/></entry></feed>